<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2025-11-17T01:04:40+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">KK0M4K BLog</title><subtitle>An amazing website.</subtitle><author><name>KK0M4k - Francesco</name></author><entry><title type="html">Git 설명</title><link href="http://localhost:4000/data%20engineering/git/git-github-guide/" rel="alternate" type="text/html" title="Git 설명" /><published>2025-11-16T11:00:00+09:00</published><updated>2025-11-16T11:00:00+09:00</updated><id>http://localhost:4000/data%20engineering/git/git-github-guide</id><content type="html" xml:base="http://localhost:4000/data%20engineering/git/git-github-guide/"><![CDATA[<h3 id="git이란-무엇인가">Git이란 무엇인가?</h3>

<p>Git은 소스 코드의 변경 이력을 관리하는 <strong>분산 버전 관리 시스템</strong>입니다. 코드를 특정 시점의 스냅샷으로 저장하고, 여러 개발자가 동시에 작업하는 환경에서 코드 충돌을 최소화하며 협업할 수 있도록 돕습니다.</p>

<h3 id="로컬-저장소와-원격-저장소-개념-이해하기">로컬 저장소와 원격 저장소: 개념 이해하기</h3>

<p>Git은 ‘분산’ 버전 관리 시스템이라는 점이 중요합니다. 이는 저장소가 내 컴퓨터와 원격 서버에 각각 독립적으로 존재한다는 의미입니다.</p>

<ul>
  <li><strong>로컬 저장소 (Local Repository)</strong>: 개발자 개인의 컴퓨터에 저장되는 저장소입니다. 여기서 자유롭게 코드를 수정하고, 버전을 기록(커밋)할 수 있습니다. 네트워크 연결 없이도 대부분의 작업이 가능하여 매우 빠릅니다.</li>
  <li><strong>원격 저장소 (Remote Repository)</strong>: GitHub, GitLab과 같은 서버에 존재하는 저장소입니다. 여러 개발자가 작업한 내용을 공유하고 통합하는 중앙 허브 역할을 합니다. 팀원들과 코드를 주고받으려면(<code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">pull</code>) 반드시 원격 저장소가 필요합니다.</li>
</ul>

<h3 id="git의-핵심-키워드-이해하기">Git의 핵심 키워드 이해하기</h3>

<p>Git을 효과적으로 사용하려면 몇 가지 핵심 용어와 각 영역 간의 관계를 시각적으로 이해하는 것이 중요합니다. 아래 다이어그램은 Git의 주요 영역과 각 영역을 오가는 핵심 명령어들을 보여줍니다.</p>

<pre><code class="language-mermaid">graph TD
    subgraph "내 컴퓨터 (Local)"
        A["&lt;font size=4&gt;&lt;b&gt;Working Directory&lt;/b&gt;&lt;/font&gt;&lt;br/&gt;(작업 공간)"]
        B["&lt;font size=4&gt;&lt;b&gt;Staging Area&lt;/b&gt;&lt;/font&gt;&lt;br/&gt;(인덱스)"]
        C["&lt;font size=4&gt;&lt;b&gt;Local Repository&lt;/b&gt;&lt;/font&gt;&lt;br/&gt;(.git 디렉토리)"]
    end

    subgraph "원격 서버 (Remote)"
        D[("&lt;font size=4&gt;&lt;b&gt;Remote Repository&lt;/b&gt;&lt;/font&gt;")]
    end

    %% Styling - 각 영역에 의미있는 색상 부여
    style A fill:#fff3b0,stroke:#333,stroke-width:2px
    style B fill:#cce5ff,stroke:#333,stroke-width:2px
    style C fill:#d4edda,stroke:#333,stroke-width:2px
    style D fill:#f8d7da,stroke:#333,stroke-width:2px

    %% Workflow - 데이터 흐름을 나타내는 실선 화살표
    A -- "&lt;b&gt;git add&lt;/b&gt;&lt;br/&gt;(파일 수정 후 스테이징)" --&gt; B
    B -- "&lt;b&gt;git commit&lt;/b&gt;&lt;br/&gt;(스테이징된 변경사항 커밋)" --&gt; C
    C -- "&lt;b&gt;git checkout / reset&lt;/b&gt;&lt;br/&gt;(특정 버전으로 복원)" --&gt; A
    C -- "&lt;b&gt;git push&lt;/b&gt;&lt;br/&gt;(원격 서버에 업로드)" --&gt; D
    D -- "&lt;b&gt;git fetch / pull&lt;/b&gt;&lt;br/&gt;(원격 변경사항 가져오기)" --&gt; C
    
    %% Comparison - 상태를 변경하지 않는 비교 명령어는 점선으로 표현
    A -. "&lt;b&gt;git diff&lt;/b&gt;&lt;br/&gt;(Working vs Staging)" .-&gt; B
    B -. "&lt;b&gt;git diff --staged&lt;/b&gt;&lt;br/&gt;(Staging vs Local Repo)" .-&gt; C
</code></pre>

<ul>
  <li><strong>Working Directory (작업 디렉토리)</strong>: 현재 작업 중인, 실제 파일들로 이루어진 프로젝트 폴더입니다. 여기서 파일을 수정, 생성, 삭제합니다.</li>
  <li><strong>Staging Area (Index)</strong>: 커밋할 변경 사항들을 임시로 모아두는 가상의 공간입니다. <code class="language-plaintext highlighter-rouge">git add</code> 명령을 통해 작업 디렉토리의 변경 사항 중, 커밋에 포함하고 싶은 내용만 선택적으로 추가할 수 있습니다.</li>
  <li><strong>Local Repository (.git)</strong>: 프로젝트의 모든 이력(커밋, 브랜치, 태그 등)이 저장되는 데이터베이스입니다. <code class="language-plaintext highlighter-rouge">git commit</code>을 실행하면 Staging Area에 있던 변경 사항들이 이곳에 영구적인 스냅샷으로 기록됩니다.</li>
  <li><strong>Remote Repository</strong>: GitHub와 같은 원격 서버에 있는 저장소입니다. <code class="language-plaintext highlighter-rouge">git push</code>를 통해 로컬 저장소의 변경 사항을 공유하고, <code class="language-plaintext highlighter-rouge">git pull</code>이나 <code class="language-plaintext highlighter-rouge">git fetch</code>를 통해 다른 사람의 변경 사항을 가져올 수 있습니다.</li>
  <li><strong>HEAD</strong>: 현재 작업중인 브랜치의 가장 최신 커밋을 가리키는 포인터입니다. 즉, 현재 작업 위치를 나타냅니다.</li>
  <li><strong>Commit</strong>: 프로젝트의 특정 시점을 기록한 스냅샷입니다. 각 커밋은 고유한 해시(hash) 값을 가지며, 이전 커밋을 가리키는 포인터를 포함하여 체인처럼 연결됩니다.</li>
  <li><strong>Branch</strong>: 독립적인 개발 라인을 의미합니다. 기존 코드에 영향을 주지 않고 새로운 기능을 개발하거나 버그를 수정할 때 사용합니다.</li>
  <li><strong>Tag</strong>: 특정 커밋에 붙이는 영구적인 이름표입니다. 주로 <code class="language-plaintext highlighter-rouge">v1.0.0</code>과 같이 릴리즈 버전을 표시하는 데 사용됩니다.</li>
</ul>

<hr />

<h3 id="1-git-기본-명령어-마스터하기">1. Git 기본 명령어 마스터하기</h3>

<p>프로젝트를 시작하고 코드를 관리하는 데 필요한 핵심 명령어들입니다.</p>

<h4 id="11-git-저장소-시작하기-git-init">1.1. Git 저장소 시작하기: <code class="language-plaintext highlighter-rouge">git init</code></h4>
<p>내 컴퓨터의 프로젝트 폴더를 Git이 관리하도록 초기화합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>my-awesome-project <span class="o">&amp;&amp;</span> <span class="nb">cd </span>my-awesome-project
git init
</code></pre></div></div>

<h4 id="12-버전-관리에서-제외하기-gitignore">1.2. 버전 관리에서 제외하기: <code class="language-plaintext highlighter-rouge">.gitignore</code></h4>
<p>추적하고 싶지 않은 파일(보안 정보, 로그, 빌드 파일 등)을 지정합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"node_modules/"</span> <span class="o">&gt;&gt;</span> .gitignore
<span class="nb">echo</span> <span class="s2">"*.log"</span> <span class="o">&gt;&gt;</span> .gitignore
</code></pre></div></div>

<h4 id="13-변경-사항-확인-및-스테이징-git-status--git-add">1.3. 변경 사항 확인 및 스테이징: <code class="language-plaintext highlighter-rouge">git status</code> &amp; <code class="language-plaintext highlighter-rouge">git add</code></h4>
<p><code class="language-plaintext highlighter-rouge">git status</code>로 변경된 파일을 확인하고, <code class="language-plaintext highlighter-rouge">git add</code>로 커밋할 파일들을 스테이징 영역(Staging Area)에 추가합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 파일 생성 및 수정</span>
<span class="nb">echo</span> <span class="s2">"# My Project"</span> <span class="o">&gt;</span> README.md

<span class="c"># 변경 상태 확인</span>
git status

<span class="c"># 스테이징 영역에 추가</span>
git add README.md
</code></pre></div></div>

<h4 id="14-변경-이력-저장하기-git-commit">1.4. 변경 이력 저장하기: <code class="language-plaintext highlighter-rouge">git commit</code></h4>
<p>스테이징된 파일들을 하나의 의미 있는 변경 단위(버전)로 묶어 로컬 저장소에 기록합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"Initial commit: Add README.md"</span>
</code></pre></div></div>

<h4 id="15-변경-내용-비교하기-git-diff">1.5. 변경 내용 비교하기: <code class="language-plaintext highlighter-rouge">git diff</code></h4>
<p><code class="language-plaintext highlighter-rouge">git diff</code>는 코드의 변경된 부분을 비교하는 데 사용되는 강력한 명령어입니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 작업 디렉토리와 스테이징 영역 비교</span>
<span class="c"># (아직 add 하지 않은 변경 사항 확인)</span>
git diff

<span class="c"># 2. 스테이징 영역과 마지막 커밋 비교</span>
<span class="c"># (add는 했지만 아직 commit 하지 않은 변경 사항 확인)</span>
git diff <span class="nt">--staged</span>

<span class="c"># 3. 두 브랜치 간의 차이점 비교</span>
git diff main develop
</code></pre></div></div>

<h4 id="16-원격-저장소-다루기-remote-push-fetch-pull">1.6. 원격 저장소 다루기: <code class="language-plaintext highlighter-rouge">remote</code>, <code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">fetch</code>, <code class="language-plaintext highlighter-rouge">pull</code></h4>
<p>로컬 저장소를 원격 저장소와 연결하고, 코드를 주고받습니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">git remote add</code></strong>: 로컬 저장소에 원격 저장소 주소를 추가합니다.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote add origin https://github.com/your-username/my-awesome-project.git
</code></pre></div>    </div>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">git push</code></strong>: 로컬의 커밋 내역을 원격 저장소로 업로드합니다.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push <span class="nt">-u</span> origin main
</code></pre></div>    </div>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">git fetch</code></strong>: 원격 저장소의 최신 변경 이력을 로컬로 가져오기만 합니다. <strong>로컬 브랜치에 자동으로 병합하지 않습니다.</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
</code></pre></div>    </div>
    <p><code class="language-plaintext highlighter-rouge">fetch</code>를 실행하면 <code class="language-plaintext highlighter-rouge">origin/main</code>, <code class="language-plaintext highlighter-rouge">origin/develop</code>과 같은 ‘원격 추적 브랜치’가 업데이트됩니다. 이를 통해 내 로컬 작업에 영향을 주지 않고 팀원들의 작업 내용을 확인할 수 있습니다. (<code class="language-plaintext highlighter-rouge">git diff main origin/main</code>)</p>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">git pull</code></strong>: 원격 저장소의 변경 사항을 가져와 <strong>현재 브랜치와 즉시 병합(merge)</strong>합니다.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull origin main
</code></pre></div>    </div>
    <p><code class="language-plaintext highlighter-rouge">git pull</code>은 내부적으로 <code class="language-plaintext highlighter-rouge">git fetch</code> + <code class="language-plaintext highlighter-rouge">git merge</code>를 순차적으로 실행하는 것과 같습니다. 편리하지만, 원격의 변경 내용을 확인하지 않고 바로 병합하므로 예상치 못한 충돌이 발생할 수 있습니다.</p>
  </li>
</ul>

<blockquote>
  <p><strong><code class="language-plaintext highlighter-rouge">fetch</code> vs <code class="language-plaintext highlighter-rouge">pull</code> 언제 무엇을 쓸까?</strong></p>
  <ul>
    <li><strong>안전하게 원격의 변경 사항을 확인하고 싶을 때</strong>: <code class="language-plaintext highlighter-rouge">git fetch</code>를 사용한 후, <code class="language-plaintext highlighter-rouge">git log origin/main</code> 등으로 변경 내용을 검토하고 원하는 시점에 <code class="language-plaintext highlighter-rouge">git merge origin/main</code>을 실행하세요.</li>
    <li><strong>개인 브랜치에서 작업하거나, 원격의 변경 내용이 확실할 때</strong>: <code class="language-plaintext highlighter-rouge">git pull</code>을 사용하여 과정을 단축할 수 있습니다.</li>
  </ul>
</blockquote>

<h4 id="17-커밋-이력-확인하기-git-log">1.7. 커밋 이력 확인하기: <code class="language-plaintext highlighter-rouge">git log</code></h4>
<p>지금까지의 커밋 이력을 시간순으로 보여줍니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log
</code></pre></div></div>

<hr />

<h3 id="2-효율적인-협업의-시작-브랜치와-병합">2. 효율적인 협업의 시작: 브랜치와 병합</h3>

<p>브랜치(Branch)는 기존 코드에 영향을 주지 않고 독립적인 작업을 수행하기 위해 만드는 ‘코드의 복사본’과 같습니다. 신기능 개발, 버그 수정 등 여러 작업을 동시에 안전하게 진행할 수 있습니다.</p>

<h4 id="21-브랜치-생성-및-확인-git-branch">2.1. 브랜치 생성 및 확인: <code class="language-plaintext highlighter-rouge">git branch</code></h4>
<p>새로운 브랜치를 만들거나, 현재 로컬에 있는 브랜치 목록을 확인할 수 있습니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 'develop' 브랜치 생성</span>
git branch develop

<span class="c"># 'feature/login' 브랜치 생성</span>
git branch feature/login

<span class="c"># 모든 로컬 브랜치 목록 확인</span>
git branch
<span class="c"># 출력:</span>
<span class="c"># * main</span>
<span class="c">#   develop</span>
<span class="c">#   feature/login</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">*</code> 표시는 현재 내가 위치한 브랜치를 의미합니다.</p>

<h4 id="22-브랜치-이동-git-checkout">2.2. 브랜치 이동: <code class="language-plaintext highlighter-rouge">git checkout</code></h4>
<p>다른 브랜치로 작업 공간을 전환합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 'develop' 브랜치로 이동</span>
git checkout develop

<span class="c"># 브랜치를 새로 만들면서 동시에 해당 브랜치로 이동</span>
git checkout <span class="nt">-b</span> feature/signup
<span class="c"># 위 명령어는 아래 두 명령어와 동일합니다.</span>
<span class="c"># git branch feature/signup</span>
<span class="c"># git checkout feature/signup</span>
</code></pre></div></div>

<h4 id="23-로컬에서-브랜치-병합-git-merge">2.3. 로컬에서 브랜치 병합: <code class="language-plaintext highlighter-rouge">git merge</code></h4>
<p>한 브랜치에서 완료된 작업을 다른 브랜치로 합칠 때 사용합니다. 예를 들어 <code class="language-plaintext highlighter-rouge">feature/login</code> 기능 개발이 끝나면 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치로 병합합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 병합의 기준이 될 'develop' 브랜치로 이동</span>
git checkout develop

<span class="c"># 2. 'feature/login' 브랜치의 변경 사항을 'develop'으로 가져와 병합</span>
git merge feature/login
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">merge</code>는 두 브랜치의 이력을 합치면서 새로운 <strong>‘머지 커밋’</strong>을 생성합니다. 이를 통해 누가, 언제, 어떤 브랜치를 병합했는지 명확한 이력을 남길 수 있습니다.</p>

<hr />

<h3 id="3-히스토리-관리-rebase로-깔끔한-커밋-라인-만들기">3. 히스토리 관리: Rebase로 깔끔한 커밋 라인 만들기</h3>

<p><code class="language-plaintext highlighter-rouge">rebase</code>는 <code class="language-plaintext highlighter-rouge">merge</code>와 같이 다른 브랜치의 변경 사항을 합치는 또 다른 방법입니다. <code class="language-plaintext highlighter-rouge">merge</code>가 이력을 그대로 보존하며 합친다면, <code class="language-plaintext highlighter-rouge">rebase</code>는 커밋 히스토리를 <strong>재작성</strong>하여 더 깔끔하고 선형적인 이력을 만듭니다.</p>

<h4 id="31-rebase는-어떻게-동작하는가">3.1. Rebase는 어떻게 동작하는가?</h4>
<p><code class="language-plaintext highlighter-rouge">feature</code> 브랜치에서 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 최신 변경 사항을 반영하고 싶다고 가정해 봅시다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. feature 브랜치에서 작업</span>
git checkout feature/new-feature
<span class="c"># ... 기능 개발 및 커밋 ...</span>

<span class="c"># 2. develop 브랜치의 최신 코드를 가져옴</span>
git fetch origin develop

<span class="c"># 3. feature 브랜치의 베이스를 develop의 최신 커밋으로 변경</span>
git rebase origin/develop
</code></pre></div></div>
<p>이 과정은 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에서 했던 커밋들을 잠시 떼어놓고, <code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 최신 커밋 위로 하나씩 다시 쌓는 것과 같습니다. 그 결과, <code class="language-plaintext highlighter-rouge">feature</code> 브랜치는 마치 <code class="language-plaintext highlighter-rouge">develop</code>의 가장 최신 버전에서 막 작업을 시작한 것처럼 보이게 됩니다.</p>

<h4 id="32-merge-vs-rebase">3.2. Merge vs Rebase</h4>

<p><strong>시나리오 설정</strong>: <code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 <code class="language-plaintext highlighter-rouge">C1</code> 커밋에서 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 생성했습니다. 그 사이 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치에는 팀원이 <code class="language-plaintext highlighter-rouge">C2</code>라는 <strong>새로운 커밋을 완료</strong>했고, 나는 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에 <code class="language-plaintext highlighter-rouge">F1</code>이라는 <strong>커밋을 완료</strong>하여 브랜치가 아래와 같이 갈라진 상태가 되었습니다. <code class="language-plaintext highlighter-rouge">Merge</code>와 <code class="language-plaintext highlighter-rouge">Rebase</code>는 이처럼 <strong>이미 존재하는 커밋들을 통합</strong>하기 위한 명령어들입니다. 이제 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에 <code class="language-plaintext highlighter-rouge">develop</code>의 최신 변경사항(<code class="language-plaintext highlighter-rouge">C2</code>)을 반영해야 합니다.</p>

<ul>
  <li><strong>시나리오 재현 명령어</strong>:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c"># (C1 커밋이 있는 develop 브랜치에서 시작)</span>
  <span class="c"># 1. C1을 기반으로 feature 브랜치 생성 및 이동</span>
  git checkout <span class="nt">-b</span> feature

  <span class="c"># 2. feature 브랜치에서 F1 커밋 작업</span>
  <span class="c"># (...파일 수정...) &amp;&amp; git add . &amp;&amp; git commit -m "F1: 새로운 기능 추가"</span>

  <span class="c"># 3. develop 브랜치로 이동</span>
  git checkout develop

  <span class="c"># 4. develop 브랜치에서 C2 커밋 작업 (다른 팀원이 했다고 가정)</span>
  <span class="c"># (...파일 수정...) &amp;&amp; git add . &amp;&amp; git commit -m "C2: 다른 기능 추가"</span>
</code></pre></div>    </div>
  </li>
  <li><strong>현재 브랜치 상태 (Before)</strong>:
    <pre><code class="language-mermaid">  graph TB
      classDef common fill:#ecf0f1,color:#333
      classDef develop fill:#3498db,color:#fff
      classDef feature fill:#f1c40f,color:#333

      subgraph "현재 브랜치 상태"
          C1(C1):::common --&gt; C2(C2):::develop
          C1 --&gt; F1(F1):::feature
      end
</code></pre>
  </li>
</ul>

<p>이제 이 상태에서 <code class="language-plaintext highlighter-rouge">Merge</code>와 <code class="language-plaintext highlighter-rouge">Rebase</code>를 각각 실행했을 때 히스토리가 어떻게 달라지는지 비교해 보겠습니다.</p>

<ul>
  <li><strong>Merge</strong>: <strong>“두 브랜치의 역사를 그대로 보존하며 합칩니다.”</strong>
    <ul>
      <li><strong>설명</strong>: <code class="language-plaintext highlighter-rouge">Merge</code>는 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치의 작업 내역(<code class="language-plaintext highlighter-rouge">F1</code>)과 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 작업 내역(<code class="language-plaintext highlighter-rouge">C2</code>)을 합쳐, <code class="language-plaintext highlighter-rouge">M3</code>라는 새로운 <strong>병합 커밋(Merge Commit)</strong>을 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치에 생성합니다. 이를 통해 ‘feature 브랜치가 develop에 병합되었다’는 사실이 히스토리에 명확하게 남습니다.</li>
      <li><strong>명령어</strong>:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c"># 1. develop 브랜치로 이동</span>
  git checkout develop
  <span class="c"># 2. feature 브랜치를 현재 브랜치(develop)에 병합</span>
  git merge feature
</code></pre></div>        </div>
      </li>
      <li><strong>결과 (After <code class="language-plaintext highlighter-rouge">git merge</code>)</strong>:
        <pre><code class="language-mermaid">  graph TB
      classDef common fill:#ecf0f1,color:#333
      classDef develop fill:#3498db,color:#fff
      classDef feature fill:#f1c40f,color:#333
      classDef merge fill:#95a5a6,color:#fff

      subgraph "Merge 이후"
          C1_2(C1):::common --&gt; C2_2(C2):::develop --&gt; M3(M3):::merge
          F1_2(F1):::feature --&gt; M3
          C1_2 --&gt; F1_2
      end
</code></pre>
      </li>
    </ul>
  </li>
  <li><strong>Rebase</strong>: <strong>“히스토리를 한 줄로 깔끔하게 재정렬합니다.”</strong>
    <ul>
      <li><strong>설명</strong>: <code class="language-plaintext highlighter-rouge">Rebase</code>는 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치의 시작점(base)을 <code class="language-plaintext highlighter-rouge">C1</code>에서 <code class="language-plaintext highlighter-rouge">develop</code>의 최신 커밋인 <code class="language-plaintext highlighter-rouge">C2</code>로 옮깁니다. 그 다음, <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에 있던 <code class="language-plaintext highlighter-rouge">F1</code> 커밋의 변경사항을 <code class="language-plaintext highlighter-rouge">C2</code> 위에 다시 적용하여 <code class="language-plaintext highlighter-rouge">F1'</code>이라는 <strong>새로운 커밋</strong>을 생성합니다. 그 결과, <code class="language-plaintext highlighter-rouge">feature</code> 브랜치는 마치 처음부터 최신 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치에서 작업을 시작한 것처럼 깨끗한 직선 히스토리를 갖게 됩니다.</li>
      <li><strong>명령어 및 내부 동작</strong>:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c"># 1. feature 브랜치로 이동</span>
  git checkout feature
  <span class="c"># 2. develop 브랜치를 기준으로 현재 브랜치(feature)를 재정렬</span>
  git rebase develop
</code></pre></div>        </div>
        <blockquote>
          <p><strong><code class="language-plaintext highlighter-rouge">git rebase develop</code> 명령어의 내부 동작 단계:</strong></p>
          <ol>
            <li><code class="language-plaintext highlighter-rouge">feature</code> 브랜치에서만 변경된 내용(커밋 <code class="language-plaintext highlighter-rouge">F1</code>)을 임시 공간에 저장합니다.</li>
            <li><code class="language-plaintext highlighter-rouge">feature</code> 브랜치의 포인터를 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 최신 커밋(<code class="language-plaintext highlighter-rouge">C2</code>)으로 이동시킵니다.</li>
            <li>임시 저장했던 변경 내용(<code class="language-plaintext highlighter-rouge">F1</code>)을 현재 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치(<code class="language-plaintext highlighter-rouge">C2</code> 위치) 위에 새로운 커밋(<code class="language-plaintext highlighter-rouge">F1'</code>)으로 적용합니다.</li>
          </ol>
        </blockquote>
      </li>
      <li><strong>결과 (After <code class="language-plaintext highlighter-rouge">git rebase</code>)</strong>:
        <pre><code class="language-mermaid">  graph TB
      classDef common fill:#ecf0f1,color:#333
      classDef develop fill:#3498db,color:#fff
      classDef feature fill:#f1c40f,color:#333

      subgraph "Rebase 이후"
          C1_2(C1):::common --&gt; C2_2(C2):::develop --&gt; F1_r(F1'):::feature
      end
</code></pre>
      </li>
    </ul>
  </li>
</ul>

<blockquote>
  <p><strong>Rebase의 황금률: 절대로 공유된 브랜치를 Rebase하지 마라!</strong>
<code class="language-plaintext highlighter-rouge">main</code>이나 <code class="language-plaintext highlighter-rouge">develop</code>처럼 여러 사람이 함께 사용하는 브랜치를 <code class="language-plaintext highlighter-rouge">rebase</code>하면, 다른 팀원들의 저장소와 히스토리가 꼬여버리는 대재앙이 발생할 수 있습니다. <code class="language-plaintext highlighter-rouge">rebase</code>는 아직 원격에 푸시하지 않았거나, 나만 사용하는 개인 브랜치에만 사용해야 합니다.</p>
</blockquote>

<hr />

<h3 id="4-충돌-해결하기-conflict-resolution">4. 충돌 해결하기 (Conflict Resolution)</h3>

<p>협업 시 여러 개발자가 같은 파일의 같은 부분을 수정하면 <strong>충돌(Conflict)</strong>이 발생합니다. Git은 어떤 코드를 선택해야 할지 모르기 때문에 사용자에게 직접 해결을 요청합니다.</p>

<h4 id="41-충돌은-언제-발생하는가">4.1. 충돌은 언제 발생하는가?</h4>
<ul>
  <li><code class="language-plaintext highlighter-rouge">git merge</code> 또는 <code class="language-plaintext highlighter-rouge">git pull</code> 실행 시</li>
  <li><code class="language-plaintext highlighter-rouge">git rebase</code> 실행 시</li>
</ul>

<h4 id="42-merge-충돌-해결하기">4.2. Merge 충돌 해결하기</h4>
<ol>
  <li><code class="language-plaintext highlighter-rouge">git pull</code> 또는 <code class="language-plaintext highlighter-rouge">git merge</code>를 실행했을 때 충돌이 발생하면, Git은 어떤 파일에서 충돌이 났는지 알려줍니다.</li>
  <li><code class="language-plaintext highlighter-rouge">git status</code>를 통해 충돌 중인 파일 목록을 확인할 수 있습니다.</li>
  <li>
    <p>해당 파일을 열면 아래와 같은 충돌 표시자(conflict marker)가 보입니다. 예를 들어, 로컬 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에서 <code class="language-plaintext highlighter-rouge">git pull origin main</code>을 실행하여 <code class="language-plaintext highlighter-rouge">main</code> 브랜치의 최신 내용을 가져오려 할 때 충돌이 발생한 상황입니다.</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
// 내가 작업한 내용 (현재 브랜치: feature)
const message = "Hello World";
=======
// 상대방이 작업한 내용 (가져오려는 브랜치: origin/main)
const message = "Hello Git";
&gt;&gt;&gt;&gt;&gt;&gt;&gt; origin/main
</code></pre></div>    </div>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</code>: <code class="language-plaintext highlighter-rouge">HEAD</code>가 가리키는 현재 내 브랜치(<code class="language-plaintext highlighter-rouge">feature</code>)에서 변경된 내용을 의미합니다.</li>
      <li><code class="language-plaintext highlighter-rouge">=======</code>: 내 변경 내용과 충돌이 발생한 상대방의 변경 내용을 구분하는 선입니다.</li>
      <li><code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt; origin/main</code>: 병합을 시도한 대상인 <code class="language-plaintext highlighter-rouge">origin/main</code> 브랜치에서 변경된 내용을 의미합니다.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code class="language-plaintext highlighter-rouge">=======</code>, <code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 표시자를 모두 제거하고, 두 코드를 비교하여 최종적으로 남길 코드를 결정하고 파일을 수정합니다.
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// 예시: 두 내용을 모두 반영하기로 결정
const myMessage = "Hello World";
const theirMessage = "Hello Git";
</code></pre></div>    </div>
  </li>
  <li>수정이 완료되면 파일을 저장하고, <code class="language-plaintext highlighter-rouge">git add</code>를 통해 충돌이 해결되었음을 Git에 알립니다.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add &lt;충돌이 발생했던 파일명&gt;
</code></pre></div>    </div>
  </li>
  <li>마지막으로 <code class="language-plaintext highlighter-rouge">git commit</code>을 실행하여 병합을 완료합니다. (<code class="language-plaintext highlighter-rouge">rebase</code> 중이었다면 <code class="language-plaintext highlighter-rouge">git rebase --continue</code>)</li>
</ol>

<h4 id="43-실전-충돌-케이스-및-해결-방법">4.3. 실전 충돌 케이스 및 해결 방법</h4>

<p>충돌은 협업 시 빈번하게 발생하는 일상적인 이슈입니다. 다음은 실제 개발 환경에서 자주 마주치는 구체적인 충돌 케이스들과 각각의 해결 방법입니다.</p>

<h5 id="케이스-1-같은-함수의-다른-부분을-수정한-경우">케이스 1: 같은 함수의 다른 부분을 수정한 경우</h5>
<p><strong>상황</strong>: 팀원 A는 함수의 첫 부분을 수정하고, 팀원 B는 같은 함수의 마지막 부분을 수정했습니다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 원본 코드</span>
<span class="kd">function</span> <span class="nx">calculatePrice</span><span class="p">(</span><span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">let</span> <span class="nx">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">item</span> <span class="k">of</span> <span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">total</span> <span class="o">+=</span> <span class="nx">item</span><span class="p">.</span><span class="nx">price</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="nx">total</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>팀원 A의 변경사항:</p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nx">calculatePrice</span><span class="p">(</span><span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// 입력 검증 추가</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="nx">items</span> <span class="o">||</span> <span class="nx">items</span><span class="p">.</span><span class="nx">length</span> <span class="o">===</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="kd">let</span> <span class="nx">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">item</span> <span class="k">of</span> <span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">total</span> <span class="o">+=</span> <span class="nx">item</span><span class="p">.</span><span class="nx">price</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="nx">total</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>팀원 B의 변경사항:</p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nx">calculatePrice</span><span class="p">(</span><span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">let</span> <span class="nx">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">item</span> <span class="k">of</span> <span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">total</span> <span class="o">+=</span> <span class="nx">item</span><span class="p">.</span><span class="nx">price</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="c1">// 세금 계산 추가</span>
    <span class="kd">const</span> <span class="nx">tax</span> <span class="o">=</span> <span class="nx">total</span> <span class="o">*</span> <span class="mf">0.1</span><span class="p">;</span>
    <span class="k">return</span> <span class="nx">total</span> <span class="o">+</span> <span class="nx">tax</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>충돌 발생 시 표시</strong>:</p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nx">calculatePrice</span><span class="p">(</span><span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
<span class="o">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</span> <span class="nx">HEAD</span>
    <span class="c1">// 입력 검증 추가</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="nx">items</span> <span class="o">||</span> <span class="nx">items</span><span class="p">.</span><span class="nx">length</span> <span class="o">===</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="p">}</span>
<span class="o">=======</span>
<span class="o">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</span> <span class="nx">feature</span><span class="o">/</span><span class="nx">add</span><span class="o">-</span><span class="nx">tax</span>
    <span class="kd">let</span> <span class="nx">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">item</span> <span class="k">of</span> <span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">total</span> <span class="o">+=</span> <span class="nx">item</span><span class="p">.</span><span class="nx">price</span><span class="p">;</span>
    <span class="p">}</span>
<span class="o">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</span> <span class="nx">HEAD</span>
    <span class="k">return</span> <span class="nx">total</span><span class="p">;</span>
<span class="o">=======</span>
    <span class="c1">// 세금 계산 추가</span>
    <span class="kd">const</span> <span class="nx">tax</span> <span class="o">=</span> <span class="nx">total</span> <span class="o">*</span> <span class="mf">0.1</span><span class="p">;</span>
    <span class="k">return</span> <span class="nx">total</span> <span class="o">+</span> <span class="nx">tax</span><span class="p">;</span>
<span class="o">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</span> <span class="nx">feature</span><span class="o">/</span><span class="nx">add</span><span class="o">-</span><span class="nx">tax</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>해결 방법</strong>: 두 변경사항 모두 유용하므로 둘 다 포함시킵니다.</p>
<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nx">calculatePrice</span><span class="p">(</span><span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// 입력 검증 추가</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="nx">items</span> <span class="o">||</span> <span class="nx">items</span><span class="p">.</span><span class="nx">length</span> <span class="o">===</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="kd">let</span> <span class="nx">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">item</span> <span class="k">of</span> <span class="nx">items</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">total</span> <span class="o">+=</span> <span class="nx">item</span><span class="p">.</span><span class="nx">price</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="c1">// 세금 계산 추가</span>
    <span class="kd">const</span> <span class="nx">tax</span> <span class="o">=</span> <span class="nx">total</span> <span class="o">*</span> <span class="mf">0.1</span><span class="p">;</span>
    <span class="k">return</span> <span class="nx">total</span> <span class="o">+</span> <span class="nx">tax</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h5 id="케이스-2-파일-이동이름-변경과-내용-수정이-겹친-경우">케이스 2: 파일 이동/이름 변경과 내용 수정이 겹친 경우</h5>
<p><strong>상황</strong>: 팀원 A는 <code class="language-plaintext highlighter-rouge">utils.js</code> 파일을 <code class="language-plaintext highlighter-rouge">helpers/utils.js</code>로 이동했고, 팀원 B는 원래 위치의 <code class="language-plaintext highlighter-rouge">utils.js</code> 파일 내용을 수정했습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 팀원 A의 작업</span>
git <span class="nb">mv </span>utils.js helpers/utils.js
git commit <span class="nt">-m</span> <span class="s2">"Reorganize: Move utils.js to helpers directory"</span>

<span class="c"># 팀원 B의 작업 (동시에)</span>
<span class="c"># utils.js 파일에 새 함수 추가</span>
<span class="nb">echo</span> <span class="s2">"export function newFunction() { ... }"</span> <span class="o">&gt;&gt;</span> utils.js
git commit <span class="nt">-m</span> <span class="s2">"Add newFunction to utils.js"</span>
</code></pre></div></div>

<p><strong>충돌 발생 시</strong>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONFLICT <span class="o">(</span>rename/modify<span class="o">)</span>: utils.js renamed to helpers/utils.js <span class="k">in </span>HEAD but modified <span class="k">in </span>feature/add-function
</code></pre></div></div>

<p><strong>해결 방법</strong>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 충돌 상태 확인</span>
git status

<span class="c"># 2. 이동된 파일에 변경사항 수동으로 적용</span>
<span class="c"># helpers/utils.js 파일을 열어 팀원 B의 변경사항을 추가</span>

<span class="c"># 3. 기존 위치의 파일 제거 (필요시)</span>
git <span class="nb">rm </span>utils.js

<span class="c"># 4. 변경사항 스테이징 및 커밋</span>
git add helpers/utils.js
git commit <span class="nt">-m</span> <span class="s2">"Merge: Apply changes to moved utils.js file"</span>
</code></pre></div></div>

<h5 id="케이스-3-배열이나-객체의-동일-위치-수정">케이스 3: 배열이나 객체의 동일 위치 수정</h5>
<p><strong>상황</strong>: package.json의 dependencies에 서로 다른 패키지를 동일한 위치에 추가한 경우</p>

<p>원본:</p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"react"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^18.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"axios"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^1.0.0"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>팀원 A의 추가:</p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"react"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^18.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"axios"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^1.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"lodash"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^4.17.0"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>팀원 B의 추가:</p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"react"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^18.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"axios"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^1.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"moment"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^2.29.0"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>충돌 표시</strong>:</p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"react"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^18.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"axios"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^1.0.0"</span><span class="p">,</span><span class="w">
</span><span class="err">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</span><span class="w"> </span><span class="err">HEAD</span><span class="w">
    </span><span class="nl">"lodash"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^4.17.0"</span><span class="w">
</span><span class="err">=======</span><span class="w">
    </span><span class="nl">"moment"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^2.29.0"</span><span class="w">
</span><span class="err">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</span><span class="w"> </span><span class="err">feature/add-moment</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>해결 방법</strong>: 두 패키지 모두 필요하다면 둘 다 추가합니다.</p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"react"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^18.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"axios"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^1.0.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"lodash"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^4.17.0"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"moment"</span><span class="p">:</span><span class="w"> </span><span class="s2">"^2.29.0"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h5 id="케이스-4-삭제된-파일에-대한-수정">케이스 4: 삭제된 파일에 대한 수정</h5>
<p><strong>상황</strong>: 팀원 A는 파일을 삭제했지만, 팀원 B는 같은 파일을 수정했습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 팀원 A</span>
git <span class="nb">rm </span>old-component.js
git commit <span class="nt">-m</span> <span class="s2">"Remove deprecated component"</span>

<span class="c"># 팀원 B (동시에)</span>
<span class="c"># old-component.js 파일 수정</span>
git commit <span class="nt">-m</span> <span class="s2">"Fix bug in old-component.js"</span>
</code></pre></div></div>

<p><strong>충돌 발생 시</strong>:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONFLICT (modify/delete): old-component.js deleted in HEAD and modified in feature/fix-bug
</code></pre></div></div>

<p><strong>해결 방법</strong>: 팀과 상의하여 결정해야 합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 옵션 1: 파일 삭제를 유지 (팀원 A의 의도 존중)</span>
git <span class="nb">rm </span>old-component.js
git add <span class="nt">-u</span>
git commit <span class="nt">-m</span> <span class="s2">"Resolve conflict: Keep file deletion"</span>

<span class="c"># 옵션 2: 파일을 복원하고 수정사항 유지 (팀원 B의 수정이 중요한 경우)</span>
git add old-component.js
git commit <span class="nt">-m</span> <span class="s2">"Resolve conflict: Keep modified file"</span>
</code></pre></div></div>

<h5 id="케이스-5-rebase-중-발생하는-연속적인-충돌">케이스 5: Rebase 중 발생하는 연속적인 충돌</h5>
<p><strong>상황</strong>: feature 브랜치를 develop에 rebase하는 중 여러 커밋에서 충돌이 발생합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase develop
<span class="c"># 첫 번째 커밋에서 충돌 발생</span>
CONFLICT <span class="o">(</span>content<span class="o">)</span>: Merge conflict <span class="k">in </span>app.js
</code></pre></div></div>

<p><strong>해결 과정</strong>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 첫 번째 충돌 해결</span>
<span class="c"># app.js 파일 수정</span>
git add app.js
git rebase <span class="nt">--continue</span>

<span class="c"># 2. 두 번째 커밋에서 또 충돌 발생</span>
CONFLICT <span class="o">(</span>content<span class="o">)</span>: Merge conflict <span class="k">in </span>app.js

<span class="c"># 3. 두 번째 충돌 해결</span>
<span class="c"># app.js 파일 다시 수정</span>
git add app.js
git rebase <span class="nt">--continue</span>

<span class="c"># 4. Rebase 완료될 때까지 반복</span>
</code></pre></div></div>

<p><strong>팁</strong>: Rebase가 너무 복잡해지면 중단할 수 있습니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--abort</span>  <span class="c"># rebase 취소하고 원래 상태로 돌아가기</span>
</code></pre></div></div>

<h5 id="케이스-6-바이너리-파일-충돌">케이스 6: 바이너리 파일 충돌</h5>
<p><strong>상황</strong>: 이미지, PDF 등 바이너리 파일에서 충돌이 발생한 경우</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONFLICT <span class="o">(</span>content<span class="o">)</span>: Merge conflict <span class="k">in </span>logo.png
</code></pre></div></div>

<p><strong>해결 방법</strong>: 바이너리 파일은 텍스트처럼 병합할 수 없으므로 하나를 선택해야 합니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 옵션 1: 내 버전 사용</span>
git checkout <span class="nt">--ours</span> logo.png

<span class="c"># 옵션 2: 상대방 버전 사용</span>
git checkout <span class="nt">--theirs</span> logo.png

<span class="c"># 옵션 3: 완전히 새로운 파일로 교체</span>
<span class="nb">cp</span> /path/to/new/logo.png <span class="nb">.</span>
git add logo.png
</code></pre></div></div>

<h4 id="44-충돌-예방을-위한-모범-사례">4.4. 충돌 예방을 위한 모범 사례</h4>

<ol>
  <li><strong>자주 Pull/Fetch 하기</strong>: 원격 저장소의 변경사항을 자주 가져와 로컬을 최신 상태로 유지합니다.
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
git merge origin/develop  <span class="c"># 또는 git pull</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>작은 단위로 커밋하기</strong>: 큰 변경사항을 작은 논리적 단위로 나누어 커밋하면 충돌 범위가 줄어듭니다.</p>
  </li>
  <li>
    <p><strong>브랜치 수명 줄이기</strong>: Feature 브랜치를 오래 유지하지 말고 빠르게 병합합니다.</p>
  </li>
  <li>
    <p><strong>코드 리뷰 활용</strong>: PR을 통해 충돌 가능성을 사전에 파악합니다.</p>
  </li>
  <li><strong>팀 커뮤니케이션</strong>: 같은 파일을 작업하기 전에 팀원들과 미리 조율합니다.</li>
</ol>

<h4 id="45-rebase-후-강제-푸시-force-push">4.5. Rebase 후 강제 푸시 (<code class="language-plaintext highlighter-rouge">force push</code>)</h4>
<p><code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 <code class="language-plaintext highlighter-rouge">rebase</code>하면 로컬 브랜치의 커밋 히스토리가 변경됩니다. 만약 이 브랜치를 이미 원격에 <code class="language-plaintext highlighter-rouge">push</code>한 적이 있다면, 원격 저장소의 이력과 로컬의 이력이 달라져 일반적인 <code class="language-plaintext highlighter-rouge">git push</code>가 거부됩니다.</p>

<p>이때는 히스토리가 의도적으로 변경되었음을 Git에 알리고 강제로 푸시해야 합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Rebase 후 원격에 강제로 푸시 (주의!)</span>
git push origin feature/my-feature <span class="nt">--force</span>
</code></pre></div></div>

<blockquote>
  <p><strong>더 안전한 강제 푸시: <code class="language-plaintext highlighter-rouge">--force-with-lease</code></strong>
<code class="language-plaintext highlighter-rouge">git push --force</code>는 매우 위험한 옵션입니다. 만약 내가 모르는 사이에 다른 팀원이 해당 브랜치에 새로운 커밋을 푸시했다면, 그 커밋을 덮어쓰고 유실시킬 수 있습니다.</p>

  <p><code class="language-plaintext highlighter-rouge">git push --force-with-lease</code>는 내가 마지막으로 <code class="language-plaintext highlighter-rouge">fetch</code>한 이후 원격 브랜치에 변경 사항이 없는 경우에만 강제 푸시를 허용합니다. 이는 다른 사람의 작업을 덮어쓸 위험을 크게 줄여주는 훨씬 안전한 대안입니다.</p>

  <p><strong>결론: 웬만하면 <code class="language-plaintext highlighter-rouge">--force-with-lease</code>를 사용하세요.</strong></p>
  <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin feature/my-feature <span class="nt">--force-with-lease</span>
</code></pre></div>  </div>
</blockquote>

<hr />

<h3 id="5-코드-리뷰와-협업-풀-리퀘스트pull-request">5. 코드 리뷰와 협업: 풀 리퀘스트(Pull Request)</h3>

<p>로컬에서 <code class="language-plaintext highlighter-rouge">git merge</code>를 통해 직접 브랜치를 합칠 수도 있지만, 팀 프로젝트에서는 <strong>풀 리퀘스트(Pull Request, PR)</strong> 또는 <strong>머지 리퀘스트(Merge Request, MR)</strong> 방식을 사용하는 것이 일반적입니다.</p>

<p>풀 리퀘스트는 내가 작업한 브랜치의 변경 사항을 다른 브랜치에 합치기 전에, <strong>팀원들에게 코드 리뷰를 요청하는 과정</strong>입니다. 이를 통해 코드의 품질을 높이고, 잠재적인 버그를 사전에 발견하며, 팀 전체가 프로젝트의 변경 사항을 쉽게 파악할 수 있습니다.</p>

<p><strong>기본적인 PR 흐름:</strong></p>
<ol>
  <li>로컬에서 기능 개발 완료 후, 원격 저장소에 내 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 <code class="language-plaintext highlighter-rouge">push</code>합니다.</li>
  <li>GitHub/GitLab 사이트에서 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 <code class="language-plaintext highlighter-rouge">develop</code>이나 <code class="language-plaintext highlighter-rouge">main</code> 브랜치로 보내는 풀 리퀘스트를 생성합니다.</li>
  <li>팀원들이 코드를 리뷰하고 의견을 남깁니다.</li>
  <li>리뷰가 완료되고 승인(Approve)되면, PR을 통해 원격 저장소에서 브랜치를 병합합니다.</li>
</ol>

<hr />

<h3 id="6-실전-협업-전략-git-flow-워크플로우">6. 실전 협업 전략: Git-Flow 워크플로우</h3>

<p>Git-Flow는 복잡한 프로젝트에서 여러 개발자가 효율적으로 협업하기 위한 브랜치 관리 전략입니다.</p>

<h4 id="61-git-flow의-핵심-브랜치">6.1. Git-Flow의 핵심 브랜치</h4>
<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">main</code> (또는 <code class="language-plaintext highlighter-rouge">master</code>)</strong>: <strong>배포 가능한</strong> 안정적인 버전의 코드를 관리하는 가장 중요한 브랜치입니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">develop</code></strong>: 다음 버전 배포를 위해 <strong>개발 중인 코드</strong>를 통합하는 브랜치입니다. 모든 기능 개발은 이 브랜치를 기준으로 시작됩니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">feature</code></strong>: 신기능 개발을 위한 브랜치. <code class="language-plaintext highlighter-rouge">develop</code>에서 분기하여 개발 완료 후 <code class="language-plaintext highlighter-rouge">develop</code>으로 병합됩니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">release</code></strong>: 배포 준비를 위한 브랜치. <code class="language-plaintext highlighter-rouge">develop</code>에서 분기하여 버그 수정, 버전 기록 등을 수행한 후 <code class="language-plaintext highlighter-rouge">main</code>과 <code class="language-plaintext highlighter-rouge">develop</code>에 모두 병합됩니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">hotfix</code></strong>: 배포된 <code class="language-plaintext highlighter-rouge">main</code> 브랜치의 긴급 버그를 수정하는 브랜치. <code class="language-plaintext highlighter-rouge">main</code>에서 분기하여 수정 후 <code class="language-plaintext highlighter-rouge">main</code>과 <code class="language-plaintext highlighter-rouge">develop</code>에 모두 병합됩니다.</li>
</ul>

<h4 id="62-feature-브랜치-워크플로우-풀-리퀘스트-활용">6.2. Feature 브랜치 워크플로우 (풀 리퀘스트 활용)</h4>
<p>가장 일반적인 기능 개발 흐름입니다.</p>
<ol>
  <li><strong><code class="language-plaintext highlighter-rouge">develop</code> 브랜치의 최신 코드를 가져옵니다.</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout develop
git pull origin develop
</code></pre></div>    </div>
  </li>
  <li><strong>새로운 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 생성하고 이동합니다.</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> feature/new-awesome-feature
</code></pre></div>    </div>
  </li>
  <li><strong>기능을 개발하고, 작업 내용을 커밋합니다.</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 코드 작업...</span>
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Feat: Implement new awesome feature"</span>
</code></pre></div>    </div>
  </li>
  <li><strong>원격 저장소에 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치를 푸시합니다.</strong>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin feature/new-awesome-feature
</code></pre></div>    </div>
  </li>
  <li><strong>GitHub에서 풀 리퀘스트를 생성합니다.</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">feature/new-awesome-feature</code> 브랜치를 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치로 향하는 PR을 생성합니다.</li>
      <li>팀원들의 코드 리뷰를 거친 후, PR을 병합(Merge)합니다.</li>
    </ul>
  </li>
</ol>

<h4 id="63-릴리즈와-태그-관리-git-tag">6.3. 릴리즈와 태그 관리: <code class="language-plaintext highlighter-rouge">git tag</code></h4>
<p><code class="language-plaintext highlighter-rouge">release</code> 브랜치가 <code class="language-plaintext highlighter-rouge">main</code>으로 병합되어 새로운 버전이 배포될 때, 해당 커밋 지점을 영구적으로 기억하기 위해 <strong>태그(Tag)</strong>를 사용합니다. 태그는 특정 버전을 쉽게 찾아볼 수 있도록 하는 ‘책갈피’와 같습니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. main 브랜치로 이동하여 최신 코드를 받습니다.</span>
git checkout main
git pull origin main

<span class="c"># 2. 'v1.0.0'이라는 이름의 Annotated 태그를 생성합니다.</span>
<span class="c"># -a: 태그 이름, -m: 태그에 대한 설명</span>
git tag <span class="nt">-a</span> v1.0.0 <span class="nt">-m</span> <span class="s2">"Release version 1.0.0"</span>

<span class="c"># 3. 생성한 태그를 원격 저장소에 푸시합니다.</span>
git push origin v1.0.0
</code></pre></div></div>

<h4 id="64-git-flow-플로우-다이어그램-mermaid">6.4. Git-Flow 플로우 다이어그램 (Mermaid)</h4>

<p>Git-Flow는 Vincent Driessen이 제안한 브랜치 관리 전략으로, <code class="language-plaintext highlighter-rouge">main</code>과 <code class="language-plaintext highlighter-rouge">develop</code>이라는 두 개의 핵심 브랜치를 중심으로 운영됩니다. 이 모델은 신규 기능 개발, 릴리즈 준비, 긴급 버그 수정 등 각기 다른 목적을 가진 브랜치들을 체계적으로 분리하고 통합하는 규칙을 제공하여, 복잡한 프로젝트를 효율적으로 관리할 수 있도록 돕습니다.</p>

<p>아래 다이어그램은 Git-Flow의 전체적인 흐름을 시각적으로 보여줍니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">main</code></strong> 브랜치는 항상 배포 가능한 안정 상태를 유지합니다. (진한 남색)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">develop</code></strong> 브랜치는 다음 릴리즈를 위한 개발의 통합 지점입니다. (파란색)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">feature</code></strong> 브랜치는 <code class="language-plaintext highlighter-rouge">develop</code>에서 시작하여 기능 개발이 완료되면 다시 <code class="language-plaintext highlighter-rouge">develop</code>으로 병합됩니다. (노란색)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">release</code></strong> 브랜치는 배포 준비를 위해 <code class="language-plaintext highlighter-rouge">develop</code>에서 분기하며, 완료되면 <code class="language-plaintext highlighter-rouge">main</code>과 <code class="language-plaintext highlighter-rouge">develop</code> 양쪽에 모두 병합됩니다. (초록색)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">hotfix</code></strong> 브랜치는 <code class="language-plaintext highlighter-rouge">main</code>에서 발생한 긴급 버그를 수정하기 위해 분기하며, 완료되면 <code class="language-plaintext highlighter-rouge">main</code>과 <code class="language-plaintext highlighter-rouge">develop</code> 양쪽에 모두 병합됩니다. (빨간색)</li>
</ul>

<pre><code class="language-mermaid">graph TB
    %% Define classes for styling each branch type
    classDef main fill:#2c3e50,color:#fff,stroke-width:2px,stroke:#fff
    classDef develop fill:#3498db,color:#fff,stroke-width:2px,stroke:#fff
    classDef feature fill:#f1c40f,color:#333,stroke-width:2px,stroke:#fff
    classDef release fill:#2ecc71,color:#fff,stroke-width:2px,stroke:#fff
    classDef hotfix fill:#e74c3c,color:#fff,stroke-width:2px,stroke:#fff

    %% Main and Develop branches as the main arteries
    subgraph "main (Production)"
        direction LR
        M1("v1.0"):::main --&gt; M2("v1.0.1"):::main --&gt; M3("v1.1"):::main
    end

    subgraph "develop (Next Release)"
        direction LR
        D1(dev) --&gt; D2(dev) --&gt; D3(dev) --&gt; D4(dev)
        class D1,D2,D3,D4 develop
    end
    
    %% Feature, Release, Hotfix branches
    subgraph "feature"
        F1("feat: new-login"):::feature
    end

    subgraph "release"
        R1("release: v1.1"):::release
    end

    subgraph "hotfix"
        H1("hotfix: v1.0.1"):::hotfix
    end

    %% Define the flows between branches
    M1 -- "Start Project" --&gt; D1
    
    D1 -. "Start Feature" .-&gt; F1
    F1 -- "PR Merge" --&gt; D2

    D2 -. "Start Release" .-&gt; R1
    R1 -- "Merge to main" --&gt; M3
    R1 -- "Merge back" --&gt; D4

    M1 -. "Urgent Bug Found" .-&gt; H1
    H1 -- "Merge to main" --&gt; M2
    H1 -- "Merge back" --&gt; D3
</code></pre>

<hr />

<h3 id="7-자동화-github-actions로-git-저장소-미러링하기">7. 자동화: GitHub Actions로 Git 저장소 미러링하기</h3>

<p>GitHub Actions를 사용하면 한 저장소의 변경 사항을 다른 저장소로 자동으로 동기화하는 <strong>미러링(Mirroring)</strong> 파이프라인을 쉽게 구축할 수 있습니다. 예를 들어, GitHub에 있는 개인 프로젝트를 회사 GitLab이나 다른 원격 저장소로 백업하고 싶을 때 유용합니다.</p>

<h4 id="github-actions-워크플로우-설정하기">GitHub Actions 워크플로우 설정하기</h4>

<ol>
  <li>
    <p><strong>미러링 대상 저장소에 대한 접근 토큰 생성</strong>:
미러링할 대상 저장소(예: 다른 GitHub 저장소, GitLab)에 접근할 수 있는 권한을 가진 <strong>Personal Access Token (PAT)</strong> 을 생성합니다. 토큰은 <code class="language-plaintext highlighter-rouge">repo</code> 스코프를 가져야 합니다.</p>
  </li>
  <li><strong>소스 저장소에 시크릿 등록</strong>:
소스 GitHub 저장소의 <code class="language-plaintext highlighter-rouge">Settings</code> &gt; <code class="language-plaintext highlighter-rouge">Secrets and variables</code> &gt; <code class="language-plaintext highlighter-rouge">Actions</code>로 이동하여, 위에서 생성한 PAT를 <code class="language-plaintext highlighter-rouge">DESTINATION_PAT</code>와 같은 이름의 시크릿으로 등록합니다. 또한, 미러링할 대상 저장소의 URL(사용자 이름 포함)을 <code class="language-plaintext highlighter-rouge">DESTINATION_REPO_URL</code>과 같은 이름으로 등록합니다.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">DESTINATION_PAT</code>: <code class="language-plaintext highlighter-rouge">ghp_xxxxxxxx</code></li>
      <li><code class="language-plaintext highlighter-rouge">DESTINATION_REPO_URL</code>: <code class="language-plaintext highlighter-rouge">https://&lt;your-username&gt;@github.com/your-username/destination-repo.git</code></li>
    </ul>
  </li>
  <li>
    <p><strong>워크플로우 파일 작성</strong>:
소스 저장소의 <code class="language-plaintext highlighter-rouge">.github/workflows/mirror.yml</code> 경로에 아래와 같이 워크플로우 파일을 작성합니다.</p>

    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Mirror Repository</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">main</span> <span class="c1"># main 브랜치에 push될 때마다 실행</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">mirror</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout source repository</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">fetch-depth</span><span class="pi">:</span> <span class="m">0</span> <span class="c1"># 모든 히스토리를 가져옵니다.</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Mirror to destination repository</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">DESTINATION_REPO_URL</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">DESTINATION_PAT</span><span class="pi">:</span> <span class="s">$</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s"># 원격 저장소 URL에 인증 정보를 포함하여 재구성합니다.</span>
          <span class="s"># URL 형식: https://&lt;username&gt;:&lt;token&gt;@github.com/user/repo.git</span>
          <span class="s">DEST_URL_WITH_AUTH=$(echo "$DESTINATION_REPO_URL" | sed "s|://|://$DESTINATION_PAT@|")</span>

          <span class="s"># 대상 저장소로 미러링 푸시를 실행합니다.</span>
          <span class="s"># --mirror 옵션은 모든 브랜치, 태그 등 모든 참조를 그대로 복제합니다.</span>
          <span class="s">git push --mirror "$DEST_URL_WITH_AUTH"</span>
</code></pre></div>    </div>
  </li>
</ol>

<h4 id="워크플로우-상세-설명">워크플로우 상세 설명</h4>

<ul>
  <li><code class="language-plaintext highlighter-rouge">name</code>: 워크플로우의 이름을 지정합니다. GitHub Actions의 ‘Actions’ 탭에서 이 이름으로 워크플로우를 확인할 수 있습니다.</li>
  <li><code class="language-plaintext highlighter-rouge">on</code>: 어떤 이벤트가 발생했을 때 이 워크플로우를 실행할지 정의합니다.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">push</code>: <code class="language-plaintext highlighter-rouge">git push</code> 이벤트가 발생했을 때를 의미합니다.</li>
      <li><code class="language-plaintext highlighter-rouge">branches: [main]</code>: <code class="language-plaintext highlighter-rouge">push</code> 이벤트가 <code class="language-plaintext highlighter-rouge">main</code> 브랜치에서 발생했을 때만 워크플로우를 실행하도록 제한합니다.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">jobs</code>: 워크플로우에서 실행될 하나 이상의 작업(job)들의 모음입니다.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">mirror</code>: <code class="language-plaintext highlighter-rouge">mirror</code>라는 ID를 가진 작업을 정의합니다.</li>
      <li><code class="language-plaintext highlighter-rouge">runs-on: ubuntu-latest</code>: 작업을 실행할 가상 머신(Runner)의 종류를 지정합니다. <code class="language-plaintext highlighter-rouge">ubuntu-latest</code>는 GitHub에서 제공하는 최신 버전의 Ubuntu Linux 환경을 사용하겠다는 의미입니다.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">steps</code>: 작업(job) 내에서 순차적으로 실행될 단계(step)들의 목록입니다.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">name</code>: 각 단계의 이름을 지정합니다. UI에 표시되어 진행 상황을 쉽게 파악할 수 있습니다.</li>
      <li><code class="language-plaintext highlighter-rouge">uses: actions/checkout@v4</code>: 다른 사람이 만들었거나 공식적으로 제공되는 액션(Action)을 재사용하는 키워드입니다. <code class="language-plaintext highlighter-rouge">actions/checkout@v4</code>는 현재 저장소의 코드를 Runner로 내려받는 공식 액션입니다.</li>
      <li><code class="language-plaintext highlighter-rouge">with</code>: <code class="language-plaintext highlighter-rouge">uses</code>로 지정된 액션에 특정 파라미터를 전달할 때 사용합니다.
        <ul>
          <li><code class="language-plaintext highlighter-rouge">fetch-depth: 0</code>: Git 히스토리를 얼마나 깊게 복제할지 결정합니다. <code class="language-plaintext highlighter-rouge">0</code>은 전체 히스토리를 모두 가져오라는 의미로, 저장소의 모든 브랜치와 태그를 완벽하게 미러링하기 위해 필수적인 옵션입니다.</li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">env</code>: 해당 단계(step)에서 사용할 환경 변수를 설정합니다.
        <ul>
          <li><code class="language-plaintext highlighter-rouge">DESTINATION_REPO_URL: $</code>: GitHub 저장소의 <code class="language-plaintext highlighter-rouge">Settings &gt; Secrets</code>에 등록된 <code class="language-plaintext highlighter-rouge">DESTINATION_REPO_URL</code> 값을 이 환경 변수에 할당합니다. <code class="language-plaintext highlighter-rouge">$</code> 구문은 GitHub Actions의 컨텍스트 및 표현식에 접근하는 방법으로, <code class="language-plaintext highlighter-rouge">secrets</code> 컨텍스트에서 값을 안전하게 가져옵니다.</li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">run</code>: Runner의 셸(기본적으로 bash)을 사용하여 직접 명령어를 실행합니다.
        <ul>
          <li><code class="language-plaintext highlighter-rouge">sed "s|://|://$DESTINATION_PAT@|"</code>: <code class="language-plaintext highlighter-rouge">sed</code> 명령어는 문자열을 치환하는 스트림 편집기입니다. 여기서는 <code class="language-plaintext highlighter-rouge">https://github.com/...</code> 같은 URL을 <code class="language-plaintext highlighter-rouge">https://&lt;토큰값&gt;@github.com/...</code> 형태로 동적으로 변경하여, <code class="language-plaintext highlighter-rouge">git push</code> 시 인증을 통과할 수 있도록 합니다.</li>
          <li><code class="language-plaintext highlighter-rouge">git push --mirror "$DEST_URL_WITH_AUTH"</code>: 이 명령어가 미러링의 핵심입니다. <code class="language-plaintext highlighter-rouge">--mirror</code> 옵션은 현재 로컬에 복제된 저장소의 모든 참조(브랜치, 태그 등)를 지정된 원격 저장소로 그대로 복제하여 완벽한 동기화를 수행합니다.</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>]]></content><author><name>KK0M4k - Francesco</name></author><category term="Data Engineering" /><category term="Git" /><category term="git" /><category term="github" /><summary type="html"><![CDATA[Git이란 무엇인가?]]></summary></entry><entry><title type="html">Gemini LLM을 활용한 Tool 호출 구현하기</title><link href="http://localhost:4000/fastmcp/gemini-llm-tool-calling/" rel="alternate" type="text/html" title="Gemini LLM을 활용한 Tool 호출 구현하기" /><published>2025-08-29T10:00:00+09:00</published><updated>2025-08-29T10:00:00+09:00</updated><id>http://localhost:4000/fastmcp/gemini-llm-tool-calling</id><content type="html" xml:base="http://localhost:4000/fastmcp/gemini-llm-tool-calling/"><![CDATA[<p>최신 애플리케이션 아키텍처에서는 특정 기능을 수행하는 모듈을 “Tool”로 정의하고, 필요에 따라 이를 호출하여 사용하는 방식이 보편화되고 있습니다. 예를 들어, 계산기 기능, 데이터베이스 조회, 외부 API 호출 등이 모두 Tool이 될 수 있습니다. <strong>LLM을 이용한 Tool 호출</strong>: LLM이 사용자의 자연어 요청을 해석하여 적절한 Tool을 찾아 호출하는 방식에 대해서 Gemini LLM를 사용하는 케이스를 정리하였다.</p>

<h2 id="gemini-llm을-이용한-tool-호출-방식-llm-based-tool-call">Gemini LLM을 이용한 Tool 호출 방식 (LLM-based Tool Call)</h2>

<p>이 방식은 사용자의 자연어 요청을 LLM이 이해하고, 그 의도에 가장 적합한 Tool과 필요한 인자를 스스로 찾아내어 호출을 요청하는 방식입니다. 개발자는 LLM에게 사용할 수 있는 Tool의 목록과 명세(이름, 설명, 인자)만 제공하면 됩니다.</p>

<h3 id="다이어그램-mermaid">다이어그램 (Mermaid)</h3>

<pre><code class="language-mermaid">sequenceDiagram
    participant User as 사용자
    participant Client as MCP 클라이언트
    participant Gemini as Gemini LLM
    participant MCPServer as FastMCP 서버
    participant Calculator as 계산기 Tool

    User-&gt;&gt;Client: "3 곱하기 5는 얼마야?"
    Client-&gt;&gt;MCPServer: MCP 서버 연결 및 도구 목록 조회
    MCPServer--&gt;&gt;Client: 사용 가능한 도구 목록 반환
    Client-&gt;&gt;Gemini: 프롬프트와 사용 가능한 Tool 목록 전달
    Gemini--&gt;&gt;Client: Tool 호출 요청: multiply_tool(a=3, b=5)
    Client-&gt;&gt;MCPServer: multiply_tool(a=3, b=5) 호출
    MCPServer-&gt;&gt;Calculator: multiply(a=3, b=5) 실행
    Calculator--&gt;&gt;MCPServer: 결과 (15) 반환
    MCPServer--&gt;&gt;Client: 결과 (15) 반환
    Client-&gt;&gt;Gemini: Tool 실행 결과(15) 전달
    Gemini--&gt;&gt;Client: 최종 답변 생성 ("3 곱하기 5의 결과는 15입니다.")
    Client--&gt;&gt;User: 최종 결과 표시
</code></pre>

<h3 id="샘플-코드-및-단계별-설명">샘플 코드 및 단계별 설명</h3>

<p>Gemini API를 사용하여 사용자의 자연어 요청을 처리하는 전체 과정을 단계별로 설명합니다.</p>

<h4 id="1단계-라이브러리-설치"><strong>1단계: 라이브러리 설치</strong></h4>

<p>필요한 라이브러리들을 설치합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>google-generativeai fastmcp mcp
</code></pre></div></div>

<h4 id="2단계-api-키-설정"><strong>2단계: API 키 설정</strong></h4>

<p>Google AI Studio에서 발급받은 API 키를 설정합니다. 보안을 위해 환경 변수를 사용하는 것이 좋습니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">google.generativeai</span> <span class="k">as</span> <span class="n">genai</span>
<span class="kn">import</span> <span class="nn">os</span>

<span class="c1"># API_KEY = "YOUR_API_KEY" # 실제 키로 교체하거나 환경 변수 사용
# genai.configure(api_key=API_KEY)
</span>
<span class="c1"># 또는 환경 변수에서 로드
</span><span class="n">genai</span><span class="p">.</span><span class="n">configure</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"GEMINI_API_KEY"</span><span class="p">])</span>
</code></pre></div></div>

<h4 id="3단계-fastmcp-서버-구현-mcp_serverpy"><strong>3단계: FastMCP 서버 구현 (mcp_server.py)</strong></h4>

<p>계산기 도구를 제공하는 MCP 서버를 구현합니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s">"""
FastMCP 서버 구현
계산기 도구를 제공하는 MCP 서버
"""</span>

<span class="kn">import</span> <span class="nn">asyncio</span>
<span class="kn">from</span> <span class="nn">fastmcp</span> <span class="kn">import</span> <span class="n">FastMCP</span>
<span class="kn">from</span> <span class="nn">calculator_tool</span> <span class="kn">import</span> <span class="n">multiply</span><span class="p">,</span> <span class="n">divide</span><span class="p">,</span> <span class="n">add</span><span class="p">,</span> <span class="n">subtract</span>

<span class="c1"># FastMCP 서버 인스턴스 생성
</span><span class="n">mcp</span> <span class="o">=</span> <span class="n">FastMCP</span><span class="p">(</span><span class="s">"Calculator Server"</span><span class="p">)</span>

<span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">multiply_tool</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
    <span class="s">"""두 정수를 곱한 결과를 반환합니다."""</span>
    <span class="k">return</span> <span class="n">multiply</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>

<span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">divide_tool</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span> <span class="o">|</span> <span class="nb">str</span><span class="p">:</span>
    <span class="s">"""첫 번째 정수를 두 번째 정수로 나눈 결과를 반환합니다."""</span>
    <span class="k">return</span> <span class="n">divide</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>

<span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">add_tool</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
    <span class="s">"""두 정수를 더한 결과를 반환합니다."""</span>
    <span class="k">return</span> <span class="n">add</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>

<span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">subtract_tool</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
    <span class="s">"""첫 번째 정수에서 두 번째 정수를 뺀 결과를 반환합니다."""</span>
    <span class="k">return</span> <span class="n">subtract</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="c1"># 서버 실행
</span>    <span class="n">mcp</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">transport</span><span class="o">=</span><span class="s">"stdio"</span><span class="p">)</span>
</code></pre></div></div>

<h4 id="4단계-mcp-클라이언트-구현-mcp_clientpy"><strong>4단계: MCP 클라이언트 구현 (mcp_client.py)</strong></h4>

<p>MCP 서버와 연동하여 Gemini LLM이 계산기 도구를 사용할 수 있도록 하는 클라이언트를 구현합니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="s">"""
MCP 클라이언트를 사용하여 Gemini LLM과 연동하는 코드
FastMCP 2.0 버전 사용
"""</span>

<span class="kn">import</span> <span class="nn">asyncio</span>
<span class="kn">import</span> <span class="nn">google.generativeai</span> <span class="k">as</span> <span class="n">genai</span>
<span class="kn">import</span> <span class="nn">os</span>
<span class="kn">from</span> <span class="nn">fastmcp.client</span> <span class="kn">import</span> <span class="n">MCPClient</span>

<span class="k">class</span> <span class="nc">MCPGeminiClient</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">server_script_path</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">server_script_path</span> <span class="o">=</span> <span class="n">server_script_path</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">client</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">available_tools</span> <span class="o">=</span> <span class="p">[]</span>
        
        <span class="c1"># Gemini API 설정
</span>        <span class="n">genai</span><span class="p">.</span><span class="n">configure</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="s">"GEMINI_API_KEY"</span><span class="p">])</span>
        
    <span class="k">async</span> <span class="k">def</span> <span class="nf">connect_to_server</span><span class="p">(</span><span class="bp">self</span><span class="p">):</span>
        <span class="s">"""MCP 서버에 연결하고 사용 가능한 도구를 가져옵니다."""</span>
        <span class="bp">self</span><span class="p">.</span><span class="n">client</span> <span class="o">=</span> <span class="n">MCPClient</span><span class="p">()</span>
        
        <span class="c1"># 서버 연결
</span>        <span class="k">await</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span>
            <span class="n">command</span><span class="o">=</span><span class="s">"python"</span><span class="p">,</span>
            <span class="n">args</span><span class="o">=</span><span class="p">[</span><span class="bp">self</span><span class="p">.</span><span class="n">server_script_path</span><span class="p">]</span>
        <span class="p">)</span>
        
        <span class="c1"># 사용 가능한 도구 목록 가져오기
</span>        <span class="bp">self</span><span class="p">.</span><span class="n">available_tools</span> <span class="o">=</span> <span class="k">await</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span><span class="p">.</span><span class="n">list_tools</span><span class="p">()</span>
        
        <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"✅ MCP 서버 연결됨. 사용 가능한 도구: </span><span class="si">{</span><span class="nb">len</span><span class="p">(</span><span class="bp">self</span><span class="p">.</span><span class="n">available_tools</span><span class="p">)</span><span class="si">}</span><span class="s">개"</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">tool</span> <span class="ow">in</span> <span class="bp">self</span><span class="p">.</span><span class="n">available_tools</span><span class="p">:</span>
            <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  - </span><span class="si">{</span><span class="n">tool</span><span class="p">[</span><span class="s">'name'</span><span class="p">]</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">tool</span><span class="p">[</span><span class="s">'description'</span><span class="p">]</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
        
        <span class="k">return</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span>
    
    <span class="k">def</span> <span class="nf">create_gemini_function_declarations</span><span class="p">(</span><span class="bp">self</span><span class="p">):</span>
        <span class="s">"""MCP 도구를 Gemini 함수 선언으로 변환합니다."""</span>
        <span class="n">function_declarations</span> <span class="o">=</span> <span class="p">[]</span>
        
        <span class="k">for</span> <span class="n">tool</span> <span class="ow">in</span> <span class="bp">self</span><span class="p">.</span><span class="n">available_tools</span><span class="p">:</span>
            <span class="n">parameters</span> <span class="o">=</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"object"</span><span class="p">,</span> <span class="s">"properties"</span><span class="p">:</span> <span class="p">{},</span> <span class="s">"required"</span><span class="p">:</span> <span class="p">[]}</span>
            
            <span class="k">if</span> <span class="s">"inputSchema"</span> <span class="ow">in</span> <span class="n">tool</span> <span class="ow">and</span> <span class="s">"properties"</span> <span class="ow">in</span> <span class="n">tool</span><span class="p">[</span><span class="s">"inputSchema"</span><span class="p">]:</span>
                <span class="n">parameters</span><span class="p">[</span><span class="s">"properties"</span><span class="p">]</span> <span class="o">=</span> <span class="n">tool</span><span class="p">[</span><span class="s">"inputSchema"</span><span class="p">][</span><span class="s">"properties"</span><span class="p">]</span>
                <span class="k">if</span> <span class="s">"required"</span> <span class="ow">in</span> <span class="n">tool</span><span class="p">[</span><span class="s">"inputSchema"</span><span class="p">]:</span>
                    <span class="n">parameters</span><span class="p">[</span><span class="s">"required"</span><span class="p">]</span> <span class="o">=</span> <span class="n">tool</span><span class="p">[</span><span class="s">"inputSchema"</span><span class="p">][</span><span class="s">"required"</span><span class="p">]</span>
            
            <span class="n">function_declarations</span><span class="p">.</span><span class="n">append</span><span class="p">({</span>
                <span class="s">"name"</span><span class="p">:</span> <span class="n">tool</span><span class="p">[</span><span class="s">"name"</span><span class="p">],</span>
                <span class="s">"description"</span><span class="p">:</span> <span class="n">tool</span><span class="p">[</span><span class="s">"description"</span><span class="p">],</span>
                <span class="s">"parameters"</span><span class="p">:</span> <span class="n">parameters</span>
            <span class="p">})</span>
        
        <span class="k">return</span> <span class="n">function_declarations</span>
    
    <span class="k">async</span> <span class="k">def</span> <span class="nf">call_mcp_tool</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">function_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">function_args</span><span class="p">:</span> <span class="nb">dict</span><span class="p">):</span>
        <span class="s">"""MCP 도구를 호출합니다."""</span>
        <span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span><span class="p">.</span><span class="n">call_tool</span><span class="p">(</span><span class="n">function_name</span><span class="p">,</span> <span class="n">function_args</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">result</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"content"</span><span class="p">,</span> <span class="p">[{}])[</span><span class="mi">0</span><span class="p">].</span><span class="n">get</span><span class="p">(</span><span class="s">"text"</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>
    
    <span class="k">async</span> <span class="k">def</span> <span class="nf">handle_user_request</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">user_prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="s">"""사용자 요청을 처리합니다."""</span>
        <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"👤 사용자: </span><span class="si">{</span><span class="n">user_prompt</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
        
        <span class="c1"># Gemini 모델 설정
</span>        <span class="n">model</span> <span class="o">=</span> <span class="n">genai</span><span class="p">.</span><span class="n">GenerativeModel</span><span class="p">(</span>
            <span class="n">model_name</span><span class="o">=</span><span class="s">"gemini-1.5-flash"</span><span class="p">,</span>
            <span class="n">tools</span><span class="o">=</span><span class="bp">self</span><span class="p">.</span><span class="n">create_gemini_function_declarations</span><span class="p">()</span>
        <span class="p">)</span>
        
        <span class="k">try</span><span class="p">:</span>
            <span class="n">response</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">generate_content</span><span class="p">(</span>
                <span class="n">user_prompt</span><span class="p">,</span>
                <span class="n">tool_config</span><span class="o">=</span><span class="p">{</span><span class="s">"function_calling_config"</span><span class="p">:</span> <span class="p">{</span><span class="s">"mode"</span><span class="p">:</span> <span class="s">"AUTO"</span><span class="p">}}</span>
            <span class="p">)</span>
            
            <span class="c1"># 함수 호출이 요청된 경우
</span>            <span class="k">if</span> <span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span> <span class="ow">and</span>
                <span class="nb">len</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="ow">and</span>
                <span class="nb">hasattr</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="s">'function_call'</span><span class="p">)</span> <span class="ow">and</span>
                <span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">function_call</span><span class="p">):</span>
                
                <span class="n">function_call</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">function_call</span>
                <span class="n">function_name</span> <span class="o">=</span> <span class="n">function_call</span><span class="p">.</span><span class="n">name</span>
                <span class="n">function_args</span> <span class="o">=</span> <span class="p">{</span><span class="n">key</span><span class="p">:</span> <span class="n">value</span> <span class="k">for</span> <span class="n">key</span><span class="p">,</span> <span class="n">value</span> <span class="ow">in</span> <span class="n">function_call</span><span class="p">.</span><span class="n">args</span><span class="p">.</span><span class="n">items</span><span class="p">()}</span>
                
                <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"🔧 MCP 도구 호출: </span><span class="si">{</span><span class="n">function_name</span><span class="si">}</span><span class="s">(</span><span class="si">{</span><span class="n">function_args</span><span class="si">}</span><span class="s">)"</span><span class="p">)</span>
                
                <span class="c1"># MCP 도구 실행
</span>                <span class="n">tool_result</span> <span class="o">=</span> <span class="k">await</span> <span class="bp">self</span><span class="p">.</span><span class="n">call_mcp_tool</span><span class="p">(</span><span class="n">function_name</span><span class="p">,</span> <span class="n">function_args</span><span class="p">)</span>
                
                <span class="c1"># 결과를 포함하여 다시 Gemini에 요청
</span>                <span class="n">function_response_content</span> <span class="o">=</span> <span class="n">genai</span><span class="p">.</span><span class="n">protos</span><span class="p">.</span><span class="n">Content</span><span class="p">(</span>
                    <span class="n">parts</span><span class="o">=</span><span class="p">[</span><span class="n">genai</span><span class="p">.</span><span class="n">protos</span><span class="p">.</span><span class="n">Part</span><span class="p">(</span>
                        <span class="n">function_response</span><span class="o">=</span><span class="n">genai</span><span class="p">.</span><span class="n">protos</span><span class="p">.</span><span class="n">FunctionResponse</span><span class="p">(</span>
                            <span class="n">name</span><span class="o">=</span><span class="n">function_name</span><span class="p">,</span>
                            <span class="n">response</span><span class="o">=</span><span class="p">{</span><span class="s">"result"</span><span class="p">:</span> <span class="n">tool_result</span><span class="p">}</span>
                        <span class="p">)</span>
                    <span class="p">)],</span>
                    <span class="n">role</span><span class="o">=</span><span class="s">"user"</span>
                <span class="p">)</span>
                
                <span class="n">response</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">generate_content</span><span class="p">([</span>
                    <span class="n">genai</span><span class="p">.</span><span class="n">protos</span><span class="p">.</span><span class="n">Content</span><span class="p">(</span><span class="n">parts</span><span class="o">=</span><span class="p">[</span><span class="n">genai</span><span class="p">.</span><span class="n">protos</span><span class="p">.</span><span class="n">Part</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="n">user_prompt</span><span class="p">)],</span> <span class="n">role</span><span class="o">=</span><span class="s">"user"</span><span class="p">),</span>
                    <span class="n">response</span><span class="p">.</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">content</span><span class="p">,</span>
                    <span class="n">function_response_content</span>
                <span class="p">])</span>
            
            <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"🤖 Gemini: </span><span class="si">{</span><span class="n">response</span><span class="p">.</span><span class="n">text</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
            
        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"🤖 Gemini: 오류가 발생했습니다: </span><span class="si">{</span><span class="nb">str</span><span class="p">(</span><span class="n">e</span><span class="p">)</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
    
    <span class="k">async</span> <span class="k">def</span> <span class="nf">close</span><span class="p">(</span><span class="bp">self</span><span class="p">):</span>
        <span class="s">"""클라이언트 연결을 종료합니다."""</span>
        <span class="k">if</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span><span class="p">:</span>
            <span class="k">await</span> <span class="bp">self</span><span class="p">.</span><span class="n">client</span><span class="p">.</span><span class="n">close</span><span class="p">()</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="s">"""메인 실행 함수"""</span>
    <span class="n">server_script</span> <span class="o">=</span> <span class="s">"mcp_server.py"</span>
    <span class="n">client</span> <span class="o">=</span> <span class="n">MCPGeminiClient</span><span class="p">(</span><span class="n">server_script</span><span class="p">)</span>
    
    <span class="k">try</span><span class="p">:</span>
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">connect_to_server</span><span class="p">()</span>
        
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">handle_user_request</span><span class="p">(</span><span class="s">"안녕 제미니"</span><span class="p">)</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"-"</span> <span class="o">*</span> <span class="mi">40</span><span class="p">)</span>
        
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">handle_user_request</span><span class="p">(</span><span class="s">"3이랑 5를 곱해줘"</span><span class="p">)</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"-"</span> <span class="o">*</span> <span class="mi">40</span><span class="p">)</span>
        
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">handle_user_request</span><span class="p">(</span><span class="s">"100을 4로 나누면 결과가 뭐야?"</span><span class="p">)</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"-"</span> <span class="o">*</span> <span class="mi">40</span><span class="p">)</span>
        
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">handle_user_request</span><span class="p">(</span><span class="s">"50 더하기 30에서 15를 뺀 값은?"</span><span class="p">)</span>
        
    <span class="k">finally</span><span class="p">:</span>
        <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="n">close</span><span class="p">()</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">main</span><span class="p">())</span>
</code></pre></div></div>

<h3 id="실행-방법">실행 방법</h3>

<ol>
  <li><strong>환경 변수 설정</strong>: <code class="language-plaintext highlighter-rouge">GEMINI_API_KEY</code> 환경 변수에 Google AI Studio에서 발급받은 API 키를 설정합니다.</li>
  <li><strong>서버 실행</strong>: <code class="language-plaintext highlighter-rouge">python mcp_server.py</code>로 MCP 서버를 실행할 수도 있지만, 클라이언트에서 자동으로 서버를 시작합니다.</li>
  <li><strong>클라이언트 실행</strong>: <code class="language-plaintext highlighter-rouge">python mcp_client.py</code>로 MCP 클라이언트를 실행합니다.</li>
</ol>

<p>MCP 아키텍처를 사용함으로써 도구의 모듈화가 향상되고, 서로 다른 LLM 클라이언트들이 동일한 MCP 서버의 도구를 공유할 수 있게 됩니다.</p>

<h3 id="별첨">별첨</h3>

<p>1.MCP Tool를 Gemini 함수 선언으로 변환하는 코드에서 주요 파라미텅에 대한 추가 설명</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  - inputSchema: MCP 도구의 입력 스키마를 정의하는 JSON Schema 형식. 도구가 받을 수 있는 파라미터들의 타입과 제약조건을 명시
  - properties: 각 파라미터의 상세 정의 (타입, 설명, 기본값 등)
  - required: 필수 파라미터 목록 (배열 형식)
  - description: 도구의 기능을 설명하는 텍스트

  예시 
  - MCP 도구 형식
  {
      "name": "multiply_tool",
      "description": "두 정수를 곱한 결과를 반환합니다.",
      "inputSchema": {
          "type": "object",
          "properties": {
              "a": {"type": "integer", "description": "첫 번째 정수"},
              "b": {"type": "integer", "description": "두 번째 정수"}
          },
          "required": ["a", "b"]
      }
  }

  - Gemini가 요구하는 형식
  {
      "name": "multiply_tool",
      "description": "두 정수를 곱합니다",
      "parameters": {  # Gemini는 'parameters' 사용
          "type": "object",
          "properties": {...},
          "required": [...]
      }
  }

  # 위 MCP 도구 형식을 Gemini가 인식하는 형식으로 변환이 필요함
</code></pre></div></div>

<ol>
  <li>generate_content() 함수</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Gemini LLM에게 텍스트나 멀티모달 콘텐츠를 입력으로 제공하고, AI가 생성한 응답을 받아오는 함수이며, 리스트로 여러 Content 객체를 전달할 때는 순서가 중요합니다.

  response = model.generate_content([
      genai.protos.Content(parts=[genai.protos.Part(text=user_prompt)],role="user"),
      response.candidates[0].content,
      function_response_content
  ])

  이 순서는 대화의 시간순 흐름을 나타냅니다:

  1. 첫 번째: 사용자의 원래 질문 (user_prompt)
  2. 두 번째: Gemini의 함수 호출 응답 (response.candidates[0].content)
  3. 세 번째: 함수 실행 결과 (function_response_content)

  이 순서를 바꾸면 Gemini가 대화 맥락을 잘못 이해할 수 있어서 올바른 응답을 생성하지 못할 수 있습니다. 대화형 AI에서는 메시지의 시간적 순서가 매우 중요하기 때문입니다.

</code></pre></div></div>]]></content><author><name>KK0M4k - Francesco</name></author><category term="fastmcp" /><category term="fastmcp" /><category term="gemini" /><category term="tool-calling" /><category term="llm" /><summary type="html"><![CDATA[최신 애플리케이션 아키텍처에서는 특정 기능을 수행하는 모듈을 “Tool”로 정의하고, 필요에 따라 이를 호출하여 사용하는 방식이 보편화되고 있습니다. 예를 들어, 계산기 기능, 데이터베이스 조회, 외부 API 호출 등이 모두 Tool이 될 수 있습니다. LLM을 이용한 Tool 호출: LLM이 사용자의 자연어 요청을 해석하여 적절한 Tool을 찾아 호출하는 방식에 대해서 Gemini LLM를 사용하는 케이스를 정리하였다.]]></summary></entry><entry><title type="html">pyenv 환경에서 MCP 서버 설정하기: Claude Desktop &amp;amp; Cursor AI</title><link href="http://localhost:4000/fastmcp/fastmcp-pyenv-calculator-guide/" rel="alternate" type="text/html" title="pyenv 환경에서 MCP 서버 설정하기: Claude Desktop &amp;amp; Cursor AI" /><published>2025-08-27T10:00:00+09:00</published><updated>2025-08-27T10:00:00+09:00</updated><id>http://localhost:4000/fastmcp/fastmcp-pyenv-calculator-guide</id><content type="html" xml:base="http://localhost:4000/fastmcp/fastmcp-pyenv-calculator-guide/"><![CDATA[<p>Claude Desktop과 Cursor AI에서 pyenv 가상환경을 사용하는 MCP(Model Context Protocol) 서버를 설정하는 방법입니다. 저는 주로 pyenv를 사용하여 가상환경을 만들고, 필요한 패키지를 설치하여 개발을 합니다. 일반적으로 uv를 사용한 설정은 있지만, pyenv는 없어서 간략히 정리하여 포스팅하였습니다.</p>

<h2 id="mcp-서버-설정-파일">MCP 서버 설정 파일</h2>

<p>Claude Desktop이나 Cursor AI의 파일에 다음과 같이 설정합니다:</p>
<ul>
  <li>Cursor AI: mcp.json</li>
  <li>Claude Desktop: claude_desktop_config.json</li>
</ul>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"mcpServers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"calculator"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/bin/zsh"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"-c"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"PYENV_VERSION=mcp-venv pyenv exec python /path/to/your/mcp_server.py"</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="설정-해석">설정 해석</h3>
<ul>
  <li><code class="language-plaintext highlighter-rouge">PYENV_VERSION=mcp-venv</code>: pyenv 가상환경 지정</li>
  <li><code class="language-plaintext highlighter-rouge">pyenv exec python</code>: 해당 가상환경의 Python 실행</li>
  <li><code class="language-plaintext highlighter-rouge">/bin/bash -c</code>: 환경변수와 함께 명령 실행</li>
</ul>

<h2 id="빠른-시작-가이드">빠른 시작 가이드</h2>

<h3 id="1-pyenv-가상환경-생성">1. pyenv 가상환경 생성</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyenv virtualenv 3.10.4 mcp-venv
<span class="nv">PYENV_VERSION</span><span class="o">=</span>mcp-venv pip <span class="nb">install </span>fastmcp
</code></pre></div></div>

<h3 id="2-샘플-mcp-서버-예제">2. 샘플 MCP 서버 (예제)</h3>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># mcp_server.py
</span><span class="kn">from</span> <span class="nn">fastmcp</span> <span class="kn">import</span> <span class="n">FastMCP</span>

<span class="n">mcp</span> <span class="o">=</span> <span class="n">FastMCP</span><span class="p">(</span><span class="s">"name=calculator"</span><span class="p">)</span>

<span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span>
<span class="k">def</span> <span class="nf">multiply</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="nb">float</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">float</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="s">""" multiply function """</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">*</span> <span class="n">b</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="s">"__main__"</span><span class="p">:</span>
    <span class="n">mcp</span><span class="p">.</span><span class="n">run</span><span class="p">()</span>

</code></pre></div></div>

<h3 id="3-클라이언트-설정">3. 클라이언트 설정</h3>
<p>위의 JSON 설정을 Claude Desktop 또는 Cursor AI의 설정 파일에 추가하면 완료입니다. 설정 파일을 인식시키기 위해서 재 실행이 필요합니다.</p>

<h2 id="주의사항">주의사항</h2>
<ul>
  <li>경로는 반드시 절대경로 사용</li>
  <li>pyenv가 시스템에 설치되어 있어야 함</li>
  <li>가상환경 이름이 정확해야 함</li>
</ul>]]></content><author><name>KK0M4k - Francesco</name></author><category term="fastmcp" /><category term="fastmcp" /><summary type="html"><![CDATA[Claude Desktop과 Cursor AI에서 pyenv 가상환경을 사용하는 MCP(Model Context Protocol) 서버를 설정하는 방법입니다. 저는 주로 pyenv를 사용하여 가상환경을 만들고, 필요한 패키지를 설치하여 개발을 합니다. 일반적으로 uv를 사용한 설정은 있지만, pyenv는 없어서 간략히 정리하여 포스팅하였습니다.]]></summary></entry><entry><title type="html">AWS NAT Gateway와 VPC Flow Log를 이용한 IP 추적 분석</title><link href="http://localhost:4000/aws/analyzing-vpc-flow-logs-with-nat-gateway/" rel="alternate" type="text/html" title="AWS NAT Gateway와 VPC Flow Log를 이용한 IP 추적 분석" /><published>2025-08-26T00:00:00+09:00</published><updated>2025-08-26T00:00:00+09:00</updated><id>http://localhost:4000/aws/analyzing-vpc-flow-logs-with-nat-gateway</id><content type="html" xml:base="http://localhost:4000/aws/analyzing-vpc-flow-logs-with-nat-gateway/"><![CDATA[<p>AWS 환경에서 보안을 강화하기 위해 데이터베이스나 백엔드 애플리케이션 서버 등 외부에서 직접 접근할 필요가 없는 리소스들은 보통 Private Subnet에 배치합니다. 하지만 이 서버들도 소프트웨어 패치, 외부 API 호출 등 인터넷으로 아웃바운드 통신이 필요한 경우가 발생합니다.</p>

<p>이때 Private Subnet의 인스턴스들이 인터넷과 안전하게 통신할 수 있도록 해주는 핵심 서비스가 바로 <strong>NAT(Network Address Translation) Gateway</strong>입니다. NAT Gateway는 여러 사설 IP 주소를 단일 공인 IP 주소로 변환하여 외부와 통신하게 해주는 역할을 합니다.</p>

<p>그런데 여기서 한 가지 궁금증이 생깁니다. “만약 여러 EC2 인스턴스가 동시에 NAT Gateway를 통해 외부와 통신했다면, 어떤 인스턴스가 어느 외부 서비스와 통신했는지 어떻게 알 수 있을까?” 특히 보안 감사, 침해사고, 그리고 트러블슈팅 상황에서 이러한 트래픽 추적은 매우 중요합니다. VPC의 네트워크 트래픽을 모니터링하는 <strong>VPC Flow Logs</strong>를 활용하여 NAT Gateway를 통과하는 트래픽의 원래 출발지(EC2 인스턴스)를 추적, 그리고 EKS POD에서 출발지를 추적하고 분석하는 방법을 구체적인 데이터 플로우와 로그 샘플을 통해 알아보겠습니다.</p>

<h2 id="핵심-원리-vpc-flow-logs는-어떻게-동작하는가">핵심 원리: VPC Flow Logs는 어떻게 동작하는가?</h2>

<p>먼저 VPC Flow Logs는 VPC 내의 네트워크 인터페이스(ENI)를 오가는 IP 트래픽을 기록한다는 점을 이해해야 합니다. 무엇보다도, <strong>하나의 로그 항목에 ‘EC2의 사설 IP’와 ‘NAT Gateway의 공인 IP’가 동시에 기록되지는 않습니다.</strong>  트래픽이 흘러가는 경로에 있는 각기 다른 ENI(EC2의 ENI, NAT Gateway의 ENI)에서 생성된 로그들을 <strong>연결(correlate)</strong>하여 간접적으로 전체 흐름을 파악해야 합니다.</p>

<h2 id="전체-통신-흐름도">전체 통신 흐름도</h2>

<p>아래 다이어그램은 Private Subnet의 EC2 인스턴스가 NAT Gateway를 통해 외부 서버와 통신하는 전체 과정을 보여줍니다.</p>

<pre><code class="language-mermaid">graph TD
    subgraph "VPC (10.0.0.0/16)"
        subgraph "Private Subnet (10.0.1.0/24)"
            EC2[EC2 Instance&lt;br&gt;eni-ec2&lt;br&gt;10.0.1.10]
        end
        subgraph "Public Subnet (10.0.0.0/24)"
            NATGW[NAT Gateway&lt;br&gt;eni-natgw&lt;br&gt;Private: 10.0.0.5&lt;br&gt;Public: 203.0.113.12]
        end
        IGW[Internet Gateway]
        
        EC2 -- "(1) Request (src: 10.0.1.10)" --&gt; NATGW
        NATGW -- "(2) SNAT (src: 203.0.113.12)" --&gt; IGW
    end

    ExternalServer[External Server&lt;br&gt;209.10.20.30]
    IGW -- "(3) Request to External" --&gt; ExternalServer
    ExternalServer -- "(4) Response to NAT GW" --&gt; IGW
    IGW -- "(5) Response" --&gt; NATGW
    NATGW -- "(6) DNAT (dst: 10.0.1.10)" --&gt; EC2

    style EC2 fill:#f9f,stroke:#333,stroke-width:2px
    style NATGW fill:#ccf,stroke:#333,stroke-width:2px
    style ExternalServer fill:#9cf,stroke:#333,stroke-width:2px
</code></pre>

<h3 id="시나리오-가정">시나리오 가정</h3>

<p>분석을 위해 아래와 같은 환경을 가정합니다.</p>

<ul>
  <li><strong>EC2 인스턴스</strong>: 사설 IP <code class="language-plaintext highlighter-rouge">10.0.1.10</code>, ENI ID <code class="language-plaintext highlighter-rouge">eni-ec2</code></li>
  <li><strong>NAT Gateway</strong>: 사설 IP <code class="language-plaintext highlighter-rouge">10.0.0.5</code>, 할당된 공인 IP <code class="language-plaintext highlighter-rouge">203.0.113.12</code>, ENI ID <code class="language-plaintext highlighter-rouge">eni-natgw</code></li>
  <li><strong>외부 서버</strong>: 공인 IP <code class="language-plaintext highlighter-rouge">209.10.20.30</code></li>
  <li><strong>상황</strong>: EC2 인스턴스가 외부 서버의 443 포트로 HTTPS 요청</li>
</ul>

<h2 id="단계별-데이터-플로우와-vpc-flow-log-분석">단계별 데이터 플로우와 VPC Flow Log 분석</h2>

<h3 id="1-outbound-ec2-인스턴스--nat-gateway">1. Outbound: EC2 인스턴스 → NAT Gateway</h3>

<p>EC2 인스턴스(<code class="language-plaintext highlighter-rouge">10.0.1.10</code>)가 외부 서버(<code class="language-plaintext highlighter-rouge">209.10.20.30</code>)로 요청을 시작합니다. Private Subnet의 라우팅 테이블은 <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 트래픽을 NAT Gateway로 전달하도록 설정되어 있습니다.</p>

<h4 id="-ec2-인스턴스-eni-eni-ec2-로그">📜 EC2 인스턴스 ENI (<code class="language-plaintext highlighter-rouge">eni-ec2</code>) 로그</h4>
<p>EC2에서 나가는 트래픽입니다. 출발지(<code class="language-plaintext highlighter-rouge">srcaddr</code>)는 EC2의 사설 IP, 목적지(<code class="language-plaintext highlighter-rouge">dstaddr</code>)는 최종 목적지인 외부 서버의 공인 IP로 기록됩니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version account-id interface-id srcaddr dstaddr srcport dstport protocol packets bytes start end action log-status</span>
2 123456789012 eni-ec2 10.0.1.10 209.10.20.30 49152 443 6 10 480 &lt;timestamp&gt; &lt;timestamp&gt; ACCEPT OK
</code></pre></div></div>

<h4 id="-nat-gateway-eni-eni-natgw-로그">📜 NAT Gateway ENI (<code class="language-plaintext highlighter-rouge">eni-natgw</code>) 로그</h4>
<p>동일한 트래픽이 NAT Gateway로 들어옵니다. 이 로그에서도 출발지는 <strong>여전히 EC2의 사설 IP</strong>입니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version account-id interface-id srcaddr dstaddr srcport dstport protocol packets bytes start end action log-status</span>
2 123456789012 eni-natgw 10.0.1.10 209.10.20.30 49152 443 6 10 480 &lt;timestamp&gt; &lt;timestamp&gt; ACCEPT OK
</code></pre></div></div>

<blockquote>
  <p><strong>Point!</strong> 
이 단계까지의 로그에서는 NAT Gateway의 공인 IP(<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)가 전혀 나타나지 않습니다.</p>
</blockquote>

<h3 id="2-outbound-nat-gateway--외부-서버-snat">2. Outbound: NAT Gateway → 외부 서버 (SNAT)</h3>

<p>NAT Gateway는 트래픽의 출발지 IP 주소를 자신의 공인 IP(<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)로 변환(Source NAT)한 후, Internet Gateway를 통해 외부 서버로 전송합니다. <strong>이 변환 과정 자체는 VPC Flow Log에 기록되지 않습니다.</strong></p>

<h3 id="3-inbound-외부-서버--nat-gateway">3. Inbound: 외부 서버 → NAT Gateway</h3>

<p>외부 서버는 요청의 출발지였던 NAT Gateway의 공인 IP(<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)로 응답을 보냅니다.</p>

<h3 id="4-inbound-nat-gateway--ec2-인스턴스-dnat">4. Inbound: NAT Gateway → EC2 인스턴스 (DNAT)</h3>

<p>응답 트래픽이 NAT Gateway에 도착하면, NAT Gateway는 자신의 상태 테이블(state table)을 참조하여 목적지 IP 주소를 원래 요청을 보냈던 EC2 인스턴스의 사설 IP(<code class="language-plaintext highlighter-rouge">10.0.1.10</code>)로 다시 변환(Destination NAT)합니다.</p>

<h4 id="-nat-gateway-eni-eni-natgw-로그-1">📜 NAT Gateway ENI (<code class="language-plaintext highlighter-rouge">eni-natgw</code>) 로그</h4>
<p>NAT Gateway에서 EC2로 나가는 응답 트래픽입니다. 출발지는 외부 서버 IP, 목적지는 EC2의 사설 IP로 기록됩니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version account-id interface-id srcaddr dstaddr srcport dstport protocol packets bytes start end action log-status</span>
2 123456789012 eni-natgw 209.10.20.30 10.0.1.10 443 49152 6 8 520 &lt;timestamp&gt; &lt;timestamp&gt; ACCEPT OK
</code></pre></div></div>

<h4 id="-ec2-인스턴스-eni-eni-ec2-로그-1">📜 EC2 인스턴스 ENI (<code class="language-plaintext highlighter-rouge">eni-ec2</code>) 로그</h4>
<p>최종적으로 응답 트래픽이 EC2에 도착합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version account-id interface-id srcaddr dstaddr srcport dstport protocol packets bytes start end action log-status</span>
2 123456789012 eni-ec2 209.10.20.30 10.0.1.10 443 49152 6 8 520 &lt;timestamp&gt; &lt;timestamp&gt; ACCEPT OK
</code></pre></div></div>

<h2 id="심화-pkt-srcaddr와-pkt-dstaddr-필드로-가시성-높이기">심화: <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-dstaddr</code> 필드로 가시성 높이기</h2>

<p>VPC Flow Logs는 <strong>2020년 12월에 발표된 버전 5</strong>부터 <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-dstaddr</code>라는 매우 유용한 필드를 추가로 제공하기 시작했습니다. 이 필드들은 특히 EKS와 같은 컨테이너 환경이나 NAT Gateway, Gateway Load Balancer 등 중간에서 패킷을 처리하는 서비스들의 트래픽을 분석할 때 가시성을 획기적으로 높여줍니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">srcaddr</code> / <code class="language-plaintext highlighter-rouge">dstaddr</code></strong>: 네트워크 인터페이스(ENI) 레벨에서 보이는 출발지/목적지 IP입니다. (겉에 싸여있는 패킷의 IP)</li>
  <li><strong><code class="language-plaintext highlighter-rouge">pkt-srcaddr</code> / <code class="language-plaintext highlighter-rouge">pkt-dstaddr</code></strong>: 캡슐화된 원본 패킷(inner packet)의 실제 출발지/목적지 IP입니다. (패킷 속 진짜 IP)</li>
</ul>

<h3 id="보안-사고-조사-관점에서의-중요성">보안 사고 조사 관점에서의 중요성</h3>

<p>이 필드들은 보안 사고 대응 및 포렌식 조사에서 결정적인 역할을 합니다.</p>

<ul>
  <li>
    <p><strong>정확한 공격 출처 식별</strong>: 과거에는 EKS 환경에서 악성 트래픽이 발생해도 워커 노드의 IP만 기록되어 어떤 Pod이 문제의 원인인지 즉시 파악하기 어려웠습니다. 하지만 <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code>를 통해 <strong>공격을 유발한 특정 Pod을 정확히 식별</strong>할 수 있게 되어, 다른 서비스에 영향을 주지 않고 해당 Pod만 신속하게 격리하고 분석할 수 있습니다.</p>
  </li>
  <li>
    <p><strong>피해 범위 및 내부망 이동(Lateral Movement) 추적</strong>: 공격자가 클러스터 내부의 다른 Pod이나 서비스로 공격을 확산하려 할 때, <code class="language-plaintext highlighter-rouge">pkt-</code> 필드는 Pod 간의 모든 통신을 명확하게 기록합니다. 이를 통해 공격자의 내부 이동 경로를 추적하고 피해 범위를 파악하는 데 필수적인 데이터를 확보할 수 있습니다.</p>
  </li>
  <li>
    <p><strong>신뢰할 수 있는 증거</strong>: 명확한 출발지(Pod)와 목적지가 기록된 로그는 포렌식 분석과 감사 추적을 위한 신뢰도 높은 증거 자료로 활용됩니다.</p>
  </li>
</ul>

<h3 id="1-eks-환경에서-pod-ip-식별">1. EKS 환경에서 Pod IP 식별</h3>

<p>보안 관점에서의 중요성은 EKS 환경에서 가장 잘 드러납니다. EKS 워커 노드(EC2)의 ENI에서 Flow Log를 수집할 때, 버전 4까지는 모든 트래픽의 <code class="language-plaintext highlighter-rouge">srcaddr</code>가 노드의 IP로 기록되었습니다.</p>

<p>하지만 버전 5부터는 <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code>를 통해 실제 통신을 시작한 Pod의 IP를 직접 확인할 수 있습니다.</p>

<p><strong>시나리오:</strong></p>
<ul>
  <li>EKS 워커 노드 IP: <code class="language-plaintext highlighter-rouge">10.0.10.100</code></li>
  <li>노드 위에서 실행 중인 Pod IP: <code class="language-plaintext highlighter-rouge">10.0.10.150</code></li>
  <li>Pod가 외부 IP <code class="language-plaintext highlighter-rouge">209.10.20.30</code>으로 통신</li>
</ul>

<p><strong>📜 EKS 워커 노드 ENI에서 수집된 v5 Flow Log 샘플:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version ... srcaddr dstaddr ... pkt-srcaddr pkt-dstaddr ...</span>
5 ... 10.0.10.100 209.10.20.30 ... 10.0.10.150 209.10.20.30 ... ACCEPT OK
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">srcaddr</code>는 노드의 IP(<code class="language-plaintext highlighter-rouge">10.0.10.100</code>)이지만, <strong><code class="language-plaintext highlighter-rouge">pkt-srcaddr</code></strong>에는 실제 통신을 유발한 <strong>Pod의 IP(<code class="language-plaintext highlighter-rouge">10.0.10.150</code>)</strong>가 정확히 기록됩니다.</li>
</ul>

<h3 id="2-nat-gateway-통신-분석의-명확성-증가">2. NAT Gateway 통신 분석의 명확성 증가</h3>

<p><code class="language-plaintext highlighter-rouge">pkt-</code> 필드는 NAT Gateway를 통과하는 트래픽의 양방향 흐름을 모두 더 명확하게 만들어 줍니다.</p>

<h4 id="아웃바운드-ec2--외부-요청-트래픽">아웃바운드 (EC2 → 외부) 요청 트래픽</h4>

<p>아웃바운드 요청이 NAT Gateway의 ENI에 도달하는 시점을 살펴보겠습니다. 이 트래픽은 아직 다른 무언가에 의해 캡슐화되지 않은 원본 패킷입니다. 따라서 Flow Log를 보면 <strong><code class="language-plaintext highlighter-rouge">srcaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code> 필드의 값이 동일하게</strong> EC2 인스턴스의 사설 IP로 나타납니다.</p>

<p><strong>📜 NAT Gateway ENI의 Outbound v5 Flow Log 샘플:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version ... srcaddr dstaddr ... pkt-srcaddr pkt-dstaddr ...</span>
5 ... 10.0.1.10 209.10.20.30 ... 10.0.1.10 209.10.20.30 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">srcaddr</code> (10.0.1.10)</strong>: NAT GW의 ENI로 패킷을 보낸 리소스의 IP, 즉 EC2의 IP입니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">pkt-srcaddr</code> (10.0.1.10)</strong>: 패킷 헤더의 원본 출발지 IP로, 역시 EC2의 IP입니다.</li>
</ul>

<blockquote>
  <p><strong>중요</strong>: NAT Gateway가 출발지 IP를 자신의 공인 IP로 변환하는 <strong>SNAT 과정 자체</strong>와, 변환된 후 인터넷 게이트웨이로 나가는 트래픽은 VPC Flow Log에 기록되지 않습니다. Flow Log는 ENI를 통과하는 트래픽을 기록하므로, 우리가 볼 수 있는 마지막 아웃바운드 기록은 변환 전의 패킷이 NAT Gateway의 ENI에 도착하는 시점입니다.</p>
</blockquote>

<h4 id="인바운드-외부--ec2-응답-트래픽">인바운드 (외부 → EC2) 응답 트래픽</h4>

<p>이 필드들의 진정한 가치는 외부에서 NAT Gateway를 통해 내부 EC2로 돌아오는 <strong>인바운드 응답 트래픽</strong>에서 나타납니다.</p>

<p><strong>📜 NAT Gateway ENI의 Inbound v5 Flow Log 샘플:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version ... srcaddr dstaddr ... pkt-srcaddr pkt-dstaddr ...</span>
5 ... 209.10.20.30 10.0.0.5 ... 209.10.20.30 10.0.1.10 ... ACCEPT OK
</code></pre></div></div>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">dstaddr</code> (10.0.0.5)</strong>: 패킷이 도착한 ENI의 주소, 즉 NAT Gateway의 사설 IP입니다.</li>
  <li>하지만 <strong><code class="language-plaintext highlighter-rouge">pkt-dstaddr</code> (10.0.1.10)</strong>를 보면, 이 패킷의 <strong>최종 목적지</strong>가 원래 요청을 보냈던 <strong>EC2 인스턴스의 사설 IP</strong>임을 명확하게 알 수 있습니다.</li>
</ul>

<p>결론적으로 <code class="language-plaintext highlighter-rouge">pkt-</code> 필드는 트래픽의 양방향을 모두 분석할 때, 패킷의 겉과 속을 모두 보여줌으로써 복잡한 라우팅 환경에서도 흐름을 혼동 없이 파악할 수 있게 도와줍니다.</p>

<h2 id="결론-그래서-어떻게-ip를-매핑하는가">결론: 그래서 어떻게 IP를 매핑하는가?</h2>

<p>지금까지의 과정을 통해 우리는 VPC Flow Logs와 AWS 리소스 정보를 결합하여 IP 매핑을 추적하는 방법을 알 수 있습니다. 방법은 다음과 같습니다.</p>

<ol>
  <li><strong>NAT Gateway의 ENI ID로 Flow Logs를 필터링</strong>하는 것이 가장 중요합니다.</li>
  <li>필터링된 로그에서 <strong><code class="language-plaintext highlighter-rouge">srcaddr</code>가 VPC 내부 사설 IP</strong>이고 <strong><code class="language-plaintext highlighter-rouge">dstaddr</code>가 외부 공인 IP</strong>인 아웃바운드 로그 항목을 찾습니다.</li>
  <li>위 조건에 맞는 로그 (예: <code class="language-plaintext highlighter-rouge">srcaddr: 10.0.1.10</code>, <code class="language-plaintext highlighter-rouge">dstaddr: 209.10.20.30</code>)를 찾았다면, 이는 “<strong><code class="language-plaintext highlighter-rouge">10.0.1.10</code> 인스턴스가 해당 NAT Gateway를 통해 <code class="language-plaintext highlighter-rouge">209.10.20.30</code> 서버와 통신했다</strong>“는 명백한 증거가 됩니다.</li>
  <li><strong>[핵심]</strong> 이때 EC2의 사설 IP(<code class="language-plaintext highlighter-rouge">10.0.1.10</code>)가 어떤 공인 IP로 변환(SNAT)되었는지는 Flow Log에 직접 기록되지 않습니다. 따라서 로그에서 찾은 <strong>NAT Gateway의 <code class="language-plaintext highlighter-rouge">interface-id</code></strong>를 사용하여 AWS에 직접 그 정보를 조회해야 합니다.</li>
  <li>
    <p>아래와 같이 AWS CLI 명령어를 사용하면 로그에 기록된 ENI ID를 통해 NAT Gateway의 공인 IP를 확인할 수 있습니다.</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 로그에서 찾은 NAT Gateway의 ENI ID로 공인 IP를 확인하는 명령어</span>
aws ec2 describe-nat-gateways <span class="nt">--filter</span> <span class="s2">"Name=network-interface-id,Values=&lt;eni-natgw-from-log&gt;"</span> <span class="nt">--query</span> <span class="s2">"NatGateways[].NatGatewayAddresses[].PublicIp"</span> <span class="nt">--output</span> text
</code></pre></div>    </div>
    <p>위 명령어를 통해 얻은 공인 IP (<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)가 바로 외부 서버와 통신한 실제 IP가 되는 것입니다.</p>
  </li>
</ol>

<p>대규모 환경에서는 수많은 로그가 쌓이기 때문에 수동으로 분석하기는 어렵습니다. <strong>Amazon Athena</strong>나 <strong>CloudWatch Logs Insights</strong>를 사용하여 특정 ENI ID를 필터링하고 <code class="language-plaintext highlighter-rouge">srcaddr</code>와 <code class="language-plaintext highlighter-rouge">dstaddr</code>를 기준으로 쿼리하면 원하는 통신 기록을 훨씬 효율적으로 찾아낼 수 있습니다.</p>

<p>이처럼 VPC Flow Logs의 동작 방식을 이해하면 복잡해 보이는 NAT 환경의 트래픽도 명확하게 추적하고 분석할 수 있습니다.</p>

<hr />

<h2 id="별첨-v5-flow-log-전체-흐름-상세-분석">별첨: v5 Flow Log 전체 흐름 상세 분석</h2>

<p>이 섹션에서는 EC2 인스턴스가 NAT Gateway를 통해 외부와 통신하고 응답을 받는 전체 과정에서, 각 네트워크 인터페이스(ENI)에서 v5 포맷으로 기록되는 모든 VPC Flow Log를 단계별로 상세하게 추적합니다.</p>

<p><strong>시나리오 정보:</strong></p>
<ul>
  <li><strong>EC2 인스턴스</strong>: <code class="language-plaintext highlighter-rouge">eni-ec2</code>, 사설 IP <code class="language-plaintext highlighter-rouge">10.0.1.10</code></li>
  <li><strong>NAT Gateway</strong>: <code class="language-plaintext highlighter-rouge">eni-natgw</code>, 사설 IP <code class="language-plaintext highlighter-rouge">10.0.0.5</code>, 공인 IP <code class="language-plaintext highlighter-rouge">203.0.113.12</code></li>
  <li><strong>외부 서버</strong>: 공인 IP <code class="language-plaintext highlighter-rouge">209.10.20.30</code></li>
  <li><strong>로그 포맷</strong>: v5 (pkt-srcaddr, pkt-dstaddr 필드 포함)</li>
</ul>

<h3 id="1-아웃바운드-ec2--외부-서버">1. 아웃바운드 (EC2 → 외부 서버)</h3>

<h4 id="1-1-ec2-eni-egress-ec2에서-패킷-출발">1-1. EC2 ENI Egress (EC2에서 패킷 출발)</h4>
<p>EC2 인스턴스가 외부 서버로 통신을 시작하며 자신의 ENI(<code class="language-plaintext highlighter-rouge">eni-ec2</code>)를 통해 패킷을 내보냅니다.</p>

<p><strong>📜 <code class="language-plaintext highlighter-rouge">eni-ec2</code> 로그:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version interface-id srcaddr dstaddr pkt-srcaddr pkt-dstaddr ...</span>
5 eni-ec2 10.0.1.10 209.10.20.30 10.0.1.10 209.10.20.30 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong>해석</strong>: <code class="language-plaintext highlighter-rouge">eni-ec2</code>에서 트래픽이 나가는(Egress) 기록입니다. 아직 캡슐화가 없으므로 <code class="language-plaintext highlighter-rouge">srcaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-srcaddr</code>는 모두 EC2의 IP이고, <code class="language-plaintext highlighter-rouge">dstaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-dstaddr</code>는 모두 외부 서버의 IP로 동일합니다.</li>
</ul>

<h4 id="1-2-nat-gateway-eni-ingress-nat-gw에-패킷-도착">1-2. NAT Gateway ENI Ingress (NAT GW에 패킷 도착)</h4>
<p>위 패킷이 라우팅 테이블에 따라 NAT Gateway의 ENI(<code class="language-plaintext highlighter-rouge">eni-natgw</code>)에 도착합니다.</p>

<p><strong>📜 <code class="language-plaintext highlighter-rouge">eni-natgw</code> 로그:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version interface-id srcaddr dstaddr pkt-srcaddr pkt-dstaddr ...</span>
5 eni-natgw 10.0.1.10 209.10.20.30 10.0.1.10 209.10.20.30 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong>해석</strong>: <code class="language-plaintext highlighter-rouge">eni-natgw</code>로 트래픽이 들어오는(Ingress) 기록입니다. 이 시점까지도 패킷의 내용은 변하지 않았습니다. 출발지는 여전히 EC2 IP, 목적지는 외부 서버 IP입니다.</li>
</ul>

<h4 id="1-3-nat-gateway-내부-snat-변환">1-3. NAT Gateway 내부 (SNAT 변환)</h4>
<p>NAT Gateway 서비스 내부에서 패킷의 출발지 IP 주소를 자신의 공인 IP(<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)로 변환(SNAT)합니다.</p>

<ul>
  <li><strong>🚫 VPC Flow Log 없음</strong>: 이 변환 과정은 AWS 관리형 서비스 내부에서 일어나며, 특정 ENI를 통과하는 트래픽이 아니므로 VPC Flow Log에 기록되지 않습니다.</li>
</ul>

<h4 id="1-4-nat-gateway--인터넷-게이트웨이">1-4. NAT Gateway → 인터넷 게이트웨이</h4>
<p>변환된 패킷이 인터넷 게이트웨이를 통해 외부로 나갑니다.</p>

<ul>
  <li><strong>🚫 VPC Flow Log 없음</strong>: 인터넷 게이트웨이는 Flow Log를 설정할 수 있는 ENI를 가지지 않으므로, 이 구간의 트래픽은 VPC Flow Log에 기록되지 않습니다.</li>
</ul>

<h4 id="1-5-internet-gateway--외부-서버">1-5. Internet Gateway → 외부 서버</h4>
<p>Internet Gateway는 NAT Gateway로부터 받은 패킷(이제 출발지 IP는 NAT Gateway의 공인 IP)을 최종 목적지인 외부 서버로 라우팅합니다. 이 단계는 VPC 외부의 통신으로, ENI를 거치지 않기 때문에 VPC Flow Log에 기록되지 않습니다.</p>

<h3 id="2-인바운드-외부-서버--ec2">2. 인바운드 (외부 서버 → EC2)</h3>

<h4 id="2-1-internet-gateway-igw-ingress--destination-nat">2-1. Internet Gateway (IGW) Ingress &amp; Destination NAT</h4>
<p>외부 서버에서 출발한 응답 패킷은 가장 먼저 VPC의 관문인 Internet Gateway(IGW)에 도착합니다. 이때 패킷의 목적지 주소는 NAT Gateway의 공인 IP(<code class="language-plaintext highlighter-rouge">203.0.113.12</code>)입니다.</p>

<p>IGW는 이 공인 IP와 연결된 NAT Gateway의 사설 IP(<code class="language-plaintext highlighter-rouge">10.0.0.5</code>)를 내부 맵핑 정보를 통해 인지하고 있으며, 패킷의 목적지 주소를 사설 IP로 변환(Destination NAT)하여 VPC 내부로 전달합니다.</p>

<ul>
  <li><strong>🚫 VPC Flow Log 없음</strong>: Internet Gateway는 ENI를 가지지 않으므로, IGW에서 일어나는 이 주소 변환 과정은 VPC Flow Log에 기록되지 않습니다.</li>
</ul>

<h4 id="2-2-nat-gateway-eni-ingress-nat-gw에-응답-패킷-도착">2-2. NAT Gateway ENI Ingress (NAT GW에 응답 패킷 도착)</h4>
<p>외부 서버가 보낸 응답 패킷이 NAT Gateway의 공인 IP를 목적지로 하여 <code class="language-plaintext highlighter-rouge">eni-natgw</code>에 도착합니다. <strong>이 로그가 v5의 핵심입니다.</strong></p>

<p><strong>📜 <code class="language-plaintext highlighter-rouge">eni-natgw</code> 로그:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version interface-id srcaddr dstaddr pkt-srcaddr pkt-dstaddr ...</span>
5 eni-natgw 209.10.20.30 10.0.0.5 209.10.20.30 10.0.1.10 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong>해석</strong>: <code class="language-plaintext highlighter-rouge">eni-natgw</code>로 트래픽이 들어오는(Ingress) 기록입니다.
    <ul>
      <li><code class="language-plaintext highlighter-rouge">dstaddr: 10.0.0.5</code>: 패킷의 겉 목적지는 <strong>NAT Gateway의 사설 IP</strong>입니다.</li>
      <li><code class="language-plaintext highlighter-rouge">pkt-dstaddr: 10.0.1.10</code>: 하지만 패킷의 진짜 최종 목적지는 <strong>EC2의 사설 IP</strong>임을 명확히 보여줍니다.</li>
    </ul>
  </li>
</ul>

<h4 id="2-3-nat-gateway-내부-dnat-변환">2-3. NAT Gateway 내부 (DNAT 변환)</h4>
<p>NAT Gateway가 상태 테이블을 참조하여 패킷의 목적지 IP를 원래 요청을 보냈던 EC2의 사설 IP(<code class="language-plaintext highlighter-rouge">10.0.1.10</code>)로 변환(DNAT)합니다.</p>

<ul>
  <li><strong>🚫 VPC Flow Log 없음</strong>: SNAT와 마찬가지로 서비스 내부 동작이므로 로그가 기록되지 않습니다.</li>
</ul>

<h4 id="2-4-nat-gateway-eni-egress-nat-gw에서-패킷-출발">2-4. NAT Gateway ENI Egress (NAT GW에서 패킷 출발)</h4>
<p>DNAT 변환이 완료된 패킷이 <code class="language-plaintext highlighter-rouge">eni-natgw</code>를 떠나 VPC 내부망을 통해 EC2로 향합니다.</p>

<p><strong>📜 <code class="language-plaintext highlighter-rouge">eni-natgw</code> 로그:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version interface-id srcaddr dstaddr pkt-srcaddr pkt-dstaddr ...</span>
5 eni-natgw 209.10.20.30 10.0.1.10 209.10.20.30 10.0.1.10 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong>해석</strong>: <code class="language-plaintext highlighter-rouge">eni-natgw</code>에서 트래픽이 나가는(Egress) 기록입니다. DNAT이 이미 끝났으므로, 이제 <code class="language-plaintext highlighter-rouge">dstaddr</code>와 <code class="language-plaintext highlighter-rouge">pkt-dstaddr</code>는 모두 최종 목적지인 EC2의 IP로 동일합니다.</li>
</ul>

<h4 id="2-5-ec2-eni-ingress-ec2에-패킷-도착">2-5. EC2 ENI Ingress (EC2에 패킷 도착)</h4>
<p>최종적으로 응답 패킷이 EC2 인스턴스의 ENI(<code class="language-plaintext highlighter-rouge">eni-ec2</code>)에 도착합니다.</p>

<p><strong>📜 <code class="language-plaintext highlighter-rouge">eni-ec2</code> 로그:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># version interface-id srcaddr dstaddr pkt-srcaddr pkt-dstaddr ...</span>
5 eni-ec2 209.10.20.30 10.0.1.10 209.10.20.30 10.0.1.10 ... ACCEPT OK
</code></pre></div></div>
<ul>
  <li><strong>해석</strong>: <code class="language-plaintext highlighter-rouge">eni-ec2</code>로 트래픽이 들어오는(Ingress) 기록입니다. EC2가 받은 패킷의 출발지는 외부 서버, 목적지는 자기 자신입니다.</li>
</ul>]]></content><author><name>KK0M4k - Francesco</name></author><category term="AWS" /><category term="VPCFlow" /><summary type="html"><![CDATA[AWS 환경에서 보안을 강화하기 위해 데이터베이스나 백엔드 애플리케이션 서버 등 외부에서 직접 접근할 필요가 없는 리소스들은 보통 Private Subnet에 배치합니다. 하지만 이 서버들도 소프트웨어 패치, 외부 API 호출 등 인터넷으로 아웃바운드 통신이 필요한 경우가 발생합니다.]]></summary></entry><entry><title type="html">AWS NFW을 활용한 네트워크 트래픽 통제</title><link href="http://localhost:4000/aws/aws-network-firewall-guide/" rel="alternate" type="text/html" title="AWS NFW을 활용한 네트워크 트래픽 통제" /><published>2025-08-25T00:00:00+09:00</published><updated>2025-08-25T00:00:00+09:00</updated><id>http://localhost:4000/aws/aws-network-firewall-guide</id><content type="html" xml:base="http://localhost:4000/aws/aws-network-firewall-guide/"><![CDATA[<p>현대 기업의 클라우드 여정에서 가장 큰 도전 과제 중 하나는 바로 ‘보안’입니다. 특히 AWS 환경에서 수십, 수백 개의 워크로드를 운영하다 보면, 각각의 VPC와 서브넷에서 발생하는 트래픽을 일일이 추적하고 통제하는 것이 얼마나 복잡한 일인지 실감하게 됩니다. 예로, 새벽에 갑작스러운 알람이 울리고, 확인해보니 특정 EC2 인스턴스에서 의심스러운 대량의 아웃바운드 트래픽이 발생했다면, 이미 때는 늦었고, 데이터는 외부로 유출되었으며, 막대한 AWS 비용 청구서와 정보유출사고에 직면하게 됩니다. 
이러한 악몽 같은 시나리오를 방지하기 위해서는 사전에 공격 표면을 제거하고 모니터링을 강화하고 아웃바운드 통신도 통제해야 합니다. AWS는 <strong>AWS Network Firewall(NFW)</strong>이라는 관리형 서비스 제공합니다.이를 통해서 공격 탐지에 대한 가시성과 네트워크 트래픽 제어를 할수 있습니다.</p>

<h2 id="왜-aws-network-firewall을-사용해야-하는가">왜 AWS Network Firewall을 사용해야 하는가?</h2>

<p>“우리는 이미 보안 그룹과 NACL을 잘 활용하고 있는데, 굳이 NFW가 필요할까요?” 보안 그룹과 NACL은 AWS의 기본적인 보안 도구로서 중요한 역할을 합니다. 하지만 현실은 그리 단순하지 않습니다. 최근 발생한 한 사례를 들어보겠습니다. 실수로 S3 버킷의 권한을 잘못 설정했고, 악의적인 공격자가 이를 악용해 EC2 인스턴스에 악성코드를 심었습니다. 이 악성코드는 정상적인 HTTPS 포트(443)를 통해 C&amp;C 서버와 통신하며 데이터를 유출했습니다. 보안 그룹은 443 포트를 허용하고 있었기 때문에 이를 막을 수 없었고, NACL은 stateless 특성상 이러한 정교한 공격을 탐지할 수 없었습니다.</p>

<h3 id="1-중앙-집중형-보안-관리">1. 중앙 집중형 보안 관리</h3>

<p>AWS 환경을 운영해보신 분이라면 공감하실 겁니다. 프로젝트가 성장하면서 VPC가 하나둘 늘어나고, 각 VPC마다 서로 다른 보안 규칙이 적용되며, 어느 순간 전체 보안 정책을 파악하는 것조차 어려워집니다. NFW는 전통적인 방화벽의 단순한 IP/포트 기반 필터링을 훨씬 뛰어넘는 정교한 트래픽 검사 기능을 제공합니다.</p>

<p>가장 핵심적인 기능인 <strong>Stateful Inspection</strong>은 단순히 개별 패킷을 검사하는 것이 아니라, 전체 연결의 상태를 지속적으로 추적하고 모니터링합니다. 이를 통해 정상적인 통신 패턴을 벗어나는 이상 행위를 즉시 감지하고, 세션 하이재킹이나 중간자 공격과 같은 정교한 위협을 효과적으로 차단할 수 있습니다. <strong>IPS/IDS 기능</strong>은 실시간으로 네트워크 트래픽을 분석하여 알려진 악성 행위나 취약점 공격 패턴을 탐지합니다. 지속적으로 업데이트되는 위협 인텔리전스를 기반으로, 제로데이 공격이나 최신 악성코드의 활동도 신속하게 식별하고 차단할 수 있습니다. <strong>웹 필터링</strong> 기능은 도메인 이름과 URL을 기반으로 아웃바운드 트래픽을 세밀하게 제어합니다. 이는 특히 악성 C&amp;C(Command and Control) 서버와의 통신을 차단하거나, 조직의 정책에 어긋나는 부적절한 사이트로의 접근을 원천적으로 막는 데 매우 효과적입니다. 또한 암호화된 HTTPS 트래픽에 대해서도 SNI(Server Name Indication) 정보를 활용하여 도메인 기반 필터링이 가능합니다.</p>

<h3 id="2-규정-준수">2. 규정 준수</h3>

<p>“3개월 전 특정 IP에서 우리 시스템에 접근한 모든 기록을 제출해 주세요.” 금융감독원의 이런 요청을 받았을 때, 여러분의 조직은 얼마나 빠르게 대응할 수 있을까요? 많은 기업들이 이런 순간 당황하게 됩니다. 로그는 있지만 어디에 어떻게 저장되어 있는지, 어떻게 검색해야 하는지 막막하기 때문입니다. NFW는 이러한 컴플라이언스 요구사항을 염두에 두고 설계되었습니다. 모든 트래픽에 대한 상세한 Flow 로그와 보안 이벤트에 대한 Alert 로그를 자동으로 생성하며, 이를 S3에 체계적으로 저장할 수 있습니다.</p>

<p><strong>트래픽 흐름 범례:</strong></p>

<p>• 실선 화살표 (→): 인바운드 트래픽 및 그에 대한 응답 패킷의 흐름을 나타냅니다
• 점선 화살표 (–&gt;): 내부에서 시작된 아웃바운드 트래픽의 경로를 표시합니다
• 이중선 화살표 (⇒): NFW에서 생성된 로그가 저장소로 전송되는 흐름을 보여줍니다</p>

<pre><code class="language-mermaid">flowchart TB
    %% 1. Node Definitions (Simplified Text)
    Web((Internet))
    IGW[Internet Gateway]
    NAT[NAT Gateway]
    NFW_ENI[Firewall Endpoint ENI]
    NFW[AWS Network Firewall]
    TGW[Transit Gateway Router]
    TGW_ENI[TGW Attachment ENI]
    EC2[EC2 Instance]
    Firehose[Kinesis Data Firehose]
    S3[S3 Bucket]

    %% 2. Subgraph Grouping
    subgraph INTERNET[인터넷]
        Web
    end
    
    subgraph INSPECTION[Inspection VPC]
        direction TB
        subgraph Public Subnet
            IGW
            NAT
        end
        subgraph Firewall Subnet
            NFW_ENI
            NFW
        end
    end
    
    subgraph TRANSIT[Transit Gateway]
        TGW
    end
    
    subgraph WORKLOAD[Workload VPC]
        TGW_ENI
        EC2
    end
    
    subgraph LOGGING[Log Storage]
        direction LR
        Firehose
        S3
    end

    %% 3. Connections
    Web -- "A" --&gt; IGW
    IGW -- "B" --&gt; NFW_ENI
    NFW_ENI -- "C Inspect" --&gt; NFW
    NFW -- "D" --&gt; TGW
    TGW -- "E" --&gt; TGW_ENI
    TGW_ENI -- "F Forward" --&gt; EC2

    EC2 -.-&gt;|1| TGW_ENI
    TGW_ENI -.-&gt;|2| TGW
    TGW -.-&gt;|3| NFW_ENI
    NFW_ENI -.-&gt;|4 Inspect| NFW
    NFW -.-&gt;|5| NAT
    NAT -.-&gt;|6 SNAT| IGW
    IGW -.-&gt;|7 Egress| Web
    
    NFW ==&gt;|Logs| Firehose
    Firehose --&gt; S3

    %% 4. Styling
    style EC2 fill:#f9f,stroke:#333,stroke-width:2px
    style Web fill:#cde,stroke:#333,stroke-width:2px
    style NFW fill:#ff9,stroke:#f63,stroke-width:2px
    style NAT fill:#ff9,stroke:#f63,stroke-width:2px
    style IGW fill:#ff9,stroke:#f63,stroke-width:2px
    style S3 fill:#dff,stroke:#369,stroke-width:2px
    style Firehose fill:#dff,stroke:#369,stroke-width:2px
    style TGW_ENI fill:#e6f3ff,stroke:#007bff,stroke-width:2px,stroke-dasharray: 5 5
    style NFW_ENI fill:#fff2e6,stroke:#ff8c1a,stroke-width:2px,stroke-dasharray: 5 5
    style WORKLOAD fill:#f0f9ff,stroke:#333,stroke-width:1px
    style INSPECTION fill:#fffbf0,stroke:#333,stroke-width:1px
    style LOGGING fill:#f0f0ff,stroke:#333,stroke-width:1px
    style INTERNET fill:#f0f0f0,stroke:#333,stroke-width:1px
</code></pre>

<h2 id="상세-트래픽-흐름-분석">상세 트래픽 흐름 분석</h2>

<h3 id="1-아웃바운드egress-트래픽-흐름">1. 아웃바운드(Egress) 트래픽 흐름</h3>

<h4 id="시나리오-ec2-인스턴스의-소프트웨어-업데이트">시나리오: EC2 인스턴스의 소프트웨어 업데이트</h4>

<p>프로덕션 EC2 인스턴스에서 보안 패치를 적용하기 위해 <code class="language-plaintext highlighter-rouge">yum update</code> 명령을 실행합니다. 이 순간부터 흥미로운 네트워크 여정이 시작됩니다.</p>

<p><strong>Step 1 - 출발점 [EC2 → TGW]</strong>
Private Subnet (10.0.1.0/24)에 위치한 EC2 인스턴스가 패키지 저장소(예: amazonlinux.us-east-1.amazonaws.com)에 접근을 시도합니다. 인스턴스의 라우팅 테이블을 확인하면, 모든 외부 트래픽(0.0.0.0/0)은 Transit Gateway ENI로 향하도록 설정되어 있습니다. 패킷은 자신의 사설 IP(10.0.1.100)를 소스로 하여 TGW로 전송됩니다.</p>

<p><strong>Step 2 - 중앙 라우터 통과 [TGW → NFW]</strong>
Transit Gateway는 패킷을 받아 자신의 라우트 테이블을 확인합니다. 인터넷 행(0.0.0.0/0) 트래픽은 Inspection VPC의 NFW Endpoint로 라우팅하도록 설정되어 있습니다. 여기서 중요한 점은, TGW가 VPC 간 라우팅 시 원본 패킷의 소스/목적지 정보를 그대로 유지한다는 것입니다.</p>

<p><strong>Step 3 - 보안 검사 [NFW 검사 및 결정]</strong>
이제 가장 중요한 단계입니다. NFW는 다층적 검사를 수행합니다:</p>
<ul>
  <li><strong>도메인 검사</strong>: amazonlinux.us-east-1.amazonaws.com이 허용된 도메인 리스트에 있는지 확인</li>
  <li><strong>포트/프로토콜 검사</strong>: HTTPS(443) 트래픽이 허용되는지 확인</li>
  <li><strong>IPS 검사</strong>: 패킷 페이로드에 악성 패턴이 없는지 심층 분석</li>
  <li><strong>상태 추적</strong>: 이 연결을 상태 테이블에 기록하여 응답 패킷을 자동 허용</li>
</ul>

<p>모든 검사를 통과한 패킷은 NAT Gateway로 전달됩니다. 만약 차단되었다면, Drop 로그가 생성되고 연결은 즉시 종료됩니다.</p>

<p><strong>Step 4 - 주소 변환 [NAT → IGW]</strong>
NAT Gateway는 패킷의 소스 IP를 자신의 Elastic IP(예: 52.1.2.3)로 변환합니다. 이는 외부 서버가 응답을 보낼 수 있는 공인 주소를 제공합니다. 동시에 이 변환 정보를 내부 테이블에 저장하여, 응답이 돌아왔을 때 원래의 EC2 인스턴스로 전달할 수 있도록 준비합니다.</p>

<p><strong>Step 5 - 인터넷으로 [IGW → Internet]</strong>
마침내 Internet Gateway를 통해 패킷이 AWS 네트워크를 벗어나 인터넷으로 나갑니다. IGW는 VPC와 인터넷 간의 경계 역할을 하며, 공인 IP 주소를 가진 트래픽만 통과시킵니다.</p>

<h3 id="2-인바운드ingress-트래픽-흐름">2. 인바운드(Ingress) 트래픽 흐름</h3>

<h4 id="시나리오-고객의-웹-애플리케이션-접근">시나리오: 고객의 웹 애플리케이션 접근</h4>

<p><strong>Step A - 진입점 [Internet → IGW]</strong>
사용자 브라우저에서 시작된 HTTPS 요청이 여러분의 도메인에 할당된 Elastic IP로 도착합니다. Internet Gateway는 이 트래픽을 받아 VPC 내부로 전달할 준비를 합니다.</p>

<p><strong>Step B - 첫 번째 관문 [IGW → NFW]</strong>
여기서 핵심은 IGW의 라우트 테이블 설정입니다. 모든 인바운드 트래픽이 NFW를 거치도록 강제하기 위해, IGW 라우트 테이블(Edge Association)에 대상 서브넷으로의 트래픽을 NFW ENI로 향하도록 설정합니다. 이는 공격자가 NFW를 우회할 수 없도록 하는 중요한 보안 조치입니다.</p>

<p><strong>Step C - 심층 검사 [NFW 분석 및 결정]</strong>
NFW는 인바운드 트래픽에 대해서도 철저한 검사를 수행합니다:</p>
<ul>
  <li><strong>GeoIP 검사</strong>: 요청이 허용된 국가에서 왔는지 확인</li>
  <li><strong>Rate Limiting</strong>: 특정 IP에서 과도한 요청이 오고 있지 않은지 확인</li>
  <li><strong>WAF 규칙</strong>: SQL Injection, XSS 등의 웹 공격 패턴 검사</li>
  <li><strong>DDoS 방어</strong>: 비정상적인 트래픽 패턴 탐지</li>
</ul>

<p>정상적인 트래픽으로 판단되면 Transit Gateway로 전달됩니다.</p>

<p><strong>Step D - 최종 목적지 [TGW → EC2/ALB]</strong>
Transit Gateway는 패킷의 목적지 IP를 확인하고, 해당하는 Workload VPC로 라우팅합니다. 최종적으로 Application Load Balancer나 웹 서버 인스턴스가 요청을 받아 처리합니다.</p>

<h3 id="3-응답-트래픽">3. 응답 트래픽</h3>

<p>여기서 많은 분들이 놓치는 중요한 포인트가 있습니다. 인바운드 요청에 대한 응답도 동일한 보안 검사를 거쳐야 한다는 것입니다. 웹 서버가 생성한 응답 패킷(예: HTML 페이지)은 다시 TGW → NFW → NAT → IGW를 거쳐 고객에게 전달됩니다. NFW는 Stateful 특성 덕분에 이미 허용된 연결의 응답 트래픽은 자동으로 허용하지만, 여전히 IPS 검사는 수행하여 데이터 유출이나 악성코드 전파를 방지합니다.</p>

<h2 id="parquet-형식의-로그-저장-및-활용-보안-인텔리전스의-기반-구축">Parquet 형식의 로그 저장 및 활용: 보안 인텔리전스의 기반 구축</h2>

<p>보안은 단순히 위협을 차단하는 것에서 끝나지 않습니다. 진정한 보안은 모든 활동을 기록하고, 분석하며, 그로부터 인사이트를 도출하는 것입니다. NFW의 로깅 기능과 Parquet 형식의 조합은 이러한 ‘보안 인텔리전스’를 구현하는 강력한 도구입니다.</p>

<h3 id="nfw가-생성하는-두-가지-핵심-로그">NFW가 생성하는 두 가지 핵심 로그</h3>

<p>NFW는 네트워크 활동에 대한 두 가지 유형의 로그를 생성합니다:</p>

<p><strong>Flow 로그</strong>: 모든 네트워크 연결에 대한 메타데이터를 기록합니다. 소스/목적지 IP, 포트, 프로토콜, 바이트 수, 패킷 수, 연결 시작/종료 시간 등이 포함됩니다. 이는 마치 전화 통화 기록처럼, 누가 누구와 언제 얼마나 통신했는지를 보여줍니다.</p>

<p><strong>Alert 로그</strong>: 보안 규칙에 의해 탐지되거나 차단된 이벤트를 상세히 기록합니다. 어떤 규칙이 트리거되었는지, 어떤 위협이 탐지되었는지, 그리고 어떤 조치가 취해졌는지를 포함합니다. 이는 보안 사고의 직접적인 증거가 됩니다.</p>

<h3 id="실전-설정-가이드-step-by-step">실전 설정 가이드: Step by Step</h3>

<p>이제 실제로 Parquet 변환을 설정하는 방법을 상세히 알아보겠습니다.</p>

<p><strong>Step 1: NFW 로깅 활성화</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AWS Console → VPC → Network Firewall → 해당 방화벽 선택
→ Logging configuration → Edit
→ Log type: Flow logs, Alert logs 모두 선택
→ Log destination: Kinesis Data Firehose 선택
</code></pre></div></div>

<p><strong>Step 2: Kinesis Data Firehose 스트림 생성</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AWS Console → Kinesis → Data Firehose → Create delivery stream
→ Source: Direct PUT
→ Destination: Amazon S3
</code></pre></div></div>

<p><strong>Step 3: Parquet 변환 설정</strong>
Transform 섹션에서:</p>
<ul>
  <li>Record transformation: Enabled</li>
  <li>Transform source records with AWS Lambda:
    <ul>
      <li>새 Lambda 함수 생성 또는 AWS 제공 블루프린트 사용</li>
      <li>“Kinesis Data Firehose Process Record - JSON to Parquet” 선택</li>
    </ul>
  </li>
</ul>

<p><strong>Step 4: S3 버킷 구성</strong></p>
<ul>
  <li>Bucket: 전용 로그 버킷 생성 (예: company-nfw-logs-parquet)</li>
  <li>Prefix: year=!{timestamp:yyyy}/month=!{timestamp:MM}/day=!{timestamp:dd}/</li>
  <li>Error prefix: error/</li>
  <li>Compression: GZIP (Parquet 자체 압축과 함께 추가 압축)</li>
</ul>

<p><strong>Step 5: 버퍼 설정 최적화</strong></p>
<ul>
  <li>Buffer size: 128 MB (대용량 환경) 또는 64 MB (일반 환경)</li>
  <li>Buffer interval: 60 seconds</li>
  <li>이 설정은 비용과 실시간성 간의 균형을 맞춥니다</li>
</ul>

<h3 id="고급-활용-시나리오">고급 활용 시나리오</h3>

<p><strong>시나리오 1: 데이터 유출 탐지</strong>
Athena 쿼리입니다:</p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> 
  <span class="n">sourceAddress</span><span class="p">,</span>
  <span class="n">destinationAddress</span><span class="p">,</span>
  <span class="k">SUM</span><span class="p">(</span><span class="n">bytes</span><span class="p">)</span> <span class="k">as</span> <span class="n">total_bytes</span><span class="p">,</span>
  <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">as</span> <span class="n">connection_count</span>
<span class="k">FROM</span> <span class="n">nfw_flow_logs</span>
<span class="k">WHERE</span> 
  <span class="nb">date</span> <span class="k">BETWEEN</span> <span class="k">current_date</span> <span class="o">-</span> <span class="n">interval</span> <span class="s1">'7'</span> <span class="k">day</span> <span class="k">AND</span> <span class="k">current_date</span>
  <span class="k">AND</span> <span class="n">destinationPort</span> <span class="k">NOT</span> <span class="k">IN</span> <span class="p">(</span><span class="mi">80</span><span class="p">,</span> <span class="mi">443</span><span class="p">)</span>
  <span class="k">AND</span> <span class="n">bytes</span> <span class="o">&gt;</span> <span class="mi">10000000</span>  <span class="c1">-- 10MB 이상</span>
<span class="k">GROUP</span> <span class="k">BY</span> <span class="n">sourceAddress</span><span class="p">,</span> <span class="n">destinationAddress</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">total_bytes</span> <span class="k">DESC</span>
<span class="k">LIMIT</span> <span class="mi">20</span><span class="p">;</span>
</code></pre></div></div>

<p>이 쿼리는 일주일간 비표준 포트로 대량 데이터를 전송한 상위 20개 연결을 찾아냅니다.</p>

<p><strong>시나리오 2: 자동화된 위협 대응</strong>
Lambda와 EventBridge를 활용한 자동 대응 시스템:</p>
<ol>
  <li>Athena가 5분마다 의심스러운 패턴을 검색</li>
  <li>임계값 초과 시 SNS 알림 발송</li>
  <li>심각한 경우 자동으로 NFW 규칙 업데이트하여 차단</li>
</ol>

<p><strong>시나리오 3: 컴플라이언스 리포팅</strong>
월간 보안 리포트 자동 생성:</p>
<ul>
  <li>QuickSight를 S3의 Parquet 데이터에 직접 연결</li>
  <li>트래픽 트렌드, 위협 통계, 상위 통신 패턴 등 시각화</li>
  <li>규제 기관 제출용 PDF 리포트 자동 생성</li>
</ul>]]></content><author><name>KK0M4k - Francesco</name></author><category term="AWS" /><category term="AWS Network Firewall" /><category term="Security Architecture" /><summary type="html"><![CDATA[현대 기업의 클라우드 여정에서 가장 큰 도전 과제 중 하나는 바로 ‘보안’입니다. 특히 AWS 환경에서 수십, 수백 개의 워크로드를 운영하다 보면, 각각의 VPC와 서브넷에서 발생하는 트래픽을 일일이 추적하고 통제하는 것이 얼마나 복잡한 일인지 실감하게 됩니다. 예로, 새벽에 갑작스러운 알람이 울리고, 확인해보니 특정 EC2 인스턴스에서 의심스러운 대량의 아웃바운드 트래픽이 발생했다면, 이미 때는 늦었고, 데이터는 외부로 유출되었으며, 막대한 AWS 비용 청구서와 정보유출사고에 직면하게 됩니다. 이러한 악몽 같은 시나리오를 방지하기 위해서는 사전에 공격 표면을 제거하고 모니터링을 강화하고 아웃바운드 통신도 통제해야 합니다. AWS는 AWS Network Firewall(NFW)이라는 관리형 서비스 제공합니다.이를 통해서 공격 탐지에 대한 가시성과 네트워크 트래픽 제어를 할수 있습니다.]]></summary></entry><entry><title type="html">AWS Transit Gateway를 활용한 중앙 집중식 Egress 아키텍처 구축 가이드</title><link href="http://localhost:4000/aws/centralized-egress-vpc-guide/" rel="alternate" type="text/html" title="AWS Transit Gateway를 활용한 중앙 집중식 Egress 아키텍처 구축 가이드" /><published>2025-08-23T09:00:00+09:00</published><updated>2025-08-23T09:00:00+09:00</updated><id>http://localhost:4000/aws/centralized-egress-vpc-guide</id><content type="html" xml:base="http://localhost:4000/aws/centralized-egress-vpc-guide/"><![CDATA[<p>클라우드 환경에서 여러 개의 VPC(Virtual Private Cloud)를 운영할 때, 각 VPC가 개별적으로 인터넷 게이트웨이나 NAT 게이트웨이를 보유하는 구성은 관리의 복잡성과 보안 비용을 증가시키는 원인이 되곤 합니다. 이러한 문제를 효과적으로 해결할 수 있는 방안이 바로 <strong>중앙 집중식 Egress 아키텍처</strong>입니다. 이 모델은 AWS Transit Gateway(TGW)를 중심으로, 여러 VPC의 인터넷 바운드 트래픽을 단 하나의 ‘Egress VPC’에서 통합 관리하는 방식입니다. 이 포스트에서는 <code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code>(Egress VPC), <code class="language-plaintext highlighter-rouge">kkom4k-vpc-02</code>(Spoke VPC), <code class="language-plaintext highlighter-rouge">kkom4k-vpc-03</code>(Spoke VPC)의 세 가지 VPC를 사용하는 시나리오를 통해, 중앙 집중식 Egress 아키텍처를 구축하는 과정을 단계별로 살펴보겠습니다.</p>

<hr />

<h2 id="아키텍처-개요">아키텍처 개요</h2>

<p>우리가 구축할 아키텍처의 구성 요소와 역할은 다음과 같습니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code> (Egress VPC):</strong> 인터넷과 통신하는 유일한 지점(Single Point of Contact)입니다. 이곳에 NAT 게이트웨이와 인터넷 게이트웨이를 배치하여 모든 VPC의 인터넷 바운드 트래픽을 처리하는 관문 역할을 수행합니다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-vpc-02</code>, <code class="language-plaintext highlighter-rouge">kkom4k-vpc-03</code> (Spoke VPCs):</strong> 애플리케이션 서버와 같이 내부 워크로드가 위치하는 VPC입니다. 이 VPC들은 인터넷과 직접 연결되지 않는 프라이빗 서브넷으로만 구성되며, 인터넷 통신이 필요할 경우 모든 트래픽을 Transit Gateway를 통해 Egress VPC로 전달합니다.</li>
  <li><strong>Transit Gateway (TGW):</strong> 모든 VPC를 연결하는 중앙 허브로서, VPC 간 및 인터넷 방향 트래픽을 올바른 경로로 라우팅하는 핵심 요소입니다.</li>
</ul>

<h3 id="데이터-통신-흐름-mermaid-diagram">데이터 통신 흐름 (Mermaid Diagram)</h3>

<pre><code class="language-mermaid">graph TD
    %% VPCs and Instances
    subgraph kkom4k-vpc-02 [VPC-02: 10.2.0.0/16]
        EC2_VPC2(EC2 Instance)
    end

    subgraph kkom4k-vpc-03 [VPC-03: 10.3.0.0/16]
        EC2_VPC3(EC2 Instance)
    end

    subgraph kkom4k-vpc-01 [Egress VPC-01: 10.1.0.0/16]
        subgraph Public Subnet
            NAT_GW(NAT Gateway)
        end
        subgraph Private Subnet
            TGW_ENI_VPC1(TGW ENI)
        end
        IGW(Internet Gateway)
    end

    %% TGW
    subgraph TGW [Transit Gateway]
        TGW_RT(TGW Route Table)
    end

    %% External
    Internet([Internet])

    %% Egress Flow (Outbound to Internet)
    EC2_VPC2 -- "(1) Default Route (0.0.0.0/0)" --&gt; TGW_RT
    EC2_VPC3 -- "(1) Default Route (0.0.0.0/0)" --&gt; TGW_RT
    TGW_RT -- "(2) Egress Route (0.0.0.0/0)" --&gt; TGW_ENI_VPC1
    TGW_ENI_VPC1 -- "(3) Route to NAT GW" --&gt; NAT_GW
    NAT_GW -- "(4) Outbound" --&gt; IGW
    IGW -- "(5) To Internet" --&gt; Internet

    %% Ingress Flow (Return from Internet)
    Internet -- "(6) Return Traffic" --&gt; IGW
    IGW -- "(7) Forward to NAT GW" --&gt; NAT_GW
    NAT_GW -- "(8) Route to Spoke VPCs" --&gt; TGW_RT
    TGW_RT -- "(9) Forward to VPC-02" --&gt; EC2_VPC2
    TGW_RT -- "(9) Forward to VPC-03" --&gt; EC2_VPC3

    %% Inter-VPC Communication
    EC2_VPC2 -- "(10) To 10.3.0.0/16" --&gt; TGW_RT
    TGW_RT -- "(11) Forward to VPC-03" --&gt; EC2_VPC3
</code></pre>

<hr />

<h2 id="보안-관점에서의-이점">보안 관점에서의 이점</h2>

<p>이 아키텍처는 분산된 관리 포인트를 제거하고 트래픽 제어 및 모니터링을 중앙화함으로써 보안을 크게 강화합니다.</p>

<ol>
  <li>
    <p><strong>트래픽 검사 및 필터링의 중앙화 (Centralized Traffic Inspection)</strong>
모든 아웃바운드 트래픽이 Egress VPC라는 단일 지점을 통과하므로, 이곳에 AWS Network Firewall이나 차세대 방화벽(NGFW) 같은 보안 솔루션을 통합하여 모든 VPC에 대한 침입 방지(IPS), 웹 필터링, 악성 트래픽 차단 정책을 일괄적으로 적용할 수 있습니다.</p>
  </li>
  <li>
    <p><strong>일관된 보안 정책 적용 (Consistent Security Policy)</strong>
보안 정책을 Egress VPC 한 곳에서만 관리하므로, 설정 오류나 누락의 위험이 크게 줄어듭니다. 새로운 VPC가 추가되더라도 TGW에 연결하기만 하면 즉시 동일한 보안 정책의 보호를 받게 되어 정책의 일관성을 유지할 수 있습니다.</p>
  </li>
  <li>
    <p><strong>감사(Audit) 및 로깅의 간소화 (Simplified Auditing &amp; Logging)</strong>
인터넷 통신과 관련된 모든 로그(VPC Flow Logs, Firewall Logs 등)가 Egress VPC 한 곳에서 생성됩니다. 이는 보안 감사나 침해 사고 분석 시 로그 수집 및 분석 작업을 매우 용이하게 만듭니다.</p>
  </li>
  <li>
    <p><strong>공격 표면(Attack Surface) 감소</strong>
실제 애플리케이션이 동작하는 Spoke VPC들은 인터넷에 직접 노출되지 않습니다. 외부 공격자는 내부 서버에 직접 접근할 수 없으며, 여러 겹으로 보호되는 Egress VPC를 먼저 통과해야 하므로 전체적인 보안 리스크가 크게 감소합니다.</p>
  </li>
</ol>

<hr />

<h2 id="사전-준비-사항">사전 준비 사항</h2>

<p>본격적인 구축에 앞서, 다음 리소스들이 준비되어 있어야 합니다.</p>

<ul>
  <li><strong>VPCs &amp; Subnets:</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code>: <code class="language-plaintext highlighter-rouge">10.1.0.0/16</code>
        <ul>
          <li><code class="language-plaintext highlighter-rouge">public-subnet-01</code>: <code class="language-plaintext highlighter-rouge">10.1.1.0/24</code></li>
          <li><code class="language-plaintext highlighter-rouge">private-subnet-01</code> (TGW용): <code class="language-plaintext highlighter-rouge">10.1.2.0/24</code></li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">kkom4k-vpc-02</code>: <code class="language-plaintext highlighter-rouge">10.2.0.0/16</code>
        <ul>
          <li><code class="language-plaintext highlighter-rouge">private-subnet-02</code>: <code class="language-plaintext highlighter-rouge">10.2.1.0/24</code></li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">kkom4k-vpc-03</code>: <code class="language-plaintext highlighter-rouge">10.3.0.0/16</code>
        <ul>
          <li><code class="language-plaintext highlighter-rouge">private-subnet-03</code>: <code class="language-plaintext highlighter-rouge">10.3.1.0/24</code></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>Internet Gateway (IGW):</strong> <code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code>에 연결되어 있어야 합니다.</li>
  <li><strong>NAT Gateway:</strong> <code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code>의 <code class="language-plaintext highlighter-rouge">public-subnet-01</code>에 생성되어 있어야 합니다.</li>
  <li><strong>Transit Gateway (TGW):</strong> 리전 내에 생성되어 있어야 합니다.</li>
</ul>

<hr />

<h2 id="구축-단계">구축 단계</h2>

<h3 id="1단계-transit-gateway-어태치먼트-생성">1단계: Transit Gateway 어태치먼트 생성</h3>

<p>각 VPC를 TGW에 연결하기 위한 어태치먼트를 생성합니다.</p>

<ol>
  <li><strong>VPC-01 어태치먼트:</strong>
    <ul>
      <li>이름: <code class="language-plaintext highlighter-rouge">tgw-attach-vpc01</code></li>
      <li>TGW에 <code class="language-plaintext highlighter-rouge">kkom4k-vpc-01</code>을 연결하고, 서브넷은 <code class="language-plaintext highlighter-rouge">private-subnet-01</code>을 선택합니다.</li>
    </ul>
  </li>
  <li><strong>VPC-02 어태치먼트:</strong>
    <ul>
      <li>이름: <code class="language-plaintext highlighter-rouge">tgw-attach-vpc02</code></li>
      <li>TGW에 <code class="language-plaintext highlighter-rouge">kkom4k-vpc-02</code>를 연결하고, 서브넷은 <code class="language-plaintext highlighter-rouge">private-subnet-02</code>를 선택합니다.</li>
    </ul>
  </li>
  <li><strong>VPC-03 어태치먼트:</strong>
    <ul>
      <li>이름: <code class="language-plaintext highlighter-rouge">tgw-attach-vpc03</code></li>
      <li>TGW에 <code class="language-plaintext highlighter-rouge">kkom4k-vpc-03</code>를 연결하고, 서브넷은 <code class="language-plaintext highlighter-rouge">private-subnet-03</code>을 선택합니다.</li>
    </ul>
  </li>
</ol>

<h3 id="2단계-spoke-vpc-vpc-02-vpc-03-라우팅-설정">2단계: Spoke VPC (VPC-02, VPC-03) 라우팅 설정</h3>

<p>Spoke VPC의 모든 인터넷 바운드 트래픽(<code class="language-plaintext highlighter-rouge">0.0.0.0/0</code>)이 TGW를 향하도록 라우팅 테이블을 수정합니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">private-subnet-02</code>의 라우팅 테이블:</strong>
    <ul>
      <li>대상: <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code></li>
      <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">private-subnet-03</code>의 라우팅 테이블:</strong>
    <ul>
      <li>대상: <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code></li>
      <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
    </ul>
  </li>
</ul>

<h3 id="3단계-egress-vpc-vpc-01-라우팅-설정">3단계: Egress VPC (VPC-01) 라우팅 설정</h3>

<p>Egress VPC는 TGW에서 수신한 트래픽을 NAT Gateway로, 인터넷에서 돌아온 트래픽을 다시 TGW로 전달해야 합니다.</p>

<ol>
  <li><strong><code class="language-plaintext highlighter-rouge">private-subnet-01</code> (TGW ENI 및 워크로드용) 라우팅 테이블:</strong>
    <blockquote>
      <p>이 서브넷은 TGW를 통해 다른 VPC와 통신하고, 인터넷으로 나가는 트래픽은 NAT Gateway로 전달해야 합니다. 따라서 Spoke VPC로 향하는 경로를 명시적으로 추가해야 합니다.</p>
    </blockquote>
    <ul>
      <li><strong>규칙 1 (VPC-02로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">10.2.0.0/16</code> (VPC-02 CIDR)</li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
        </ul>
      </li>
      <li><strong>규칙 2 (VPC-03으로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">10.3.0.0/16</code> (VPC-03 CIDR)</li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
        </ul>
      </li>
      <li><strong>규칙 3 (인터넷으로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code></li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">NAT Gateway ID</code></li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<blockquote>
  <p><strong>참고:</strong> 규칙 1, 2가 없으면, <code class="language-plaintext highlighter-rouge">vpc-01</code>의 인스턴스가 <code class="language-plaintext highlighter-rouge">vpc-02</code>나 <code class="language-plaintext highlighter-rouge">vpc-03</code>으로 보내는 트래픽이 <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 규칙에 따라 NAT Gateway로 잘못 전송되어 통신이 실패합니다. Spoke VPC로 향하는 트래픽을 TGW로 명확하게 지정해야 합니다.</p>
</blockquote>

<ol>
  <li><strong><code class="language-plaintext highlighter-rouge">public-subnet-01</code> (NAT GW용) 라우팅 테이블:</strong>
    <blockquote>
      <p>인터넷으로 나가는 경로와, Spoke VPC로 돌아가는 경로를 모두 설정해야 합니다.</p>
    </blockquote>
    <ul>
      <li><strong>규칙 1 (인터넷으로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code></li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">Internet Gateway ID</code></li>
        </ul>
      </li>
      <li><strong>규칙 2 (VPC-02로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">10.2.0.0/16</code> (VPC-02 CIDR)</li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
        </ul>
      </li>
      <li><strong>규칙 3 (VPC-03으로):</strong>
        <ul>
          <li>대상: <code class="language-plaintext highlighter-rouge">10.3.0.0/16</code> (VPC-03 CIDR)</li>
          <li>타겟: <code class="language-plaintext highlighter-rouge">Transit Gateway ID</code></li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<blockquote>
  <p><strong>중요:</strong> 규칙 2, 3이 없으면 인터넷에서 돌아온 응답 트래픽이 Spoke VPC로 돌아가지 못해 통신이 실패합니다.</p>
</blockquote>

<h3 id="4단계-transit-gateway-라우팅-테이블-설정">4단계: Transit Gateway 라우팅 테이블 설정</h3>

<p>TGW가 Spoke VPC에서 온 인터넷 트래픽을 Egress VPC로 보내도록 라우팅 테이블을 구성합니다.</p>

<ol>
  <li>TGW에 <code class="language-plaintext highlighter-rouge">TGW-Egress-Route-Table</code>이라는 새 라우팅 테이블을 생성합니다.</li>
  <li><strong>연결(Associations):</strong> <code class="language-plaintext highlighter-rouge">tgw-attach-vpc01</code>, <code class="language-plaintext highlighter-rouge">tgw-attach-vpc02</code>, <code class="language-plaintext highlighter-rouge">tgw-attach-vpc03</code> 어태치먼트를 모두 이 라우팅 테이블에 연결합니다.</li>
  <li><strong>경로(Routes):</strong>
    <ul>
      <li><strong>규칙 1 (인터넷 경로):</strong>
        <ul>
          <li>CIDR: <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code></li>
          <li>타겟 어태치먼트: <code class="language-plaintext highlighter-rouge">tgw-attach-vpc01</code></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>전파(Propagations):</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">tgw-attach-vpc01</code>, <code class="language-plaintext highlighter-rouge">tgw-attach-vpc02</code>, <code class="language-plaintext highlighter-rouge">tgw-attach-vpc03</code> 어태치먼트가 자신의 CIDR을 이 라우팅 테이블에 자동으로 전파하도록 설정합니다. 이 설정을 통해 VPC 간 통신 경로가 동적으로 생성됩니다.</li>
    </ul>
  </li>
</ol>

<h3 id="5단계-통신-확인">5단계: 통신 확인</h3>

<p>4단계의 전파(Propagation) 설정이 완료되면, Spoke VPC 간 통신이 활성화됩니다. Spoke VPC의 라우팅 테이블에서 <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 경로의 타겟을 TGW로 지정했기 때문에, 인터넷뿐만 아니라 다른 VPC로 향하는 트래픽도 TGW로 전달되기 때문입니다.</p>

<p><strong>통신 흐름:</strong></p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">kkom4k-vpc-02</code>의 EC2 인스턴스에서 <code class="language-plaintext highlighter-rouge">kkom4k-vpc-03</code>에 있는 EC2 인스턴스의 프라이빗 IP (예: <code class="language-plaintext highlighter-rouge">10.3.1.10</code>)로 <code class="language-plaintext highlighter-rouge">ping</code>을 전송합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">vpc-02</code>의 라우팅 테이블은 <code class="language-plaintext highlighter-rouge">10.3.0.0/16</code>에 대한 특정 경로가 없으므로, 가장 넓은 범위의 <code class="language-plaintext highlighter-rouge">0.0.0.0/0</code> 경로 규칙을 따라 트래픽을 Transit Gateway로 보냅니다.</li>
  <li>TGW는 <code class="language-plaintext highlighter-rouge">TGW-Egress-Route-Table</code>을 참조합니다. 전파(Propagation) 설정 덕분에, TGW는 <code class="language-plaintext highlighter-rouge">10.3.0.0/16</code> 대역이 <code class="language-plaintext highlighter-rouge">tgw-attach-vpc03</code> 어태치먼트에 연결된 것을 인지하고 있습니다.</li>
  <li>TGW는 트래픽을 <code class="language-plaintext highlighter-rouge">vpc-03</code>으로 포워딩하고, <code class="language-plaintext highlighter-rouge">vpc-03</code>의 EC2 인스턴스는 <code class="language-plaintext highlighter-rouge">ping</code> 요청을 수신합니다.</li>
  <li>응답 트래픽은 역순으로 동일한 경로를 통해 <code class="language-plaintext highlighter-rouge">vpc-02</code>로 돌아옵니다.</li>
</ol>

<p><strong>검증:</strong></p>

<ul>
  <li>각 Spoke VPC에 EC2 인스턴스를 배치하고, 보안 그룹에서 상대방 VPC의 CIDR 대역에 대한 ICMP(Ping) 트래픽을 허용하도록 설정합니다.</li>
  <li>한 인스턴스에서 다른 인스턴스의 프라이빗 IP로 <code class="language-plaintext highlighter-rouge">ping</code>을 실행하여 통신이 정상적으로 이루어지는지 확인합니다.</li>
</ul>

<p>이처럼 TGW를 사용하면 VPC 피어링처럼 각 연결을 개별적으로 설정할 필요 없이, 중앙의 라우팅 테이블 하나로 모든 VPC 간의 통신을 효율적으로 제어할 수 있습니다.</p>]]></content><author><name>Francesco</name></author><category term="aws" /><category term="AWS" /><category term="Transit Gateway" /><category term="VPC" /><category term="Network" /><summary type="html"><![CDATA[클라우드 환경에서 여러 개의 VPC(Virtual Private Cloud)를 운영할 때, 각 VPC가 개별적으로 인터넷 게이트웨이나 NAT 게이트웨이를 보유하는 구성은 관리의 복잡성과 보안 비용을 증가시키는 원인이 되곤 합니다. 이러한 문제를 효과적으로 해결할 수 있는 방안이 바로 중앙 집중식 Egress 아키텍처입니다. 이 모델은 AWS Transit Gateway(TGW)를 중심으로, 여러 VPC의 인터넷 바운드 트래픽을 단 하나의 ‘Egress VPC’에서 통합 관리하는 방식입니다. 이 포스트에서는 kkom4k-vpc-01(Egress VPC), kkom4k-vpc-02(Spoke VPC), kkom4k-vpc-03(Spoke VPC)의 세 가지 VPC를 사용하는 시나리오를 통해, 중앙 집중식 Egress 아키텍처를 구축하는 과정을 단계별로 살펴보겠습니다.]]></summary></entry><entry><title type="html">대용량 AWS VPCFlow 로그 축약/필터링을 위한 Spark 활용</title><link href="http://localhost:4000/pyspark/analyze-aws-vpc-flow-logs-with-pyspark-emr-elasticsearch/" rel="alternate" type="text/html" title="대용량 AWS VPCFlow 로그 축약/필터링을 위한 Spark 활용" /><published>2025-08-19T09:00:00+09:00</published><updated>2025-08-19T09:00:00+09:00</updated><id>http://localhost:4000/pyspark/analyze-aws-vpc-flow-logs-with-pyspark-emr-elasticsearch</id><content type="html" xml:base="http://localhost:4000/pyspark/analyze-aws-vpc-flow-logs-with-pyspark-emr-elasticsearch/"><![CDATA[<h4 id="들어가며-왜-aws-vpcflow-로그-분석이-중요한가">들어가며: 왜 AWS VPCFlow 로그 분석이 중요한가?</h4>

<p>AWS 환경에서 네트워크 트래픽을 모니터링하고 보안을 강화하기 위해 VPCFlow 로그는 필수적인 데이터 소스입니다. VPCFlow 로그는 VPC 내의 모든 네트워크 인터페이스에서 발생하는 IP 트래픽 정보를 캡처하여, 다음과 같은 중요한 질문에 답을 제공합니다.</p>

<ul>
  <li>“어떤 IP가 우리 데이터베이스에 접근을 시도했는가?”</li>
  <li>“비정상적으로 많은 데이터가 외부로 전송되고 있지는 않은가?”</li>
  <li>“보안 그룹(Security Group)에 의해 차단(REJECT)된 트래픽은 무엇인가?”</li>
  <li>“외부와 주기적으로 통신하는 AWS 리소스가 있는가? “”</li>
</ul>

<p>하지만 VPCFlow 로그는 그 양이 엄청나게 많고, 원본(raw) 데이터는 분석하기 어려운 형태를 띄고 있습니다. 하루에도 수십, 수백 기가바이트에 달하는 로그를 효율적으로 처리하고 의미 있는 인사이트를 얻기 위해서는 강력한 데이터 처리 파이프라인이 필요합니다.</p>

<p>이 글에서는 S3에 저장된 대용량 VPCFlow 로그를 AWS EMR(Elastic MapReduce) 클러스터와 PySpark를 사용해 주기적으로 분석하고, 그 결과를 Elasticsearch에 저장하여 검색 및 시각화를 용이하게 하는 파이프라인에 대한 소개합니다.</p>

<h4 id="시스템-간-데이터-및-연동-흐름">시스템 간 데이터 및 연동 흐름</h4>

<p>이 태스크의 데이터 파이프라인은 다음과 같은 흐름으로 동작하며, 각 시스템이 유기적으로 연동됩니다.</p>

<pre><code class="language-mermaid">graph TD
    subgraph Data Source
        A[AWS S3] --&gt; B(VPC Flow Logs)
    end

    subgraph Processing &amp; Storage
        C(Scheduler/Cron) --&gt; D[EMR Cluster]
        D -- spark-submit --&gt; E(PySpark Task)
        E -- Reads Data --&gt; B
        E -- Writes Processed Data --&gt; F[Elasticsearch]
    end

    subgraph Deployment
        G[Development Environment] --&gt; H(build_egg.sh)
        H --&gt; I(deploy_to_emr.sh)
        I -- Uploads Artifacts Egg, main.py, config --&gt; J[S3 EMR Artifacts Bucket]
        D -- Fetches Code --&gt; J
    end
</code></pre>

<ol>
  <li>데이터 소스 (AWS S3): AWS VPC Flow 로그는 설정에 따라 S3 버킷에 자동으로 저장됩니다. 로그는 <code class="language-plaintext highlighter-rouge">.../year=YYYY/month=MM/day=DD/hour=HH/</code> 와 같은 시간 기반 파티션 구조로 저장되어, 특정 시간대의 데이터를 효율적으로 조회할 수 있습니다.</li>
  <li>태스크 실행 트리거 (Scheduler/Cron): 외부 스케줄러(예: AWS Lambda, AWS Step Functions, Apache Airflow 또는 간단한 Cron 잡)가 매시간 EMR 클러스터에 PySpark 태스크 실행을 지시합니다.</li>
  <li>처리 엔진 (AWS EMR &amp; PySpark):
    <ul>
      <li>스케줄러의 지시에 따라 EMR 클러스터가 활성화되거나 기존 클러스터에 새로운 <code class="language-plaintext highlighter-rouge">spark-submit</code> 명령이 전달됩니다.</li>
      <li><code class="language-plaintext highlighter-rouge">spark-submit</code>은 S3에 미리 배포된 PySpark 코드(EGG 파일 및 <code class="language-plaintext highlighter-rouge">main.py</code>)를 가져와 EMR 클러스터의 분산 환경에서 PySpark 태스크를 실행합니다.</li>
      <li>PySpark 태스크는 S3의 VPC Flow 로그 데이터를 읽어와 분산 처리합니다. 이 과정에서 AWS STS(Security Token Service)를 통해 임시 자격증명을 얻어 S3 접근 보안을 강화합니다.</li>
    </ul>
  </li>
  <li>데이터 목적지 (Elasticsearch): 처리 및 정제된 로그 데이터는 Elasticsearch에 저장됩니다. Elasticsearch는 전문 검색 엔진으로서, 복잡한 쿼리, 집계, 분석을 실시간에 가깝게 수행할 수 있으며 Kibana를 통해 쉽게 시각화할 수 있습니다.</li>
  <li>배포 과정: 개발 환경에서 작성된 PySpark 코드는 <code class="language-plaintext highlighter-rouge">build_egg.sh</code> 스크립트를 통해 Python EGG 패키지로 빌드됩니다. 이 패키지와 <code class="language-plaintext highlighter-rouge">main.py</code> 스크립트, 설정 파일(<code class="language-plaintext highlighter-rouge">config.json</code>)은 <code class="language-plaintext highlighter-rouge">deploy_to_emr.sh</code> 스크립트를 통해 EMR 클러스터가 접근할 수 있는 S3 버킷(EMR Artifacts Bucket)에 업로드됩니다. EMR 클러스터는 태스크 실행 시 이 S3 버킷에서 필요한 코드와 설정을 가져옵니다.</li>
</ol>

<h4 id="프로젝트-구조-살펴보기">프로젝트 구조 살펴보기</h4>

<p>이 태스크는 역할에 따라 명확하게 모듈이 분리되어 있습니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">main.py</code>: 파이프라인의 시작점(Entry Point)입니다. 커맨드 라인 인자(argument)를 파싱하고, 설정 파일을 로드하며, 전체 처리 과정을 오케스트레이션합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">processors/vpc_flow_processor.py</code>: 실제 데이터 처리 로직이 담긴 핵심 모듈입니다. PySpark를 사용하여 데이터를 읽고, 특정 조건에 따라 필터링 및 집계하는 역할을 수행합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">common/elasticsearch_client.py</code>: 처리된 데이터를 Elasticsearch로 전송하는 역할을 담당합니다. Bulk API를 사용하여 대량의 데이터를 효율적으로 인덱싱합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">config/config.json</code>: S3 경로, Elasticsearch 주소, 필터링 조건 등 파이프라인에 필요한 모든 설정을 관리합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">*.sh</code>: <code class="language-plaintext highlighter-rouge">build_egg.sh</code>, <code class="language-plaintext highlighter-rouge">deploy_to_emr.sh</code> 등 빌드, 배포, 실행을 자동화하는 쉘 스크립트들입니다.</li>
</ul>

<h4 id="핵심-구현-내용">핵심 구현 내용</h4>

<h5 id="1--pyspark를-이용한-데이터-처리-vpc_flow_processorpy">1.  PySpark를 이용한 데이터 처리 (<code class="language-plaintext highlighter-rouge">vpc_flow_processor.py</code>)</h5>

<p>이 태스크의 심장부입니다. <code class="language-plaintext highlighter-rouge">VPCFlowProcessor</code> 클래스는 다양한 필터링 시나리오에 맞춰 데이터를 처리하는 메서드를 제공합니다.</p>

<ul>
  <li>추상화된 기본 프로세서 (<code class="language-plaintext highlighter-rouge">BaseLogProcessor</code>): <code class="language-plaintext highlighter-rouge">processors/base_processor.py</code>에 정의된 <code class="language-plaintext highlighter-rouge">BaseLogProcessor</code>는 Python의 <code class="language-plaintext highlighter-rouge">abc</code> (Abstract Base Classes) 모듈을 사용하여 추상 클래스로 구현되었습니다. 이는 모든 로그 프로세서가 <code class="language-plaintext highlighter-rouge">initialize()</code>와 <code class="language-plaintext highlighter-rouge">process_data()</code>와 같은 필수 메서드를 구현하도록 강제하여, 코드의 일관성과 확장성을 높입니다. <code class="language-plaintext highlighter-rouge">VPCFlowProcessor</code>는 이 <code class="language-plaintext highlighter-rouge">BaseLogProcessor</code>를 상속받아 VPC Flow 로그 처리에 특화된 로직을 구현합니다.</li>
  <li>Spark 세션 생성: S3에 접근하기 위해 AWS STS(Security Token Service)를 통해 임시 자격증명을 얻고, 이를 Spark 세션 설정에 주입하여 보안을 강화합니다.</li>
  <li>데이터 로딩: <code class="language-plaintext highlighter-rouge">build_s3_paths</code> 메서드는 현재 시간으로부터 1시간 전의 S3 경로를 동적으로 생성하여, 스케줄링 실행 시점에 맞는 데이터를 정확히 찾아냅니다.</li>
  <li>
    <p>주요 필터 및 집계: <code class="language-plaintext highlighter-rouge">process_data</code> 메서드는 <code class="language-plaintext highlighter-rouge">--filter</code> 옵션 값에 따라 각기 다른 처리 메서드를 호출합니다.</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">ssh_gateway</code> 필터:
        <ul>
          <li>목적: SSH 게이트웨이 인스턴스를 통한 모든 네트워크 트래픽을 식별하고 추출하여 원격 접근 패턴 및 잠재적 보안 이벤트를 모니터링합니다.</li>
          <li>필터링: 설정된 SSH 게이트웨이 인스턴스 ID(<code class="language-plaintext highlighter-rouge">instance-id</code>)와 일치하는 로그를 필터링합니다.</li>
          <li>집계: 별도의 집계 없이 필터링된 원본 로그를 반환합니다.</li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">database_traffic</code> 필터:
        <ul>
          <li>목적: 데이터베이스 관련 트래픽 패턴을 분석합니다.</li>
          <li>필터링:
            <ul>
              <li>데이터베이스 포트(기본값: MySQL 3306, PostgreSQL 5432)로의 이그레스(egress) 트래픽 중 특정 바이트 임계값(예: 1MB)을 초과하는 경우.</li>
              <li>웹 포트(80, 443)가 아닌 포트를 통한 대용량 데이터 전송.</li>
              <li>데이터베이스 포트로의 인그레스(ingress) 트래픽 중 REJECT된 연결 시도.</li>
              <li>특정 VPC 내에서 S3 서비스로의 대용량 트래픽.</li>
              <li>특정 VPC 내에서 특정 <code class="language-plaintext highlighter-rouge">traffic-path</code> (예: 2, 8)를 통한 모든 트래픽.</li>
            </ul>
          </li>
          <li>집계: 별도의 집계 없이 필터링된 원본 로그를 반환합니다.</li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">large_transfers</code> 필터:
        <ul>
          <li>목적: 설정된 크기 임계값(기본값: 5MB)을 초과하는 대용량 데이터 전송을 식별하고 집계합니다. 직접적인 인터넷 트래픽(<code class="language-plaintext highlighter-rouge">traffic-path != 1</code>)은 제외합니다.</li>
          <li>필터링: <code class="language-plaintext highlighter-rouge">bytes</code> 필드가 임계값을 초과하고 <code class="language-plaintext highlighter-rouge">traffic-path</code>가 1이 아닌 로그를 필터링합니다.</li>
          <li>집계: <code class="language-plaintext highlighter-rouge">account-id</code>, <code class="language-plaintext highlighter-rouge">srcaddr</code>, <code class="language-plaintext highlighter-rouge">dstaddr</code>, <code class="language-plaintext highlighter-rouge">dstport</code>, <code class="language-plaintext highlighter-rouge">protocol</code> 등 연결 정보를 기준으로 그룹화하여 <code class="language-plaintext highlighter-rouge">bytes</code>와 <code class="language-plaintext highlighter-rouge">packets</code>의 합계(<code class="language-plaintext highlighter-rouge">spark_sum</code>)를 계산합니다.</li>
        </ul>
      </li>
      <li><code class="language-plaintext highlighter-rouge">rejected_traffic</code> 필터:
        <ul>
          <li>목적: 보안 이벤트, 정찰 시도 또는 잘못된 구성 등을 나타낼 수 있는 거부된 네트워크 트래픽 패턴을 분석합니다.</li>
          <li>필터링: <code class="language-plaintext highlighter-rouge">action</code>이 <code class="language-plaintext highlighter-rouge">REJECT</code>이고, <code class="language-plaintext highlighter-rouge">dstport</code>가 ‘24234’, <code class="language-plaintext highlighter-rouge">protocol</code>이 ‘6’(TCP), <code class="language-plaintext highlighter-rouge">tcp-flags</code>가 ‘2’(SYN)인 특정 패턴의 로그를 필터링합니다.</li>
          <li>집계: <code class="language-plaintext highlighter-rouge">account-id</code>, <code class="language-plaintext highlighter-rouge">srcaddr</code>, <code class="language-plaintext highlighter-rouge">dstaddr</code>, <code class="language-plaintext highlighter-rouge">dstport</code>, <code class="language-plaintext highlighter-rouge">protocol</code> 등 연결 정보를 기준으로 그룹화하여 <code class="language-plaintext highlighter-rouge">bytes</code>와 <code class="language-plaintext highlighter-rouge">packets</code>의 합계(<code class="language-plaintext highlighter-rouge">spark_sum</code>)를 계산합니다.</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># processors/vpc_flow_processor.py의 일부
</span>
<span class="kn">from</span> <span class="nn">abc</span> <span class="kn">import</span> <span class="n">ABC</span><span class="p">,</span> <span class="n">abstractmethod</span> <span class="c1"># BaseLogProcessor에서 ABC를 임포트하여 사용
</span>
<span class="k">class</span> <span class="nc">BaseLogProcessor</span><span class="p">(</span><span class="n">ABC</span><span class="p">):</span>
    <span class="c1"># ... (추상 메서드 정의)
</span>
<span class="k">class</span> <span class="nc">VPCFlowProcessor</span><span class="p">(</span><span class="n">BaseLogProcessor</span><span class="p">):</span>
    <span class="c1"># ... (필터링 및 집계 로직 구현)
</span></code></pre></div></div>

<h5 id="2--elasticsearch-연동-elasticsearch_clientpy">2.  Elasticsearch 연동 (<code class="language-plaintext highlighter-rouge">elasticsearch_client.py</code>)</h5>

<p><code class="language-plaintext highlighter-rouge">ElasticsearchClient</code> 클래스는 처리된 데이터를 Elasticsearch에 안정적으로 전송하는 모든 기능을 캡슐화합니다.</p>

<ul>
  <li>Connection 관리: 설정 파일 기반으로 Elasticsearch 클러스터에 연결합니다.</li>
  <li>Index Template: <code class="language-plaintext highlighter-rouge">create_index_template</code> 메서드는 VPC Flow 로그 필드에 최적화된 매핑을 가진 인덱스 템플릿을 생성합니다. <code class="language-plaintext highlighter-rouge">srcaddr</code>, <code class="language-plaintext highlighter-rouge">dstaddr</code> 필드를 <code class="language-plaintext highlighter-rouge">ip</code> 타입으로 매핑하여 IP 범위 검색과 같은 네트워크 분석 기능을 활성화합니다.</li>
  <li>Bulk Indexing: <code class="language-plaintext highlighter-rouge">send_data</code>와 <code class="language-plaintext highlighter-rouge">send_batch</code> 메서드는 대량의 문서를 작은 배치(batch)로 나누어 Bulk API로 전송함으로써, 네트워크 부하를 줄이고 인덱싱 성능을 극대화합니다.</li>
</ul>

<ol>
  <li>배포 자동화 (EGG 패키징 및 EMR 배포)</li>
</ol>

<p>EMR 클러스터에 PySpark 태스크를 배포하는 것은 다소 복잡할 수 있습니다. 이 태스크는 쉘 스크립트를 통해 이 과정을 자동화합니다.</p>

<ul>
  <li>
    <p>EGG 패키징: <code class="language-plaintext highlighter-rouge">build_egg.sh</code> 스크립트는 Python 프로젝트를 <code class="language-plaintext highlighter-rouge">.egg</code> 파일로 패키징합니다. <code class="language-plaintext highlighter-rouge">.egg</code> 파일은 PySpark 애플리케이션을 EMR 클러스터에 배포할 때 필요한 모든 Python 코드와 의존성을 포함하는 단일 아카이브 파일입니다. 이는 PySpark가 클러스터의 모든 노드에서 필요한 코드를 쉽게 사용할 수 있도록 합니다.</p>
  </li>
  <li>
    <p>EMR 배포: <code class="language-plaintext highlighter-rouge">deploy_to_emr.sh</code> 스크립트는 다음 단계를 수행합니다.</p>
    <ol>
      <li><code class="language-plaintext highlighter-rouge">build_egg.sh</code>를 호출하여 최신 <code class="language-plaintext highlighter-rouge">.egg</code> 파일을 생성합니다.</li>
      <li><code class="language-plaintext highlighter-rouge">aws s3 cp</code> 명령어를 사용해 패키징된 <code class="language-plaintext highlighter-rouge">.egg</code> 파일, <code class="language-plaintext highlighter-rouge">main.py</code> 스크립트, 그리고 설정 파일(<code class="language-plaintext highlighter-rouge">config.json</code> 및 기타 환경별 설정 파일)을 EMR 클러스터가 접근할 수 있는 S3 버킷에 업로드합니다. 이 S3 버킷은 EMR 클러스터가 태스크 실행 시 코드를 가져오는 소스 역할을 합니다.</li>
      <li>배포가 완료되면, EMR 클러스터에 step을 추가할 때 사용할 수 있는 <code class="language-plaintext highlighter-rouge">spark-submit</code> 명령어 예시와 JSON 설정을 자동으로 생성하여 출력해줍니다. 이 <code class="language-plaintext highlighter-rouge">spark-submit</code> 명령어는 S3에 업로드된 <code class="language-plaintext highlighter-rouge">.egg</code> 파일과 <code class="language-plaintext highlighter-rouge">main.py</code>의 경로를 지정하여 EMR 클러스터에서 PySpark 태스크를 실행하도록 지시합니다.</li>
    </ol>
  </li>
</ul>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">#</span><span class="w"> </span><span class="err">deploy_to_emr.sh</span><span class="w"> </span><span class="err">스크립트가</span><span class="w"> </span><span class="err">생성하는</span><span class="w"> </span><span class="err">EMR</span><span class="w"> </span><span class="err">Step</span><span class="w"> </span><span class="err">예시</span><span class="w">

</span><span class="nl">"Args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"spark-submit"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"--py-files"</span><span class="p">,</span><span class="w"> </span><span class="s2">"s3://your-bucket/vpc-flow-processor-1.0.0-py3.egg"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"s3://your-bucket/main.py"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"--filter"</span><span class="p">,</span><span class="w"> </span><span class="s2">"database_traffic"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"--config"</span><span class="p">,</span><span class="w"> </span><span class="s2">"s3://your-bucket/config/config.json"</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<h4 id="실행-및-활용">실행 및 활용</h4>

<p>이 파이프라인은 Airflow나 AWS Step Functions, Lambda와 같은 스케줄링 도구와 결합하여 매시간 자동으로 실행되도록 구성할 수 있습니다.</p>

<p>실행 스크립트(<code class="language-plaintext highlighter-rouge">vpcflow_dbport.sh</code>, <code class="language-plaintext highlighter-rouge">vpcflow_sshgw.sh</code> 등)는 내부적으로 <code class="language-plaintext highlighter-rouge">spark-submit</code>을 호출하며, <code class="language-plaintext highlighter-rouge">--filter</code> 옵션을 다르게 지정하여 다양한 분석 목적을 달성합니다.</p>

<p>예를 들어, <code class="language-plaintext highlighter-rouge">vpcflow_dbport.sh</code>를 실행하면 데이터베이스 관련 트래픽을 분석하고, <code class="language-plaintext highlighter-rouge">rejected_traffic.sh</code>를 실행하면 거부된 트래픽을 분석하여 각각의 결과를 Elasticsearch 인덱스에 저장합니다.</p>

<p>이렇게 저장된 데이터는 Kibana 대시보드를 통해 시각화하여, 네트워크 관리자와 보안 담당자가 트래픽 현황을 한눈에 파악하고 이상 징후를 신속하게 탐지할 수 있도록 돕습니다.</p>

<h4 id="github-저장소">GitHub 저장소</h4>
<ul>
  <li>https://github.com/kk0m4k/pyspak_vpcflow</li>
</ul>]]></content><author><name>Francesco</name></author><category term="pyspark" /><category term="PySpark" /><category term="EMR" /><summary type="html"><![CDATA[들어가며: 왜 AWS VPCFlow 로그 분석이 중요한가?]]></summary></entry><entry><title type="html">GitHub Actions와 OIDC를 이용한 안전한 AWS 배포 파이프라인 구축하기</title><link href="http://localhost:4000/aws/secure-aws-deployment-with-github-actions-oidc/" rel="alternate" type="text/html" title="GitHub Actions와 OIDC를 이용한 안전한 AWS 배포 파이프라인 구축하기" /><published>2025-08-18T00:00:00+09:00</published><updated>2025-08-18T00:00:00+09:00</updated><id>http://localhost:4000/aws/secure-aws-deployment-with-github-actions-oidc</id><content type="html" xml:base="http://localhost:4000/aws/secure-aws-deployment-with-github-actions-oidc/"><![CDATA[<p>Github Actions 기능을 이용하여 AWS ECR에 도커 이미지를 등록하고, EKS를 통해서 이미지를 배포할때, AWS Longterm Access Keys를 사용하지 않고, OIDC를 기반의 STS를 사용하는 방법에 대한 내용이니다.  많은 팀이 CI/CD 파이프라인에서 AWS에 접근하기 위해 IAM 사용자의 Access Key를 GitHub Secrets에 저장하여 사용합니다. 이 방식은 간단하지만, 다음과 같은 심각한 보안 위협을 내포하고 있습니다.</p>

<ul>
  <li><strong>키 유출 위험</strong>: Secret에 저장된 키는 유출될 경우 큰 보안 사고로 이어질 수 있습니다.</li>
  <li><strong>권한 관리의 어려움</strong>: 정적 키는 보통 넓은 권한을 가지며, 최소 권한 원칙(Principle of Least Privilege)을 지키기 어렵습니다.</li>
  <li><strong>주기적인 키 교체의 번거로움</strong>: 보안을 위해 주기적으로 키를 교체해야 하지만, 이 과정은 번거롭고 실수가 발생하기 쉽습니다.</li>
</ul>

<p>이러한 문제를 해결하기 위해, GitHub와 AWS는 OIDC를 통해 비밀번호 없는(passwordless) 인증 메커니즘을 제공합니다. 이 방식을 사용하면 GitHub Actions 워크플로우가 직접 AWS IAM 역할을 수임(AssumeRole)하여 필요한 권한만 가진 임시 자격 증명을 발급받을 수 있습니다.</p>

<h2 id="전체-배포-흐름-및-아키텍처">전체 배포 흐름 및 아키텍처</h2>

<p><code class="language-plaintext highlighter-rouge">kkom4k/github-actions-practice</code> 리포지토리의 배포 프로세스는 다음과 같이 진행됩니다.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant GA as GitHub Actions Workflow
    participant GHOIDC as GitHub OIDC Provider
    participant AWS_STS as AWS STS
    participant IAM_Role as "IAM Role"
    participant AWS_SVC as "AWS Services (ECR, EKS)"

    GA-&gt;&gt;GHOIDC: 1. Request JWT (ID Token)
    GHOIDC--&gt;&gt;GA: 2. Return signed JWT

    GA-&gt;&gt;AWS_STS: 3. AssumeRoleWithWebIdentity(JWT)

    AWS_STS-&gt;&gt;IAM_Role: 4. Validate caller against **Trust Policy**
    note right of IAM_Role: Checks if GitHub Actions&lt;br&gt;(repo, branch) is a trusted entity.
    IAM_Role--&gt;&gt;AWS_STS: Validation Success

    AWS_STS--&gt;&gt;GA: 5. Return Temporary Credentials
    note left of GA: Credentials inherit permissions&lt;br&gt;from the role's **Permissions Policy**.

    GA-&gt;&gt;AWS_SVC: 6. Access services with temp credentials
    note right of AWS_SVC: AWS validates if the action&lt;br&gt;is allowed by the **Permissions Policy**.
</code></pre>

<h2 id="구현-단계별-가이드">구현 단계별 가이드</h2>

<p>이제 실제로 어떻게 설정하는지 단계별로 알아보겠습니다.</p>

<h3 id="1단계-aws-iam-설정">1단계: AWS IAM 설정</h3>

<p>가장 먼저 AWS에서 <code class="language-plaintext highlighter-rouge">kkom4k/github-actions-practice</code> 리포지토리의 Actions를 신뢰하도록 OIDC 자격 증명 공급자와 IAM 역할을 설정해야 합니다.</p>

<h4 id="11-oidc-자격-증명-공급자identity-provider-추가">1.1. OIDC 자격 증명 공급자(Identity Provider) 추가</h4>

<ul>
  <li>IAM 콘솔 &gt; <strong>자격 증명 공급자</strong>에서 다음 정보로 공급자를 추가합니다.
    <ul>
      <li><strong>공급자 URL</strong>: <code class="language-plaintext highlighter-rouge">https://token.actions.githubusercontent.com</code></li>
      <li><strong>대상(Audience)</strong>: <code class="language-plaintext highlighter-rouge">sts.amazonaws.com</code></li>
    </ul>
  </li>
</ul>

<h4 id="12-iam-역할-생성">1.2. IAM 역할 생성</h4>

<p><code class="language-plaintext highlighter-rouge">deploy.yml</code>에서 사용하는 <code class="language-plaintext highlighter-rouge">arn:aws:iam::9923******29:role/github-actions-ecr-deploy-role</code> 역할을 생성합니다. 역할의 <strong>신뢰 정책(Trust Policy)</strong>은 GitHub Actions가 이 역할을 수임(Assume)할 수 있도록 허용하는 설정입니다.</p>

<ul>
  <li><strong>신뢰할 수 있는 엔터티 유형</strong>: 웹 자격 증명(Web identity)</li>
  <li><strong>자격 증명 공급자</strong>: 위에서 만든 <code class="language-plaintext highlighter-rouge">token.actions.githubusercontent.com</code></li>
  <li>
    <p><strong>신뢰 정책 편집</strong>: <code class="language-plaintext highlighter-rouge">kkom4k/github-actions-practice</code> 리포지토리의 <code class="language-plaintext highlighter-rouge">main</code> 브랜치에서만 역할을 수임할 수 있도록 <strong>조건(Condition)</strong>을 추가합니다.</p>

    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Principal"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                </span><span class="nl">"Federated"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::9923******29:oidc-provider/token.actions.githubusercontent.com"</span><span class="w">
            </span><span class="p">},</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"sts:AssumeRoleWithWebIdentity"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Condition"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                </span><span class="nl">"StringLike"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                    </span><span class="nl">"token.actions.githubusercontent.com:sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"repo:kkom4k/github-actions-practice:ref:refs/heads/main"</span><span class="w">
                </span><span class="p">}</span><span class="w">
            </span><span class="p">}</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
</ul>

<h4 id="13-iam-권한-정책-연결">1.3. IAM 권한 정책 연결</h4>

<p>이제 역할이 수임된 후 <strong>수행할 수 있는 작업</strong>을 정의하는 <strong>권한 정책(Permissions Policy)</strong>을 연결합니다. 최소 권한 원칙에 따라 ECR과 EKS에 필요한 최소한의 권한만 부여하는 것이 좋습니다.</p>

<h5 id="ecr-푸시를-위한-권한-정책-예시">ECR 푸시를 위한 권한 정책 예시</h5>
<p>Docker 이미지를 ECR에 푸시하기 위해 필요한 권한들입니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
                </span><span class="s2">"ecr:GetAuthorizationToken"</span><span class="w">
            </span><span class="p">],</span><span class="w">
            </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"*"</span><span class="w">
        </span><span class="p">},</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
                </span><span class="s2">"ecr:BatchCheckLayerAvailability"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:GetDownloadUrlForLayer"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:GetRepositoryPolicy"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:DescribeRepositories"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:ListImages"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:DescribeImages"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:BatchGetImage"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:InitiateLayerUpload"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:UploadLayerPart"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:CompleteLayerUpload"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"ecr:PutImage"</span><span class="w">
            </span><span class="p">],</span><span class="w">
            </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:ecr:ap-northeast-2:9923******29:repository/kkom4k-repo"</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h5 id="eks-배포를-위한-권한-정책-예시">EKS 배포를 위한 권한 정책 예시</h5>
<p><code class="language-plaintext highlighter-rouge">aws eks update-kubeconfig</code> 명령을 실행하여 <code class="language-plaintext highlighter-rouge">kubectl</code>이 클러스터와 통신할 수 있도록 설정하는 권한입니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"eks:DescribeCluster"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:eks:ap-northeast-2:9923******29:cluster/kkom4k-cluster"</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>
<p>이 정책들을 <code class="language-plaintext highlighter-rouge">github-actions-ecr-deploy-role</code> 역할에 연결합니다.</p>

<h3 id="2단계-github-actions-워크플로우-deployyml-작성">2단계: GitHub Actions 워크플로우 (<code class="language-plaintext highlighter-rouge">deploy.yml</code>) 작성</h3>

<p>다음은 <code class="language-plaintext highlighter-rouge">kkom4k/github-actions-practice</code> 리포지토리의 <code class="language-plaintext highlighter-rouge">.github/workflows/deploy.yml</code> 파일의 핵심 내용입니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .github/workflows/deploy.yml</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to Amazon EKS using OIDC</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">main</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">id-token</span><span class="pi">:</span> <span class="s">write</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">env</span><span class="pi">:</span>
  <span class="na">AWS_REGION</span><span class="pi">:</span> <span class="s">ap-northeast-2</span>
  <span class="na">ECR_REPOSITORY</span><span class="pi">:</span> <span class="s">kkom4k-repo</span>
  <span class="na">EKS_CLUSTER_NAME</span><span class="pi">:</span> <span class="s">kkom4k-cluster</span>
  <span class="na">K8S_DEPLOYMENT_NAME</span><span class="pi">:</span> <span class="s">kkom4k-app-deployment</span>
  <span class="na">K8S_CONTAINER_NAME</span><span class="pi">:</span> <span class="s">kkom4k-app-container</span>
  <span class="na">AWS_ROLE_ARN</span><span class="pi">:</span> <span class="s">arn:aws:iam::9923******29:role/github-actions-ecr-deploy-role</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build-and-push</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">Build and Push to ECR</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">outputs</span><span class="pi">:</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout source code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure AWS credentials</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/configure-aws-credentials@v2</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">role-to-assume</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">aws-region</span><span class="pi">:</span> <span class="s">$</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Login to Amazon ECR</span>
        <span class="na">id</span><span class="pi">:</span> <span class="s">login-ecr</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/amazon-ecr-login@v1</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build, tag, and push image to Amazon ECR</span>
        <span class="na">id</span><span class="pi">:</span> <span class="s">build-image</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">IMAGE_URI="$/$:$"</span>
          <span class="s">docker build -t $IMAGE_URI .</span>
          <span class="s">docker push $IMAGE_URI</span>
          <span class="s">echo "image=${IMAGE_URI}" &gt;&gt; $GITHUB_OUTPUT</span>

  <span class="na">deploy-to-eks</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to EKS Cluster</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">needs</span><span class="pi">:</span> <span class="s">build-and-push</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout source code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Configure AWS credentials</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/configure-aws-credentials@v2</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">role-to-assume</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">aws-region</span><span class="pi">:</span> <span class="s">$</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Update kubeconfig</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">aws eks update-kubeconfig --name $ --region $</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to EKS</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">IMAGE_URI=$</span>
          <span class="s">kubectl set image deployment/$ $=${IMAGE_URI}</span>
</code></pre></div></div>

<h3 id="3단계-kubernetes-매니페스트-파일-작성">3단계: Kubernetes 매니페스트 파일 작성</h3>

<p>마지막으로 EKS에 배포할 애플리케이션의 <code class="language-plaintext highlighter-rouge">Deployment</code> 매니페스트 파일 예시입니다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">kkom4k-app-deployment</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">2</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">kkom4k-app</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">kkom4k-app</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kkom4k-app-container</span>
        <span class="na">image</span><span class="pi">:</span> <span class="s">YOUR_ECR_REPO_URI</span> <span class="c1"># 이 부분은 GitHub Actions에서 동적으로 교체됩니다.</span>
        <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div></div>

<p>AWS와 GitHub를 사용하고 있다면, 지금 바로 여러분의 CI/CD 파이프라인에 OIDC 인증 방식을 도입하여 더 안전하고 효율적인 개발 문화를 만들어가시길 바랍니다.</p>]]></content><author><name>Francesco</name></author><category term="AWS" /><category term="GitHub Actions" /><category term="OIDC" /><category term="ECR" /><category term="EKS" /><summary type="html"><![CDATA[Github Actions 기능을 이용하여 AWS ECR에 도커 이미지를 등록하고, EKS를 통해서 이미지를 배포할때, AWS Longterm Access Keys를 사용하지 않고, OIDC를 기반의 STS를 사용하는 방법에 대한 내용이니다. 많은 팀이 CI/CD 파이프라인에서 AWS에 접근하기 위해 IAM 사용자의 Access Key를 GitHub Secrets에 저장하여 사용합니다. 이 방식은 간단하지만, 다음과 같은 심각한 보안 위협을 내포하고 있습니다.]]></summary></entry><entry><title type="html">AWS 다중 계정 환경에서 AssumeRole을 사용하여 안전하게 S3에 접근하기</title><link href="http://localhost:4000/aws/aws-cross-account-s3-access-with-assumerole/" rel="alternate" type="text/html" title="AWS 다중 계정 환경에서 AssumeRole을 사용하여 안전하게 S3에 접근하기" /><published>2025-08-17T10:00:00+09:00</published><updated>2025-08-17T10:00:00+09:00</updated><id>http://localhost:4000/aws/aws-cross-account-s3-access-with-assumerole</id><content type="html" xml:base="http://localhost:4000/aws/aws-cross-account-s3-access-with-assumerole/"><![CDATA[<p>많은 기업들이 AWS를 사용하면서 보안 강화, 비용 분리, 장애 영향 범위 최소화 등을 위해 여러 개의 AWS 계정을 운영하는 ‘다중 계정(Multi-Account)’ 전략을 채택합니다. 예를 들어, 다음과 같은 구조를 흔히 볼 수 있습니다.</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-prod</code> (프로덕션 계정):</strong> 실제 서비스가 운영되는 계정</li>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-dev</code> (개발 계정):</strong> 개발 및 테스트 환경을 위한 계정</li>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code> (관리 계정):</strong> 여러 계정의 로그를 중앙에서 수집하거나, 보안 감사를 수행하고, CI/CD 파이프라인을 관리하는 중앙 허브 계정</li>
</ul>

<p>이런 환경에서 다음과 같은 요구사항이 생깁니다.</p>

<blockquote>
  <p>“중앙 관리(<code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code>) 계정에서 프로덕션(<code class="language-plaintext highlighter-rouge">kkom4k-prod</code>) 계정에 있는 S3 버킷(<code class="language-plaintext highlighter-rouge">centralized-log</code>)의 로그 파일을 주기적으로 읽어와 분석하고 싶다.”
근
이때 가장 간단하지만 <strong>절대 사용해서는 안 되는 방법</strong>은 프로덕션 계정에 IAM 사용자를 만들고 Access Key를 발급받아 관리 계정의 서버에 저장하는 것입니다. 이는 Access Key 유출 시 프로덕션 계정이 직접적인 위협에 노출되는 심각한 보안 취약점을 야기합니다.</p>
</blockquote>

<p>AWS는 이러한 문제를 해결하기 위해 <strong><code class="language-plaintext highlighter-rouge">sts:AssumeRole</code></strong>이라는 안전한 메커니즘을 제공합니다. <code class="language-plaintext highlighter-rouge">AssumeRole</code>은 한 계정의 IAM 주체(사용자 또는 역할)가 다른 계정의 역할(Role)을 잠시 ‘빌려 입고’ 해당 역할의 권한으로 작업을 수행하도록 허용합니다. 이 과정에서 장기적인 자격 증명(Access Key) 대신, 짧은 시간 동안만 유효한 <strong>임시 자격 증명</strong>을 사용하므로 보안이 크게 향상됩니다.</p>

<p>이 글에서는 <code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code> 계정에서 <code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 계정의 역할을 AssumeRole 한 뒤, Boto3를 사용하여 S3 버킷의 객체 목록을 조회하는 전체 과정과 실제 코드를 상세히 다룹니다.</p>

<h2 id="1-iam-설정-신뢰의-기반-다지기">1. IAM 설정: 신뢰의 기반 다지기</h2>

<p>먼저 두 계정에 AssumeRole을 위한 IAM 역할과 정책을 설정해야 합니다.</p>

<h3 id="1-kkom4k-prod-계정-리소스-소유-계정">1) <code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 계정 (리소스 소유 계정)</h3>

<p>S3 버킷과 다른 계정이 수임할 역할을 생성합니다.</p>

<h4 id="권한-정책-permissions-policy">권한 정책 (Permissions Policy)</h4>
<p>이 역할이 최종적으로 수행할 작업을 정의합니다. 여기서는 <code class="language-plaintext highlighter-rouge">centralized-log</code> 버킷을 조회하는 권한을 부여합니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"s3:ListBucket"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:s3:::centralized-log"</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h4 id="신뢰-정책-trust-policy">신뢰 정책 (Trust Policy)</h4>
<p>어떤 주체가 이 역할을 수임할 수 있는지 정의합니다. <code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code> 계정에게 역할을 빌려줄 것을 명시합니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Principal"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
                </span><span class="nl">"AWS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::MGMT_ACCOUNT_ID:root"</span><span class="w">
            </span><span class="p">},</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"sts:AssumeRole"</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>
<blockquote>
  <p><strong>참고:</strong> <code class="language-plaintext highlighter-rouge">MGMT_ACCOUNT_ID</code>는 <code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code>의 실제 AWS 계정 ID로 변경해야 합니다. <code class="language-plaintext highlighter-rouge">Principal</code>을 특정 IAM 역할로 제한하면 더 안전합니다.</p>
</blockquote>

<h3 id="2-kkom4k-mgmt-계정-중앙-관리-계정">2) <code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code> 계정 (중앙 관리 계정)</h3>

<p><code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 계정의 역할을 수임할 권한을 가진 IAM 정책을 생성하고, 이를 사용자나 EC2 역할에 연결합니다.</p>

<h4 id="권한-정책-permissions-policy-1">권한 정책 (Permissions Policy)</h4>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"Version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2012-10-17"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"Statement"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"Effect"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Allow"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"sts:AssumeRole"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"Resource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"arn:aws:iam::PROD_ACCOUNT_ID:role/ProdS3AccessRole"</span><span class="w">
        </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>
<blockquote>
  <p><strong>참고:</strong> <code class="language-plaintext highlighter-rouge">PROD_ACCOUNT_ID</code>는 <code class="language-plaintext highlighter-rouge">kkom4k-prod</code>의 실제 AWS 계정 ID로, <code class="language-plaintext highlighter-rouge">ProdS3AccessRole</code>은 위에서 생성한 역할의 이름으로 변경해야 합니다.</p>
</blockquote>

<h2 id="2-프로세스-흐름도">2. 프로세스 흐름도</h2>

<p>IAM 설정이 완료되면, 실제 작업은 다음과 같은 흐름으로 진행됩니다.</p>

<ol>
  <li><strong><code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code></strong> 계정의 IAM 주체가 AWS STS(Security Token Service)에 <code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 계정 역할의 ARN을 전달하며 <code class="language-plaintext highlighter-rouge">AssumeRole</code>을 요청합니다.</li>
  <li><strong>AWS STS</strong>는 요청이 유효한지(<code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 역할의 신뢰 정책 확인) 검증합니다.</li>
  <li>검증 완료 시, STS는 <code class="language-plaintext highlighter-rouge">kkom4k-prod</code> 역할의 권한을 가진 <strong>임시 자격 증명</strong>(Access Key, Secret Key, Session Token)을 발급합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">kkom4k-mgmt</code>의 애플리케이션은 이 <strong>임시 자격 증명</strong>을 사용하여 S3 클라이언트를 초기화합니다.</li>
  <li>새로 생성된 S3 클라이언트로 <code class="language-plaintext highlighter-rouge">kkom4k-prod</code>의 <code class="language-plaintext highlighter-rouge">centralized-log</code> 버킷에 API 요청을 보냅니다.</li>
  <li><code class="language-plaintext highlighter-rouge">kkom4k-prod</code>의 S3 서비스는 요청에 담긴 임시 자격 증명이 유효한지 확인하고, 버킷의 객체 목록을 반환합니다.</li>
</ol>

<h2 id="3-전체-흐름-다이어그램">3. 전체 흐름 다이어그램</h2>

<pre><code class="language-mermaid">graph TD
    subgraph "kkom4k-mgmt (Management)"
        UserRole["[IAM User/Role]&lt;br&gt;- sts:AssumeRole policy"]
        Boto3App["[Boto3 App]&lt;br&gt;- Creates S3 client with temp creds"]
    end

    subgraph "kkom4k-prod (Production)"
        ProdRole["[IAM Role: ProdS3AccessRole]&lt;br&gt;- s3:ListBucket policy&lt;br&gt;- Trusts kkom4k-mgmt"]
        S3["[S3: centralized-log]"]
    end

    STS["AWS STS"]

    UserRole -- "1. AssumeRole(ProdS3AccessRole)" --&gt; STS
    STS -- "2. Validate Trust Policy" --&gt; ProdRole
    STS -- "3. Return Temporary Credentials" --&gt; Boto3App
    Boto3App -- "5. ListObjectsV2(centralized-log)" --&gt; S3
    S3 -- "6. Validate Permissions" --&gt; S3
    S3 -- "7. Return Object List" --&gt; Boto3App
</code></pre>

<h2 id="4-boto3-파이썬-코드-예제">4. Boto3 파이썬 코드 예제</h2>

<p>이제 실제 파이썬 코드를 살펴보겠습니다. 이 코드는 네트워크 불안정성에 대비하여 <strong>10번의 재시도(Retry) 로직</strong>을 포함하고 있습니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">boto3</span>
<span class="kn">from</span> <span class="nn">botocore.config</span> <span class="kn">import</span> <span class="n">Config</span>
<span class="kn">from</span> <span class="nn">botocore.exceptions</span> <span class="kn">import</span> <span class="n">ClientError</span>

<span class="c1"># --- 설정 값 (사용자 환경에 맞게 변경) ---
</span><span class="n">PROD_ACCOUNT_ID</span> <span class="o">=</span> <span class="s">"YOUR_PROD_ACCOUNT_ID"</span>  <span class="c1"># kkom4k-prod 계정 ID
</span><span class="n">PROD_ROLE_NAME</span> <span class="o">=</span> <span class="s">"ProdS3AccessRole"</span>       <span class="c1"># kkom4k-prod 계정의 역할 이름
</span><span class="n">TARGET_BUCKET</span> <span class="o">=</span> <span class="s">"centralized-log"</span>         <span class="c1"># kkom4k-prod 계정의 버킷 이름
</span><span class="n">TARGET_REGION</span> <span class="o">=</span> <span class="s">"ap-northeast-2"</span>          <span class="c1"># 대상 버킷이 있는 리전
</span>
<span class="c1"># 1. Backoff 알고리즘을 사용하는 재시도 설정
# Boto3의 표준(standard) 재시도 모드를 사용하고, 최대 시도 횟수를 10으로 설정
</span><span class="n">retry_config</span> <span class="o">=</span> <span class="n">Config</span><span class="p">(</span>
    <span class="n">region_name</span><span class="o">=</span><span class="n">TARGET_REGION</span><span class="p">,</span>
    <span class="n">retries</span><span class="o">=</span><span class="p">{</span>
        <span class="s">'max_attempts'</span><span class="p">:</span> <span class="mi">10</span><span class="p">,</span>
        <span class="s">'mode'</span><span class="p">:</span> <span class="s">'standard'</span>
    <span class="p">}</span>
<span class="p">)</span>

<span class="c1"># 2. STS 클라이언트를 생성하고 AssumeRole 호출
</span><span class="k">try</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Attempting to assume role: arn:aws:iam::</span><span class="si">{</span><span class="n">PROD_ACCOUNT_ID</span><span class="si">}</span><span class="s">:role/</span><span class="si">{</span><span class="n">PROD_ROLE_NAME</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
    
    <span class="c1"># 현재 환경의 기본 자격증명으로 STS 클라이언트 생성
</span>    <span class="n">sts_client</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="n">client</span><span class="p">(</span><span class="s">'sts'</span><span class="p">)</span>
    
    <span class="n">assumed_role_object</span> <span class="o">=</span> <span class="n">sts_client</span><span class="p">.</span><span class="n">assume_role</span><span class="p">(</span>
        <span class="n">RoleArn</span><span class="o">=</span><span class="sa">f</span><span class="s">"arn:aws:iam::</span><span class="si">{</span><span class="n">PROD_ACCOUNT_ID</span><span class="si">}</span><span class="s">:role/</span><span class="si">{</span><span class="n">PROD_ROLE_NAME</span><span class="si">}</span><span class="s">"</span><span class="p">,</span>
        <span class="n">RoleSessionName</span><span class="o">=</span><span class="s">"MgmtS3AccessSession"</span>  <span class="c1"># 감사 로그에 남는 세션 이름
</span>    <span class="p">)</span>
    
    <span class="c1"># 임시 자격 증명 추출
</span>    <span class="n">temp_credentials</span> <span class="o">=</span> <span class="n">assumed_role_object</span><span class="p">[</span><span class="s">'Credentials'</span><span class="p">]</span>
    
    <span class="k">print</span><span class="p">(</span><span class="s">"Successfully assumed role."</span><span class="p">)</span>

<span class="k">except</span> <span class="n">ClientError</span> <span class="k">as</span> <span class="n">error</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Failed to assume role. Error: </span><span class="si">{</span><span class="n">error</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
    <span class="nb">exit</span><span class="p">()</span>

<span class="c1"># 3. 임시 자격 증명과 Retry 설정을 사용하여 S3 클라이언트 생성
</span><span class="k">try</span><span class="p">:</span>
    <span class="n">s3_client_assumed_role</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="n">client</span><span class="p">(</span>
        <span class="s">'s3'</span><span class="p">,</span>
        <span class="n">aws_access_key_id</span><span class="o">=</span><span class="n">temp_credentials</span><span class="p">[</span><span class="s">'AccessKeyId'</span><span class="p">],</span>
        <span class="n">aws_secret_access_key</span><span class="o">=</span><span class="n">temp_credentials</span><span class="p">[</span><span class="s">'SecretAccessKey'</span><span class="p">],</span>
        <span class="n">aws_session_token</span><span class="o">=</span><span class="n">temp_credentials</span><span class="p">[</span><span class="s">'SessionToken'</span><span class="p">],</span>
        <span class="n">config</span><span class="o">=</span><span class="n">retry_config</span>
    <span class="p">)</span>
    
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"
Listing objects in bucket '</span><span class="si">{</span><span class="n">TARGET_BUCKET</span><span class="si">}</span><span class="s">' with assumed role..."</span><span class="p">)</span>

    <span class="c1"># 4. S3 버킷 객체 목록 조회
</span>    <span class="n">response</span> <span class="o">=</span> <span class="n">s3_client_assumed_role</span><span class="p">.</span><span class="n">list_objects_v2</span><span class="p">(</span><span class="n">Bucket</span><span class="o">=</span><span class="n">TARGET_BUCKET</span><span class="p">)</span>
    
    <span class="k">if</span> <span class="s">'Contents'</span> <span class="ow">in</span> <span class="n">response</span><span class="p">:</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"Found objects:"</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">obj</span> <span class="ow">in</span> <span class="n">response</span><span class="p">[</span><span class="s">'Contents'</span><span class="p">]:</span>
            <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"- </span><span class="si">{</span><span class="n">obj</span><span class="p">[</span><span class="s">'Key'</span><span class="p">]</span><span class="si">}</span><span class="s"> (Size: </span><span class="si">{</span><span class="n">obj</span><span class="p">[</span><span class="s">'Size'</span><span class="p">]</span><span class="si">}</span><span class="s"> bytes)"</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"Bucket is empty or no objects found."</span><span class="p">)</span>

<span class="k">except</span> <span class="n">ClientError</span> <span class="k">as</span> <span class="n">error</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Failed to list objects in bucket '</span><span class="si">{</span><span class="n">TARGET_BUCKET</span><span class="si">}</span><span class="s">'. Error: </span><span class="si">{</span><span class="n">error</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
<span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"An unexpected error occurred: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>

</code></pre></div></div>

<h2 id="참고-자료-references">참고 자료 (References)</h2>

<ul>
  <li><a href="https://docs.aws.amazon.com/ko_kr/IAM/latest/UserGuide/tutorial_cross-account-with-roles.html">자습서: IAM 역할을 사용하여 AWS 계정 간에 액세스 권한 위임 (AWS 공식 문서)</a></li>
  <li><a href="https://docs.aws.amazon.com/ko_kr/IAM/latest/UserGuide/id_roles.html">IAM 역할 (AWS 공식 문서)</a></li>
  <li><a href="https://docs.aws.amazon.com/STS/latest/APIReference/API_AssumeRole.html">STS AssumeRole API 참조 (AWS 공식 문서)</a></li>
  <li><a href="https://boto3.amazonaws.com/v1/documentation/api/latest/guide/credentials.html">Boto3 자격 증명(Credentials) 가이드 (공식)</a></li>
</ul>]]></content><author><name>KK0M4k - Francesco</name></author><category term="aws" /><category term="aws" /><category term="iam" /><category term="sts" /><category term="assumerole" /><summary type="html"><![CDATA[많은 기업들이 AWS를 사용하면서 보안 강화, 비용 분리, 장애 영향 범위 최소화 등을 위해 여러 개의 AWS 계정을 운영하는 ‘다중 계정(Multi-Account)’ 전략을 채택합니다. 예를 들어, 다음과 같은 구조를 흔히 볼 수 있습니다.]]></summary></entry><entry><title type="html">AWS S3 VPC endpoint - Gateway vs. Interface</title><link href="http://localhost:4000/aws/aws-s3-vpc-endpoint-comparison/" rel="alternate" type="text/html" title="AWS S3 VPC endpoint - Gateway vs. Interface" /><published>2025-08-17T00:00:00+09:00</published><updated>2025-08-17T00:00:00+09:00</updated><id>http://localhost:4000/aws/aws-s3-vpc-endpoint-comparison</id><content type="html" xml:base="http://localhost:4000/aws/aws-s3-vpc-endpoint-comparison/"><![CDATA[<p>AWS 환경에서 VPC(Virtual Private Cloud) 내의 리소스(예: EC2 인스턴스)가 S3 버킷과 통신해야 하는 경우는 매우 흔합니다. 이때 인터넷을 통하지 않고 AWS 내부 네트워크를 통해 안전하고 빠르게 통신하기 위해 VPC 엔드포인트를 사용합니다.</p>

<p>S3를 위한 VPC 엔드포인트는 크게 <strong>Gateway 방식</strong>과 <strong>Interface 방식</strong>, 두 가지로 나뉩니다. 두 방식은 비용, 보안, 아키텍처 유연성, 그리고 로그 추적 관점에서 중요한 차이점을 가지므로, 워크로드의 특성에 맞는 올바른 방식을 선택하는 것이 중요합니다.</p>

<p>이 글에서는 두 방식의 핵심적인 차이점을 다각도로 분석하고, 각각의 구성 흐름과 실제 S3 접근 로그의 차이점까지 상세히 알아보겠습니다.</p>

<h3 id="1-gateway-엔드포인트-gateway-endpoint">1. Gateway 엔드포인트 (Gateway Endpoint)</h3>

<p>Gateway 엔드포인트는 VPC 외부에 있는 별개의 리소스가 아니라, <strong>VPC 내에 논리적으로 존재하는 구성요소</strong>입니다. 이는 VPC의 라우팅 테이블을 수정하여 S3로 향하는 트래픽을 AWS Private Network를 통해 전달하는 방식으로 동작하며, VPC 내에서 S3로의 경로를 지정하는 ‘게이트웨이’ 역할을 합니다.</p>

<h4 id="구성-흐름-configuration-flow"><strong>구성 흐름 (Configuration Flow)</strong></h4>

<ol>
  <li>VPC 내의 EC2 인스턴스가 S3 버킷의 Public DNS 이름으로 API 요청을 보냅니다.</li>
  <li>해당 인스턴스가 속한 서브넷의 라우팅 테이블에 S3 서비스(prefix-list)로 향하는 트래픽을 VPC 엔드포인트(vpce-xxxx)로 보내라는 규칙이 정의되어 있습니다.</li>
  <li>요청은 인터넷 게이트웨이(IGW)나 NAT 게이트웨이를 거치지 않고, VPC 엔드포인트를 통해 AWS 내부 네트워크로 직접 라우팅되어 S3에 도달합니다.</li>
</ol>

<h4 id="흐름-다이어그램"><strong>흐름 다이어그램</strong></h4>

<pre><code class="language-mermaid">graph TD
    subgraph "AWS Cloud / VPC"
        EC2["EC2 Instance&lt;br&gt;(in Subnet A)"]
        RT["Route Table&lt;br&gt;Dst: s3-prefix-list&lt;br&gt;Target: vpce-xxxxxxxxxx"]
        EC2 --&gt; RT
    end

    VPCGW["VPC Gateway&lt;br&gt;Endpoint"]
    S3["AWS S3"]

    RT -- "(AWS Private Network)" --&gt; VPCGW
    VPCGW --&gt; S3
</code></pre>

<h3 id="2-interface-엔드포인트-interface-endpoint">2. Interface 엔드포인트 (Interface Endpoint)</h3>

<p>Interface 엔드포인트는 AWS PrivateLink 기술을 기반으로 하며, VPC 내 서브넷에 프라이빗 IP 주소를 가진 ENI(Elastic Network Interface)를 생성하여 S3 서비스에 연결합니다. 서비스에 대한 프라이빗 ‘진입점’을 만드는 것과 같습니다.</p>

<h4 id="구성-흐름-configuration-flow-1"><strong>구성 흐름 (Configuration Flow)</strong></h4>

<ol>
  <li>VPC 내의 EC2 인스턴스가 S3의 DNS 이름(<code class="language-plaintext highlighter-rouge">bucket.s3.region.amazonaws.com</code> 등)으로 API 요청을 보냅니다.</li>
  <li>VPC에 설정된 Private DNS 기능이 이 요청을 가로채, S3의 Public IP가 아닌 VPC 내에 생성된 Interface 엔드포인트 ENI의 <strong>프라이빗 IP</strong>로 해석(Resolve)해 줍니다.</li>
  <li>요청은 이 프라이빗 IP를 통해 AWS 내부 네트워크로 전달되어 S3에 도달합니다.</li>
</ol>

<h4 id="흐름-다이어그램-1"><strong>흐름 다이어그램</strong></h4>

<pre><code class="language-mermaid">graph TD
    OnPrem["On-Premises Network (VPN/DX)"]

    subgraph "AWS Cloud / VPC"
        EC2["EC2 Instance&lt;br&gt;(in Subnet B)"]
        DNS["VPC DNS Resolver&lt;br&gt;(s3.region...) -&gt; (10.0.1.123)"]
        ENI["ENI (Private IP)&lt;br&gt;10.0.1.123&lt;br&gt;(Security Group)"]

        EC2 --&gt; DNS
        DNS --&gt; ENI
    end

    S3["AWS S3"]

    OnPrem --&gt; ENI
    ENI -- "(AWS PrivateLink)" --&gt; S3
</code></pre>

<h3 id="3-비교-요약-표">3. 비교 요약 표</h3>

<table>
  <thead>
    <tr>
      <th style="text-align: left">항목</th>
      <th style="text-align: left">Gateway 엔드포인트</th>
      <th style="text-align: left">Interface 엔드포인트 (PrivateLink)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><strong>핵심 원리</strong></td>
      <td style="text-align: left">라우팅 테이블 타겟</td>
      <td style="text-align: left">VPC 내 프라이빗 IP를 가진 ENI</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>비용</strong></td>
      <td style="text-align: left"><strong>무료</strong></td>
      <td style="text-align: left"><strong>유료</strong> (시간당 요금 + 데이터 처리 요금)</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>보안 제어</strong></td>
      <td style="text-align: left">엔드포인트 정책, S3 버킷 정책</td>
      <td style="text-align: left"><strong>보안 그룹</strong>, NACL, 엔드포인트 정책, S3 버킷 정책</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>On-Premise 접근</strong></td>
      <td style="text-align: left"><strong>불가능</strong></td>
      <td style="text-align: left"><strong>가능</strong> (VPN, Direct Connect 경유)</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>S3 로그 <code class="language-plaintext highlighter-rouge">remoteIp</code></strong></td>
      <td style="text-align: left"><strong>소스 EC2의 Private IP</strong></td>
      <td style="text-align: left"><strong>엔드포인트 ENI의 Private IP</strong></td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>DNS</strong></td>
      <td style="text-align: left">Public DNS 사용 (경로만 변경)</td>
      <td style="text-align: left">Private DNS를 통해 프라이빗 IP로 해석</td>
    </tr>
  </tbody>
</table>

<h3 id="4-s3-접근-로그-비교-access-log-comparison">4. S3 접근 로그 비교 (Access Log Comparison)</h3>

<p>두 방식의 가장 큰 운영상의 차이점 중 하나는 S3 서버 접근 로그에 기록되는 소스 IP(<code class="language-plaintext highlighter-rouge">remoteIp</code>)입니다.</p>

<h4 id="gateway-엔드포인트를-통한-접근-로그-예시"><strong>Gateway 엔드포인트를 통한 접근 로그 예시</strong></h4>

<p>Gateway 방식은 소스 IP를 변환하지 않으므로, 로그에 <strong>요청을 보낸 EC2 인스턴스의 실제 프라이빗 IP</strong>가 기록됩니다.</p>

<pre><code class="language-log">... [17/Aug/2025:11:20:15 +0000] 10.0.1.55 ... GET.OBJECT my-object.txt ... "vpce-0123456789abcdef0"
</code></pre>

<ul>
  <li><code class="language-plaintext highlighter-rouge">remoteIp</code>: <code class="language-plaintext highlighter-rouge">10.0.1.55</code> (요청을 보낸 EC2 인스턴스의 IP)</li>
  <li><code class="language-plaintext highlighter-rouge">vpc_endpoint_id</code>: <code class="language-plaintext highlighter-rouge">vpce-0123456789abcdef0</code> (사용된 Gateway 엔드포인트의 ID)</li>
</ul>

<blockquote>
  <p><strong>장점</strong>: 어떤 인스턴스가 S3에 접근했는지 직접적으로 추적하기 용이합니다.</p>
</blockquote>

<h4 id="interface-엔드포인트를-통한-접근-로그-예시"><strong>Interface 엔드포인트를 통한 접근 로그 예시</strong></h4>

<p>Interface 방식은 트래픽이 엔드포인트의 ENI를 통해 나가므로, 로그에 <strong>Interface 엔드포인트 ENI의 프라이빗 IP</strong>가 기록됩니다.</p>

<pre><code class="language-log">... [17/Aug/2025:11:30:25 +0000] 10.0.2.123 ... GET.OBJECT my-other-object.txt ... "vpce-fedcba9876543210f"
</code></pre>

<ul>
  <li><code class="language-plaintext highlighter-rouge">remoteIp</code>: <code class="language-plaintext highlighter-rouge">10.0.2.123</code> (Interface 엔드포인트 ENI의 IP)</li>
  <li><code class="language-plaintext highlighter-rouge">vpc_endpoint_id</code>: <code class="language-plaintext highlighter-rouge">vpce-fedcba9876543210f</code> (사용된 Interface 엔드포인트의 ID)</li>
</ul>

<blockquote>
  <p><strong>고려사항</strong>: 로그만으로는 어떤 EC2 인스턴스가 요청을 보냈는지 직접 알 수 없습니다. 실제 소스를 추적하려면 해당 시간에 어떤 인스턴스가 이 엔드포인트를 사용했는지 알기 위해 <strong>VPC 흐름 로그(Flow Logs)</strong>를 추가로 분석해야 할 수 있습니다.</p>
</blockquote>

<h3 id="5-최종-선택-가이드">5. 최종 선택 가이드</h3>

<ul>
  <li><strong>Gateway 엔드포인트 추천 경우:</strong>
    <ul>
      <li><strong>비용이 가장 중요한 고려사항일 때</strong></li>
      <li>VPC 내부의 리소스에서만 S3에 접근하는 경우</li>
      <li>S3 접근 로그에서 소스 인스턴스의 IP를 직접 확인하고 싶을 때</li>
    </ul>
  </li>
  <li><strong>Interface 엔드포인트 추천 경우:</strong>
    <ul>
      <li><strong>온프레미스 데이터 센터에서 VPN/Direct Connect를 통해 S3에 접근해야 할 때</strong> (가장 핵심적인 사용 사례)</li>
      <li>VPC 피어링으로 연결된 다른 VPC에서 S3에 접근해야 할 때</li>
      <li>보안 그룹을 사용하여 VPC 내에서도 소스 IP 기반의 세밀한 네트워크 제어가 반드시 필요할 때</li>
      <li>비용과 로그 추적의 복잡성을 감수하더라도 아키텍처의 유연성과 확장성이 더 중요할 때</li>
    </ul>
  </li>
</ul>

<h3 id="6-결론">6. 결론</h3>

<p>Gateway와 Interface 엔드포인트는 각각 명확한 장단점을 가지고 있습니다. <strong>비용 효율성과 명확한 로그 추적을 원한다면 Gateway 방식</strong>이 정답에 가깝고, <strong>온프레미스 연결을 포함한 높은 수준의 유연성과 강력한 네트워크 제어가 필요하다면 Interface 방식</strong>을 선택해야 합니다.</p>

<h3 id="7-참고-자료-references">7. 참고 자료 (References)</h3>

<ul>
  <li><strong>AWS 공식 문서: VPC Endpoints</strong>
    <ul>
      <li><a href="https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints.html">https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints.html</a></li>
    </ul>
  </li>
  <li><strong>AWS 공식 문서: Gateway Endpoints</strong>
    <ul>
      <li><a href="https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints-gateway.html">https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints-gateway.html</a></li>
    </ul>
  </li>
  <li><strong>AWS 공식 문서: Interface Endpoints (AWS PrivateLink)</strong>
    <ul>
      <li><a href="https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints-interface.html">https://docs.aws.amazon.com/vpc/latest/userguide/vpc-endpoints-interface.html</a></li>
    </ul>
  </li>
  <li><strong>AWS 공식 문서: S3 서버 접근 로깅</strong>
    <ul>
      <li><a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/ServerAccessLogging.html">https://docs.aws.amazon.com/AmazonS3/latest/userguide/ServerAccessLogging.html</a></li>
    </ul>
  </li>
</ul>]]></content><author><name>Francesco</name></author><category term="aws" /><category term="AWS" /><category term="VPC Endpoint" /><category term="S3" /><summary type="html"><![CDATA[AWS 환경에서 VPC(Virtual Private Cloud) 내의 리소스(예: EC2 인스턴스)가 S3 버킷과 통신해야 하는 경우는 매우 흔합니다. 이때 인터넷을 통하지 않고 AWS 내부 네트워크를 통해 안전하고 빠르게 통신하기 위해 VPC 엔드포인트를 사용합니다.]]></summary></entry></feed>