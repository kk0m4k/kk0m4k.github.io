{% assign navigation = site.data.navigation[include.nav] %}

<nav class="nav__list">
  {% if page.sidebar.title %}<h3 class="nav__title" style="padding-left: 0;">{{ page.sidebar.title }}</h3>{% endif %}
  <input id="ac-toc" name="accordion-toc" type="checkbox" />
  <label for="ac-toc">{{ site.data.ui-text[site.locale].menu_label | default: "Toggle Menu" }}</label>
  <ul class="nav__items">
    {% for nav in navigation %}
      <li>
        <div class="nav-item-header">
          {% if nav.children %}
            <span class="toggle-icon main-toggle" data-toggle="nav-main-{{ forloop.index }}">+</span>
          {% else %}
            <span class="tree-icon">-</span>
          {% endif %}
          {% if nav.url %}
            <a href="{{ nav.url | relative_url }}"><span class="nav__sub-title">{{ nav.title }}</span></a>
          {% else %}
            <span class="nav__sub-title">{{ nav.title }}</span>
          {% endif %}
        </div>

        {% if nav.children != null %}
        <ul class="nav-tree" id="nav-main-{{ forloop.index }}" style="display: none;">
          {% for child in nav.children %}
            {% assign child_index = forloop.index %}
            {% assign is_last = forloop.last %}
            <li class="nav-tree-item">
              <span class="tree-icon">
                {% if is_last %}└{% else %}├{% endif %}─
                {% if child.children %}
                  <span class="toggle-icon" data-toggle="nav-{{ nav.title | slugify }}-{{ child_index }}">+</span>
                {% endif %}
              </span>
              <a href="{{ child.url | relative_url }}"{% if child.url == page.url %} class="active"{% endif %}>{{ child.title }}</a>
              
              {% if child.children != null %}
                <ul class="nav-subtree" id="nav-{{ nav.title | slugify }}-{{ child_index }}" style="display: none;">
                  {% for subchild in child.children %}
                    {% assign subchild_index = forloop.index %}
                    {% assign is_sublast = forloop.last %}
                    <li class="nav-subtree-item">
                      <span class="tree-icon">
                        {% if is_last %}&nbsp;&nbsp;&nbsp;&nbsp;{% else %}│&nbsp;&nbsp;&nbsp;{% endif %}
                        {% if is_sublast %}└{% else %}├{% endif %}─
                      </span>
                      <a href="{{ subchild.url | relative_url }}"{% if subchild.url == page.url %} class="active"{% endif %}>{{ subchild.title }}</a>
                    </li>
                  {% endfor %}
                </ul>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
        {% endif %}
      </li>
    {% endfor %}
  </ul>
</nav>